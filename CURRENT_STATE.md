# Learning Compass — Current State

## Handoff

- **Home banner removal:** Removed and verified locally at Mahmood's request. Home opens directly into lessons and Queue context; deployment is pending.

- **Home spacing correction:** Deployed and verified; the desktop layout keeps RSS/resurfacing below lessons and confines sticky Queue context to its own column. See the correction entry below for evidence.

- **Learning calendar and Obsidian download:** Deployed; see the release entry below for live checks and the implementation entry for scope. The explicit download includes book/chapter notes while routine Hermes archiving retains its exclusion.

- **Start here:** `AGENTS.md` owns the working rules, `PROJECT_CONTEXT.md` the durable product model, and the README contribution map points to source files and focused tests. Read older entries below only when their history is relevant.
- **Frontend audit:** The isolated Impeccable repair set reached 40/40 after independent design/technical confirmation and closure of the final two polish deductions. See the final-confirmation entry below for scope, evidence, and limits. The combined release includes these repairs.
- **Frontend:** The deployed Impeccable repairs retain Continuum and the five-root navigation while correcting reading hierarchy, responsive layouts, accessible controls, source identity, and recovery. Workspace code is lazy-loaded; the deployed PWA shell is v63.
- **Last recorded deployment:** `docs/release-snapshot.json`, observed `2026-09-05T16:05:15.086Z`, records Worker `0f89d577-5287-452b-9ba2-d8ccb3eb0fd1` from clean `0b432d5` with PWA shell v63. Live checks passed at release time; recheck for later operational work.
- **Local environment:** lockfile dependencies are restored. The deleted `.tmp/` recovery files and separate WhatsApp repository are absent; historical references to those paths are no longer usable. Generated `dist/` output was removed after the successful verification build. Dependency upgrades retain the holds in `docs/dependencies.md`.

## Home banner removal — 2026-09-05 (verified locally)

Removed the large Today banner, aggregate readiness/Queue counters, their calculation, and unused styles. Home starts directly with current lessons and Queue context; its screen-reader heading remains available. Removed the former banner-to-content padding and aligned the sticky sidebar to the lesson panel. The PWA shell advances to v64.

`npm run verify:fast`, changed-file formatting, and `git diff --check` pass. The focused `E2E_FOCUS=quality npm run test:e2e` suite passes against the finished build; an earlier run timed out loading Files during the build update. Read-only previews with production content confirm no banner or empty inset, aligned desktop columns, preserved stacked order, and no overlap or horizontal overflow at 390, 1180, 1181, and 1600px in light/dark themes. The lesson panel moves from 240px to 90px at desktop width. Initial JavaScript is 47.45 KB gzip; CSS is 108.78 KB gzip. Pre-deploy readiness and budget checks pass without blockers.

## Home spacing correction — 2026-09-05 (deployed)

Home's desktop Queue context now spans both content rows while the lesson row stays content-sized. RSS/resurfacing occupies the main column beneath lessons, removing the blank gap caused by a tall Queue card and preventing the sticky sidebar from covering RSS while scrolling. The stacked order at 1180px and below remains lessons, Queue, secondary content, then calendar. The PWA shell advances to v63.

`npm run verify:fast`, changed-file formatting, `git diff --check`, and `E2E_FOCUS=quality npm run test:e2e` pass. The browser regression covers tall Queue content, lesson-to-feed spacing, sticky overlap, and stacked order at five widths. Read-only previews with the built CSS and current production content pass desktop/mobile light/dark checks, the 1180/1181px boundary, 135% text sizing, and a longer lesson list. At 1600px, the lesson-to-feed gap shrinks from 272px to the intended 42px. The layout detector reports no findings. Initial JavaScript remains 47.58 KB gzip; CSS is 109.12 KB gzip. Pre-deploy readiness and budget checks pass without blockers.

Deployed clean `0b432d5` as Worker `0f89d577-5287-452b-9ba2-d8ccb3eb0fd1`, reusing the passing fast gate. The post-release snapshot has no readiness or budget blockers, and all 11 installed smoke checks pass. A production browser confirms the 42px gap and no sticky overlap after scrolling; the published stylesheet matches the local build byte for byte and shell v63 is live. The prior Worker `7fd27a58-d3b2-4560-b3c3-444898c101e6` is the rollback target. No schema or learning-content change was required. Generated build output was removed after verification.

## Calendar, Obsidian export, and frontend release — 2026-09-05 (deployed)

At Mahmood's explicit request, deployed clean main commit `a6c74db` as Worker `7fd27a58-d3b2-4560-b3c3-444898c101e6`, including the calendar/export and completed frontend repairs. PWA shell v62 is live. The prior Worker `9145072d-ce6c-4744-b5c2-a16550a09b7d` is the application rollback target. No schema migration or learning-content mutation was needed.

Reused the passing combined fast gate, focused regression/E2E, and Hermes checks on unchanged inputs; rebuilt the removed client output successfully. Pre/post release snapshots confirm healthy liveness/readiness, all 79 migrations in parity, all 30 required schema objects, and budget headroom without blockers. All 11 installed live smoke checks pass. Production calendar month/day reads and a real Thread Obsidian manifest pass; that sampled Thread correctly reports three sources awaiting extracted notes. All 13 published shell/JavaScript/CSS files match the local build byte for byte. Initial JavaScript remains 47.58 KB gzip including API/Preact, CSS 109.09 KB gzip, and the lazy ZIP module 0.74 KB gzip. Wrangler's initial credential refresh failure cleared through its existing OAuth login before deployment; no new credential was added. `docs/release-snapshot.json` records the observed release. Generated build output was removed after verification.

## Learning calendar and Obsidian Thread download — 2026-09-05 (local, verified)

Home now includes a monthly Cairo/DST-aware activity calendar, linked day details, pagination, and honest empty/error states. It projects existing completion/reopening, note-save, and recall events without recording passive reading or changing progression. Threads offer an Obsidian ZIP of existing notes, faithful handwriting reflections, source pages, and an ordered curriculum index, with optional companions and Level filtering. Whole-Thread and selected-Level exports have distinct folder identities. Explicit downloads include book/chapter notes as Mahmood requested; routine Hermes archive rules remain unchanged. Notes Extractor, site-operator guidance, and durable Hermes memory reflect that exception.

Verified: 43 focused unit/contract tests across calendar/export, Home completion, and agent control; `npm run verify:fast`; Hermes contracts/prompt checks; changed-file formatting and diff checks; focused calendar/export E2E against disposable Worker/D1 covering Cairo history, lesson navigation, actual ZIP download, optional PDF inclusion, unavailable-file/API errors, and desktop/light plus mobile/dark layouts. ZIP bytes/CRC and Unicode filenames also pass Python zipfile readback. The first E2E run exposed an ambiguous export-selector label, corrected before the passing rerun. No full release, migration, or unrelated browser suite was run. The latest measured initial JavaScript is 47.58 KB gzip including API/Preact, CSS 109.09 KB gzip, with ZIP code lazy-loaded at 0.74 KB gzip. No dependencies, schema, deployment, or production records changed; unrelated local edits are preserved.

## Impeccable final confirmation — 2026-09-05 (local, verified)

Recall now supports focused 1–4 grading, guards repeated native Enter/Space, keeps reveal state bound to the exact card revision, restores focus through save/retry/next-question/completion states, and puts the mobile question/Reveal action before secondary filters. Notes keeps secondary actions in Study tools, closes all expanded claim/synthesis controls with Escape, and preserves interrupted edits as per-note browser drafts. Explicit Save/Cancel, focus restoration, named content fields, formatting help, and readable direction choices complete the authoring flow. Arabic reading blocks carry language metadata independently of direction. The mobile dock uses a contrast-corrected semantic pair. Uploaded files expose readable owner identity, their own details route, and direct format access; generic file copy no longer exposes storage terminology.

Independent isolated Assessment A found concrete authoring/retrieval gaps at 31/40, confirmed their repair at 38/40, then closed the two exact wording/selector-width deductions at **40/40**. Assessment B independently confirmed its dock-contrast, reading-language, and expanded-tools Escape findings resolved. All ten Nielsen heuristics apply. This is a heuristic score for the audited revision, not an accessibility certification or a score for concurrently developed features. The detector's once-only inventory contains 459 style records (five warnings and 454 advisories), with documented semantic-quotation/configuration-input exceptions; these are not 459 product defects.

Verification used an isolated checkout based on `72ad941` plus only these repairs because unrelated calendar/export work was changing the shared checkout. The isolated `verify:fast`, 43 focused reader/theme/artifact tests, expanded frontend-quality E2E at 1440/1024/975/768/390px, dead-code, instruction/Hermes contracts, formatting, and diff checks pass. Browser tests prove actual navigation/reload draft recovery, unchanged canonical text before Save, failed-save retry, Save/Cancel focus, native held-key safety, direct file access, bilingual language, and mobile contrast. The final copy/selector changes passed a fresh build, focused lint/format, and rendered desktop/mobile checks: 556px/354px selector widths exceed the 202px full-label requirement, with no overflow. The earlier full route/material/theme/reading/Android/offline pass remains recorded in the preceding repair entry; it was not repeated for this focused follow-up.

The isolated audited build has 46.25 KB initial JavaScript gzip including API/Preact and 108.55 KB CSS gzip. After the independent calendar/export commit `7d484b6` landed, the combined checkout also passed `verify:fast` and Hermes checks; its initial JavaScript is 47.58 KB gzip and CSS is 109.09 KB gzip. All 14 audited application/test files match the independently reviewed repair set exactly. No dependency, schema, deployment, production data, or companion publication changed in this task. The additive Files `owner_title` projection and browser draft semantics are documented, and installed Hermes site-operator guidance is synchronized. Reports, scan inventory, logs, measurements, screenshots, and the exact repair patch are retained under the ignored `.impeccable/critique/` archive. Concurrent work and its outputs are preserved; the isolated preview, disposable data, and temporary checkout are removed after verification.

## Impeccable frontend repairs — 2026-09-05 (local, verified)

Home leads with one current lesson per active Thread, real readiness, explicit completion and exact-lesson undo, then the active Queue source. Authored Level numbers are consistent. Lessons render safe semantic study text before supporting controls; Arabic direction/leading, Notes reading-time agreement, named controls, route-preserving skip focus, and responsive touch targets are covered. Queue and Books disclose secondary diagnostics/offline actions. Files exposes source/book/chapter identity, canonical branches, ordered chapters, and opt-in history; immutable published pairs no longer offer an unsupported Delete action. Recall resolves branch labels without changing legacy mutation identity and explains each grade. Search announces pending/results/errors and uses native link focus; companion and preference matches use readable identity. Preferences has one usable preview, three initial styles with all eight available, labeled tuning sliders, and concrete reminder troubleshooting. Atlas keeps selection focus and opens canonical branch dossiers.

Learn, Library, and Settings load on demand, with clear chunk-load recovery and visited-workspace offline caching. Initial JavaScript is 46.13 KB gzip including Preact/API, down from 145.91 KB; CSS is 108.57 KB gzip. The graph remains lazy. No dependency, schema, deployment, production learning state, companion publication, or reminder-delivery key was changed. Additive Home source-branch, Recall branch-context, Files owner/branch, and Search file-identity fields are implemented locally and documented in `docs/API.md`; the installed Hermes site-operator guidance is synchronized.

Verified: 95 focused unit tests, `npm run verify:fast` (lint/instructions, TypeScript, build), dead-code checks, focused frontend-quality E2E at 1440/1024/975/768/390px, and the full routes/materials/theme/reading/Android/offline E2E suite pass. The full suite reports five root destinations and 16 tested internal mode states; the route registry retains its 12 grouped modes. Instruction/Hermes contracts, formatting, and diff checks pass. Older E2E assertions now follow actual Level labels, Files rows, style disclosure, mobile theme/Search/Capture controls, and Atlas settings; the theme helper sets its own desktop viewport and touch checks tolerate only 0.01px numerical rounding. A transient lesson-tools reload timeout did not recur in the final full run. No full unit/integration/migration/manager release suite was run.

Independent Impeccable assessment improved from 25/40 to **33/40**, with no remaining demonstrated priority product defect. The report retains optional Recall keyboard acceleration and quieter note-tool grouping plus explicit usability/assistive-technology limits; it does not claim 40/40. Critique snapshots, independent reports, detector inventory, verification logs, and responsive evidence remain in the ignored `.impeccable/critique/` archive. Generated build/test output and temporary preview/test servers were removed after verification.

## Repository guidance and architecture checks — 2026-09-05 (local, verified)

Added a shared contribution map and current handoff, ESLint checks for the existing layer boundaries, and `npm run verify:instructions` inside lint. The portable check reuses the Hermes instruction auditor without requiring installed skills. Removed stale mandatory Lite Visual review paperwork, fixed deployment/migration claims in durable guidance, and a forced reply template from the OpenCode adapter. Active repository and installed Worker Ops guidance now report client bundle sizes without a fixed cap. No application behavior, API, schema, or production state changed.

The lint check found maintenance importing scheduled reminder delivery from the HTTP module. Delivery and its shared helpers now live in `src/services/notifications.ts`; maintenance and notification routes use that service. Existing notification security, redaction, and recall projection tests follow the new owner.

Verified: 35 focused tests, `npm run verify:fast` (lint including eight instruction documents, TypeScript, one build), `npm run dead-code`, `npm run verify:hermes`, changed-file formatting, and diff checks. The moved delivery logic compiles identically and HTTP route bodies are unchanged. Dependency audit reports zero vulnerabilities; `npm outdated` retains the nine documented upgrade holds. Vite still reports emitted sizes and its ordinary large-chunk advisory, without an enforced bundle-size cap. No full browser/integration/manager suite or deployment ran; no new dependency was installed.

## Local dependencies restored — 2026-09-05

At Mahmood's request, `npm ci` restored the lockfile dependencies (278 packages installed, zero audit vulnerabilities). TypeScript checking passes and both dependency manifests are unchanged. The checkout now occupies about 531 MiB, including 451 MiB in `node_modules/`; `.tmp/`, the separate WhatsApp repository, and `dist/` remain absent. This supersedes the dependency-reinstallation requirement below. No build or deployment ran.

## Extended local cleanup — 2026-09-05

At Mahmood's explicit request, removed the complete `.tmp/`, `whatsapp-insights-site/` (including its separate Git repository), `node_modules/`, and `dist/` directories. This supersedes the preservation statements in the earlier cleanup entry: the 151 local recovery files, database dump, worktree archives, patches, and release receipts under `.tmp/` are gone. Historical entries referencing `.tmp/` retain their original observations but those local paths are no longer available. Backups outside this checkout were not targeted. All four directories are absent; tracked application sources and dependency manifests remain intact. Run `npm ci` before future local builds, tests, or extraction workflows that depend on this repository's Node packages. Verified the exact deletion scope, directory absence, Markdown formatting, and Git diff; no application tests, rebuild, dependency reinstall, or deployment ran.

## Local artifact cleanup — 2026-09-05

Removed 302 ignored files (12.68 MiB): the orphaned retired bridge bytecode under `tools/`, old `.playwright-cli/` browser output, `.impeccable/` review artifacts, `test-results/` screenshots, `.codex-tmp/` one-off scripts/caches, and 54 merge-stage code copies under `.tmp/release-20260905/`. All cleanup targets are absent. The remaining 151 `.tmp/` recovery files retain their exact pre-cleanup SHA-256 hashes, including the database export, worktree archives, patches, and release receipts. The separate WhatsApp repository, installed dependencies, and current build remain present. No tracked application file or production state changed; verification used the exact removal inventory, recovery-file hashes, changed-file formatting, and diff checks. No application tests or deployment were needed.

## Ponytail simplification — 2026-09-05 (local, verified)

Installed the six instruction-only Ponytail skills (`ponytail`, `ponytail-audit`, `ponytail-review`, `ponytail-debt`, `ponytail-gain`, and `ponytail-help`) under `~/.codex/skills`, byte-verified against DietrichGebert/ponytail revision `974d940a1c5344210874150b98ff0d2c861fab6a`. Applied full mode, repository reachability/duplication review, final diff review, and the debt scan. No lifecycle hooks or application dependencies were added. The upstream gain card describes an older benchmark; it is not evidence of Learning Compass savings.

Books and sources now share the identical compact offline artifact projection. Router links call their existing normalization directly. Removed unused label/link helpers, test reset, forwarding aliases, obsolete energy/format constants, and the unused standalone memory-evidence writer; the active atomic write path is unchanged. The application diff removes 93 net lines. The extended Knip scan fell from 38 to 20 unused exports, 25 to 21 exported types, and three to one duplicate-export group; the remainder has internal uses, a dynamic checksum test consumer, or active confidence-alias callers. No further implementation deletion is justified by those findings. The debt scan found zero `ponytail:` markers.

Verified: 62 focused tests including the new snapshot identity/receipt-omission regression, `npm run verify:fast` (lint, TypeScript, one build), `npm run dead-code`, changed-file formatting, and diff checks. Base JavaScript is 145.91 KB gzip including Preact; Vite retains its main-chunk size advisory. Dependency audit reports zero vulnerabilities; available upgrades retain the documented holds. No full/browser/Hermes suite ran because no product, HTTP, schema, storage protocol, or Hermes instruction contract changed. This cleanup is not deployed; production remains the v61 release below, and no production data was changed.

## Compass logo theme toggle — 2026-09-05 (deployed)

The desktop Compass brand and mobile logo now toggle light/dark mode through the existing appearance settings. The browser remembers the last palette in each mode, including custom day/night colors. Switching preserves the current workspace and typography, keeps Preferences synchronized, supports Enter/Space, and provides a 44px mobile target. Failed saves restore the previous theme with a visible message; network failures use the existing offline mutation queue. A delayed startup settings read cannot overwrite a newer theme choice.

Worker `9145072d-ce6c-4744-b5c2-a16550a09b7d` is deployed from clean main commit `ebaa50d` with PWA shell v61. Pre/post deployment liveness, readiness, migration parity, and budget checks passed with no blockers; all 11 installed smoke checks passed. The live main JavaScript, Preact chunk, CSS, and service worker match the verified local build byte for byte, and the live shell references those exact assets. `docs/release-snapshot.json` records the deployment. Application rollback is Worker `ae8753cf-33d2-42f2-8715-9e406a6136b8`.

Verified: 20 focused theme/settings tests, lint, TypeScript, production build, and focused Chromium E2E for mouse/keyboard activation, reload persistence, preset/custom palette restoration, Preferences synchronization, save-failure recovery, desktop/mobile layouts, and enlarged mobile text. Light/dark screenshots were inspected. The release cache bump also passed three Android/PWA tests and the focused shell-version regression; the build was refreshed, and unchanged checks were reused. Base JavaScript is 146.07 KB gzip including Preact. Changed-file formatting and diff checks pass. No API contract, schema, or Hermes workflow changed.

## Worktree consolidation — 2026-09-05

All registered worktree histories are incorporated into main. Nine historical commits across eight detached tips were confirmed patch-equivalent to changes already on main; merge `82f5a53` records their ancestry while preserving the exact source tree. All existing checkouts were clean and had no processes running inside them. Removed 40 extra checkouts and pruned four stale entries; only `/home/mahmud/recommendations-worker` remains. The verified Git bundle, original inventory, 22 archives of ignored local data/diagnostics, checksums, and cleanup receipt are in `/home/mahmud/backups/learning-compass/worktree-cleanup-20260905T033042Z/`. Reproducible dependencies, build output, and Python caches were excluded from those archives. No application code or deployed version changed; verification used patch equivalence, exact tree identity, ancestry checks, backup validation, final worktree inventory, formatting, and diff checks.

## Hermes skills — 2026-09-05 (loaded-size limits removed)

Removed every per-skill loaded-byte limit and the combined router/operator cap from the canonical contract and verifier, superseding the earlier Lite Visual 8,000-byte allowance. Skill content no longer fails verification because of its size. Fixed-prompt, skill-index, tool-schema, memory, ownership, instruction validity, and installed-parity checks remain active. Hermes contract/fixed-prompt verification, all 10 focused prompt-budget/instruction-audit tests, changed-script lint, formatting, and diff checks pass. This is a local verification-policy change; the deployed Worker below is unchanged and no redeployment is needed.

## Current truth — 2026-09-05 (UI refinement deployed)

Worker `ae8753cf-33d2-42f2-8715-9e406a6136b8` is deployed from clean main commit `612d2ad` with PWA shell v60. It implements the accepted Impeccable critique: a single-column Notes reader with optional contents/study tools and verbatim receipt provenance, Books next-chapter priority and section jumps, collapsed compact lesson navigation, clearer Home/Threads hierarchy, canonical branch labels, visible format names and 44px actions, Threads as Learn's default, eight local recent-search selections, and readable Atlas domain entry. Note editing preserves canonical branch IDs; explicit default-mode links no longer trigger recovery notices. No schema migration or production content change was needed. Application rollback is Worker `69dcc110-2d26-4a76-b4cb-df41b1847c4e`.

Post-deployment liveness, readiness, and free-tier budget checks pass with no blockers. All 11 installed live smoke checks pass; the deployed main JavaScript, Preact chunk, CSS, and service worker match the verified local files byte for byte. All 79 migrations and 30 required schema objects remain current. `docs/release-snapshot.json` records the deployment and clean source commit. The already successful application checks below were reused; only the changed Hermes budget contract and live release checks were rerun for deployment.

Verification passed: 39 focused unit tests; lint, TypeScript, and production build; the full responsive/offline Chromium E2E suite; and the focused reading E2E recheck after the final Atlas edge-padding adjustment. Desktop, tablet, mobile, and dark-theme screenshots were reviewed. Base JavaScript is 145.74 KB gzip, below the 150 KB limit. Changed-file formatting, diff checks, and the focused Impeccable component scan passed. The installed Site Operator route reference agrees with the new UI contracts. The Hermes contract and fixed-prompt checks now pass after the authorized Lite Visual loaded-entry budget increase from 6,000 to 8,000 bytes (installed size: 7,514 bytes). Other skill and fixed-prompt limits are unchanged.

## Workflow update — 2026-09-05 (fast commits and releases)

Verified with the fast gate, four focused release-tool tests, Hermes contract/prompt checks, changed-file formatting, and diff checks. Full suites were intentionally not run for this workflow change.

At Mahmood's request, routine releases now use `npm run verify:fast` (lint, typecheck, build) through `npm run deploy`, plus affected regression tests and the existing short live checks. `npm run deploy -- --full` retains the aggregate suite for high-risk changes or explicit requests. CI uses fast checks, skips Markdown-only commits, cancels superseded runs, and runs portable unit/browser suites only through its manual `full` option. Repository and installed Worker Ops instructions agree: no routine full backup for code-only deployment, no repeated checks for unchanged inputs, no waiting for CI by default. Migrations and risky data/storage changes retain recovery prerequisites. This changes development/release tooling only; the deployed application version below is unchanged.

## Previous deployed release — 2026-09-05 (combined release and branch consolidation)

Worker `69dcc110-2d26-4a76-b4cb-df41b1847c4e` is deployed from clean main commit `a24a864` with PWA shell v59. It combines the material notebooks, companion retirement, bounded Level reads, persistent Feeds Skip, and the material-editor focus fix. All 79 migrations through `0077_feed_entry_dismissals.sql` match production; all 30 required schema objects are present. Readiness is healthy with no release blockers. `docs/release-snapshot.json` records the live version and clean deployment source. Application rollback is Worker `0685ad21-56d8-48d0-81c6-5db846d87bfc`; the additive Feeds table can remain.

The full `npm run verify:release` gate passed: quality, 554 unit tests, TypeScript, production build, six integration suites, responsive/PWA/offline browser E2E, clean and idempotent migration rehearsal, Hermes contracts/prompt budgets, 40 manager tests, 48 installed-client tests, seven native-adapter tests, syntax, and diff checks. Base JavaScript is 145.01 KB gzip, below the 150 KB limit. GitHub CI passed for `a24a864`. Post-deployment verification passed all 11 installed smoke checks, 13 additional API reads, three shell assets, and PWA v59.

Native Hermes reads the seven-lesson Orientation Level in exactly 2,449 compact JSON bytes, with the previous lesson IDs, statuses, and primary-source mappings unchanged. Regression coverage includes both a 1.2 MB lesson body and a 1.2 MB artifact receipt; the full-path API remains compatible. The fifth existing companion pair is still ready, with its artifact identities and receipt/file hashes verified through the installed uploader's canonical GET recovery. Ambiguous 500/timeout outcomes now reconcile without an automatic second upload; unresolved outcomes remain explicit and non-retryable. The original 500's underlying server cause is unknown. This release did not regenerate companions or change lesson progress.

The pre-migration recovery snapshot `backup_20260905T020546Z_reconciled` contains 1,908 remotely acquired and checksum-verified artifacts (305,922,882 bytes) and a matching D1 export. An in-flight publication made the first export two artifacts older than its R2 inventory; that failed snapshot was preserved, and a fresh export was matched by every artifact ID, key, filename, and size. The reconciled disposable restore passed SQLite integrity, zero foreign-key violations, and all 29 pre-migration schema objects. Recovery evidence is under `/home/mahmud/backups/learning-compass/snapshots/`; the immediate pre-apply Time Travel bookmark and full release logs are under `.tmp/finish-release-20260905/`.

All five feature branches are merged and deleted; only main remains locally and remotely. Existing historical worktree drafts were archived without restoring obsolete implementations, and every extant project worktree is clean. Verified Git bundles and the cleanup receipt are under `/home/mahmud/backups/learning-compass/`. Installed Hermes identity, routing, site client, authoring guidance, and upload recovery are synchronized; snapshots of the 14 affected installed files are under `hermes-sync-20260905/final-installed/` there. The native adapter is unchanged and byte-identical to its installed copy.

## Verification history — 2026-09-05 (Hermes routing reliability)

Installed Hermes is dedicated to Learning Compass. The router and site-operator entry points total 9,210 bytes, down from 17,884; write-specific safeguards remain in `references/write-operations.md`. Common reads distinguish Thread lists, hub overviews, exact Thread/Level details, resume questions, and Queue inventory. Follow-ups preserve displayed order and IDs; corrections replace the target; inspection never changes learning state. The checked-in SOUL source matches the installed identity.

The native Queue read exposed a 19,514-byte record response that exceeded the 12,000-byte output cap. The installed first-party client now supports comma-separated leaf fields (for example `items.*.id,video_title,learning_state`) without raising output or transport limits. Actual native dispatch passed for three Threads, three hub paths, and one Queue item; the Queue result was 434 bytes. No production data was mutated.

The evaluation slate now has 31 cases, adding ten short-request, typo, Arabic, empty-list, and follow-up scenarios. Deterministic verification passes 40 manager tests and 48 installed-client tests, plus skill validation, quality, Hermes contracts, prompt budgets, and diff checks. The evaluator copies valid skill links into isolation, skips dangling retired links, preserves `HERMES_TURN_ID` for the real client ledger, and records bounded terminal commands for diagnosis.

Real-model diagnostics are not a release certificate: the first ten-case trial passed seven and exposed fixture CLI guidance failures; a focused resume/follow-up replay passed both after actionable client guidance. The Thread overview uses one hub read instead of the baseline's hub plus two unnecessary full-path reads. Additional mutation diagnostics found a missing evaluator turn identity (fixed) and overly literal historical response grading. The small higher-reasoning comparison did not establish an improvement, so the model, medium reasoning, and platform tool settings remain unchanged. No broad rerun or deployment is claimed here; the release task owns integration and deployment.

## Verification history — 2026-09-05 (combined release before deployment)

Main now integrates the deployed material notebooks and companion retirement, the bounded metadata-only Level read, and the selected Feeds split reader with persistent Skip. The combined PWA shell is v59. Migration `0077_feed_entry_dismissals.sql` is additive and awaits the verified pre-migration backup and release gate. No new companion retirement or lesson-progress change is part of this release. Earlier dated entries retain their original task status; this entry owns the combined release status.

## Current truth — 2026-09-05 (bounded Level read deployed; Orientation companions published)

Implementation `84685a6` on `fix/bounded-level-read`, based on deployed baseline `f5d0c1b`, is deployed as Worker `0685ad21-56d8-48d0-81c6-5db846d87bfc` at 100%. The paginated metadata-only `path?view=lessons&stage_id=...` projection preserves the full-path API and avoids hydrating oversized lesson bodies and artifact receipts. The live Systems Thinking Orientation response contains all seven lessons in 2,450 bytes of formatted client output. Application rollback is Worker `2fb36281-639c-47de-ba82-75f6eb10fec5`; the primary checkout remains unchanged.

The existing aggregate release gate passed: quality, 551 unit tests, TypeScript, build, six integration scenarios, browser/PWA/offline E2E, migration rehearsal, Hermes contracts, 38 manager cases, 46 installed-client tests, seven native-adapter tests, syntax, and final diff checks. The integration fixture covers a 1.2 MB lesson body, pagination, ownership, unchanged statuses, and full-path compatibility. Post-deployment readiness, deployment-version verification, and all 11 live smoke checks passed. No second deployment or repeat full gate was needed during handoff recovery.

Five existing Arabic HTML/PDF pairs were published and canonically verified for the primary sources of Feedback loops, Leverage points, Second-order effects, Modeling exercise, and One real-world application. The fifth upload initially returned HTTP 500; its canonical source showed no ready pair, and one retry of the same finished pair succeeded. All five distinct pairs are ready. Verification is integrity-only; editorial/layout audits were not run, and no companion was regenerated. Before/after Level reads preserve every lesson status and primary-source mapping: two completed lessons, Feedback loops in progress, and four not started. No lesson progress was changed.

The manifest, release/publication receipts, canonical before/after reads, and existing ten-file `level-0-companions.zip` remain under `~/.hermes/work/orientation-companions/`; `completion-receipt.json` records the complete outcome and the fifth-publication recovery.

## Current truth — 2026-09-05 (standalone companion retirement, deployed)

Worker `2fb36281-639c-47de-ba82-75f6eb10fec5` deployed at `2026-09-05T01:05:22Z` from source `ff38c8e` on `fix/retire-companion-pairs`, with PWA shell v58. It preserves the deployed scoped material notebooks from `7d4f3eb` and adds guarded, atomic retirement of complete standalone companion pairs. Application rollback is Worker `2cc5a728-7f2e-4a63-bda1-e407ce2b8414`. No migration or dependency installation was needed; the primary checkout remains unchanged.

On explicit request, 32 standalone pairs (64 artifacts) were retired across Feedback loops, Leverage points, Second-order effects, Modeling exercise, and One real-world application. This includes the 25 initially selected pairs and seven older ready pairs that became visible as fallbacks. All 32 guarded mutation receipts verify retirement. The final live Orientation read at `2026-09-05T01:09:09Z` has zero HTML/PDF companions in those five lessons, preserves all 25 source memberships and original URLs, and preserves every lesson status, the two earlier lessons, and direct notes/files/cards. Progress remains 2/7 completed, with Feedback loops in progress. Files and recovery history remain stored; corpus-managed pairs were not modified. Exact requests, receipts, before/after projections, and verification are retained under `~/.hermes/work/retire-orientation-pairs-20260905/`.

The complete `verify:release` gate passes for the deployed source: quality checks, 549 unit tests, TypeScript, build, six Worker/D1 integrations, browser/PWA/offline E2E, Hermes contracts and prompt budgets, clean and idempotent migrations, agent contracts, 38 manager cases, 46 installed-client tests, seven native-adapter tests, syntax, and diff/credential checks. The offline note assertion now allows the API's 30-second network deadline to complete before checking its durable queued result and exactly-once sync. Live health, readiness, budget policy, 11 smoke checks, v58 delivery, and exact retirement capability/readback pass. Migration parity remains exact at 78 files through `0076`.

Backup `backup_20260905T000659Z` contains 112,183,823 D1 bytes and 1,898 R2 objects (302,465,210 bytes); its restore passes integrity and foreign-key checks and covers all 64 retired files (11,524,098 bytes). Lite Visual wording fits its existing 6,000-byte budget at 5,990 bytes with authoring rules intact. The site-operator reference documents exact pair retirement and verification of older fallbacks within the requested source scope.

## Current truth — 2026-09-05 (scoped material notebooks, deployed)

Notes, Files, and Recall now use accessible tabs and full-width editors across Thread, Level, and Lesson notebooks. The lesson section removes its repeated title, ownership badge, and nested border. Draft input survives tab switches, errors remain beside the form, successful saves update the list and count, and queued offline notes/cards clear the submitted form while explaining that sync is pending. Repeated submissions are guarded; file failures preserve the chosen upload; Arabic validation exposes the server's readable explanation. The shared editor resets when its exact owner changes, and saving material never advances a lesson.

The complete aggregate release gate passes: quality checks, all 542 unit tests, TypeScript, production build, six Worker/D1 integrations, responsive/PWA/offline E2E, clean/idempotent migration rehearsal, Hermes contracts, 38 manager tests, 46 installed site-client tests, seven native adapter tests, and secret/diff checks. The material tests cover note/file/card persistence, exact ownership, duplicate submission, failed uploads, validation, draft preservation, offline sync, desktop/tablet/320px and 390px mobile layouts, dark theme, and 200% text. Release preparation also fixes the Preferences grid's enlarged-text overflow. Browser tests use isolated identities for page and service-worker requests, wait for initial service-worker activation/reload, and simulate offline saves with the actual browser connection state.

Worker `2cc5a728-7f2e-4a63-bda1-e407ce2b8414` deployed at `2026-09-05T00:36:11Z` from source `7d4f3eb` on `fix/lesson-materials-panel`, with PWA shell v57. Production health, readiness, budget, public API boundaries, and the exact Leverage points lesson's tabs and add/cancel editors pass live verification. Desktop/mobile renders were inspected, and a fresh browser installed v57 without writing learning data. Migration parity remains exact at 78 applied migrations through `0076`; no migration or dependency installation was needed. The primary checkout remains unchanged. Application rollback is Worker `cd7fa264-dac3-4069-b73f-b878c9f00c8a`. The complete D1 and 1,896-object R2 backup `backup_20260904T233845Z` passed its disposable restore; `docs/release-snapshot.json` records the newer verified recovery receipt and zero production blockers at observation time.

## Current truth — 2026-09-05 (Feeds split triage, local)

Mahmood selected the third live design variation: a compact publication/title navigator beside one article excerpt, with direct original-source access and one-click Skip. Phones put the current article and its controls before the navigator; Open/Skip sit in normal flow below the article identity, without covering the excerpt. Search and subscription management remain secondary disclosures. The reader loads the remaining feed beyond the old 50-entry ceiling and remembers the selected article within the browser session.

Skip immediately removes the exact feed/source pair and advances without reloading the list. `POST /capture/feeds/:id/entries/:recId/dismiss` stores an idempotent dismissal; the saved source, Queue state, and import GUID remain intact. Feed reads and counts exclude dismissals, so refresh and scheduled import cannot restore a skipped entry. A failed request restores the visible entry and reports the failure. Migration `0077_feed_entry_dismissals.sql`, the API/agent contract, and the installed RSS/site-operator guidance describe this separate operation rather than the legacy destructive entry DELETE.

Implementation is isolated on `codex/feeds-design-variations-20260905`. The local PWA shell is v57; migration 0077 and the application deployment remain pending. No production data or deployment was changed. The existing dependency lockfile is retained, with zero vulnerabilities reported by `npm audit` and no package installation.

Verification: 543/543 unit tests, TypeScript, and the production build pass (136.54 KB main JavaScript gzip, 144.23 KB including Preact). `E2E_FOCUS=feeds npm run test:e2e` passes against disposable local Worker/D1 storage, covering more than 50 entries, immediate removal without refetch, persisted counts and reload, idempotence, unchanged source state, failure restoration, remembered selection, search, phone content clearance, and keyboard order. The full E2E run reaches Preferences but stops on its mobile enlarged-text overflow (18px), outside the changed Feeds selectors. Desktop, 632px, and 390px captures were inspected with normal motion; the final reviewer disposition is **ship**, with both reported phone-layout/keyboard fixes scored resolved. Installed Hermes contracts and prompt/memory checks pass.

## Current truth — 2026-09-05 (all branch histories merged)

At Mahmood's request, all ten previously unmerged local branches now have explicit merge commits in the combined `main` history. Existing cherry-picked and adapted implementations remain current; conflicts from older snapshots, competing profile layouts, retired pages/MCP tooling, outdated release receipts, and duplicate tests were resolved against the verified release. The full branch histories remain reachable, and branch names/worktrees were retained.

| Branch                                         | Merge commit |
| ---------------------------------------------- | ------------ |
| `hermes/native-compass-tools`                  | `1483860`    |
| `codex/preferences-redesign-20260830`          | `3eec9ea`    |
| `codex/item-pages-20260905`                    | `534dfd4`    |
| `codex/resurfacing-card-source-route-20260901` | `1365623`    |
| `codex/system-hardening-m0-20260829`           | `df3bc50`    |
| `codex/deploy-everything-20260829`             | `8ed7733`    |
| `codex/learning-compass-threadscoped-no`       | `b6e2d9e`    |
| `codex/profile-redesign-20260830`              | `c3b7831`    |
| `codex/profile-release-20260830`               | `832b2cd`    |
| `codex/release-all-20260905`                   | `6b1465d`    |

All ten tips pass Git ancestry checks, and all 20 local branches are contained in the combined history. Runtime code, assets, schemas, tests, dependencies, and Hermes integration files are identical to the verified release at `725527e`; only merge documentation and the architecture explanation of existing budget headroom changed. Post-merge verification passes quality checks, 542/542 unit tests, TypeScript, and the native Hermes contract/prompt checks. The 19 newly reachable non-merge commits pass the credential-pattern scan, and diff checks pass. Production remains Worker `cd7fa264-dac3-4069-b73f-b878c9f00c8a` with PWA v56, so no additional deployment or data operation is needed.

## Current truth — 2026-09-05 (integrated application release, deployed)

This is the active application state; dated entries below retain historical implementation and rollout details. GitHub `main` now reconciles the primary checkout and outstanding Hardcover, resurfacing, Recall, item-page, Threads, Preferences, profile-presentation, direct Lite Visual, native-Hermes, and production-lineage work. It preserves the modular client architecture and exact applied migrations through `0076`. Older detached frontend prototypes remain recoverable. Every dirty worktree was preserved as a patch and untracked-file archive under `.tmp/release-20260905/worktree-snapshots`; the primary source snapshot is commit `5e997cc`.

Integration fixes include native-only Hermes release checks with real offline tool-schema and memory-loading verification, exact guarded improvement-receipt readback, restored 4,000,000/70,000 daily D1 budgets, source/file navigation across split Thread modules, portable native-tool tests, scoped linting around the independent nested project, and building web assets before database integration tests. The missing deterministic manager fixtures are now repository-owned; their isolated native runner must produce a real report with 38 passing cases, including the production manager context, and cannot pass with zero collected tests. Historical native skill snapshots remain intact.

The complete aggregate release gate passes 542/542 unit tests with zero skips, TypeScript, ESLint/Knip/Prettier, all six Worker/D1 integrations, responsive and Android/offline E2E across five roots and 16 internal states, clean/idempotent migration rehearsal, 38 manager tests, 46 installed site-client tests, seven native adapter tests, Hermes contracts, prompt/memory budgets, and secret/diff checks. Main JavaScript is 137.56 KB gzip (145.25 KB including Preact); heavy graph chunks remain lazy. Item-page and Thread-workspace desktop/mobile renders were inspected. GitHub Verify run `33926123861` passed on deployed source commit `bb760eea41050ef3230eebdf900da2ae12fbb470`. Locked npm dependencies were installed with zero reported vulnerabilities.

Worker version `cd7fa264-dac3-4069-b73f-b878c9f00c8a` is deployed at 100%, with PWA shell `learning-compass-shell-v56`. Rollback is `658f9cbd-e68c-4b40-a4e1-c7075f61a25b`. Post-deploy liveness, readiness, budget policy, canonical smoke, extended feature reads, exact source/file metadata, improvement-receipt filtering, direct pair contract, and all three initial fingerprinted assets pass. Native runtime dispatch also passed Worker health, NotebookLM listing, and a guarded improvement dry-run. The direct companion receiver is live and still declares quality checks not run for integrity-only receipts.

The durable observation is `docs/release-snapshot.json`: 78 migrations at exact parity, all 29 required schema objects, healthy bindings/signing, and zero release blockers. Recovery snapshot `backup_20260904T190454Z` was restore-verified before deployment (109,752,120 D1 bytes and 1,740 R2 objects totaling 278,644,623 bytes). This application release applied no migration and performed no corpus registration, upload, activation, retirement, or target mutation. Corpus counts remain three registrations, 252 targets, one active corpus, and 623 pairs. Source content and lesson progress are preserved; the separately recorded 210 empty Riyadh companion slots remain a content-rebuilding task.

## Current truth — 2026-09-05 (Arab Writer installed and connected)

Mahmood requested Arab Writer for Arabic writing. The documentation-only installation at `~/.hermes/skills/arab-writer` preserves seven original Markdown resources from `turky015-oss/Arab-Writer` commit `664a21784520a6ed115561de4e647a2461e53792`, with package and file hashes in `upstream.json`. A 4,082-byte local entrypoint replaces the 16,531-byte upstream entrypoint for normal use; detailed references load only when needed. Codex links to the same native directory. Lite Visual, Arabic Notes Extractor explanations, project routing, the active skill registry, and durable USER preferences now use it.

The adaptation keeps concrete wording, natural rhythm, and Arabic proofreading while preserving Egyptian teaching, inline technical terms, faithful quotations/handwriting, semantic HTML, uncertainty, and full explanations. Upstream Saudi defaults, forced English-line breaks, numerical scoring, mandatory extra passes, and video generation do not enter the companion workflow.

Verification: Hermes lists `arab-writer` as enabled; all three affected skill entrypoints pass validation; seven upstream file hashes, reference links, the active registry, the shared Codex path, and the 1,371/1,375-character USER memory budget pass. The registry verifier now expects the added skill (28 total), and syntax plus `git diff --check` pass. The broader Hermes verifier proceeds past the active-skill checks but currently stops on unrelated enabled/unowned `impeccable` and `linux-computer-use` installations. This changes instructions and their registry check only, with no application dependency, deployment, or artifact regeneration. The Worker release remains separate.

## Current truth — 2026-09-05 (Lite Visual direct workflow; local tools installed, Worker release pending)

Mahmood explicitly removed all mandatory editorial review passes and exhaustive HTML/PDF quality checks. This supersedes the earlier teaching-gate entry below. The default skill now reads the complete accepted source, writes and revises natural Egyptian-Arabic explanation directly in one canonical HTML article, renders the matching tagged A4 PDF once, and seals the pair's identity and bytes. There are no manual 120-word scopes, meaning-unit/review forms, mandatory design reports, forced source-copy appendix, viewport/contrast/text-enlargement sweeps, or PDF pagination/text-parity audit. The teaching/design examples remain guidance for good writing. The historical exhaustive validator remains available only on explicit request and does not run in the direct path.

`run_workflow.py finish` works locally without network publication and reuses unchanged hash-matched pairs. The new HMAC-attested `lite-visual-integrity/v1` receipt checks extraction/target binding, hashes, canonical body, and render binding, explicitly recording `quality_checks=not_run`. Worker code accepts it beside historical v6, reports quality assurance as unverified, and keeps exact atomic storage, R2 verification, job/run ownership, and source readback. Direct checkpoint evidence truthfully reports zero reviewed scopes/claims under `authoring_mode=direct`. `audit_pair_set.py` produces an ordered `lite-visual-corpus-integrity/v1` binding for direct batches without bringing back quality reviews; all-target staging/activation/retry/rollback remains guarded. No D1 migration is required.

Redundant frame waits and per-page subprocess launches were also removed from the optional old validator. Three identical synthetic 33-page runs improved its combined render/browser/PDF median from 32.882 to 1.963 seconds; the direct default skips both audit stages entirely. This is a technical benchmark, not an estimate for reading/writing 100 sources. Runner timeouts, elapsed-stage reports, immediate busy-lock failure, exact-pair reuse, and bounded independent acquisition/render overlap reduce stalls. Official tool research supports retaining the current Playwright/Chromium and source adapters; no new dependency, MCP, or service was installed.

Verification observed: all 502 unit tests and TypeScript pass; production client build passes at 138.72 KB base JavaScript gzip; focused final receipt/skill regressions, native Hermes YAML, Lite Visual skill validation, Python/Node syntax, and diff checks pass. Tests cover direct finish/reuse, no forced appendix/quality checks, wrong-source/tampered-file rejection, receiver compatibility before job claims, honest Python/Worker receipt semantics, bulk PDF legacy checks, timeouts/lock release, and complete integrity-only plus historical corpus activation/retry/rollback. Skill, router, companion/Riyadh routing, native USER preference, project/API/production/design documentation are synchronized.

Deployment remains pending: the live `/artifacts/pair-contract` returned HTTP 404, so new-schema publication stops before claiming a job and preserves finished local files. The global Hermes release checker currently stops at `Retired Hermes skill remains active: learning-thread-curation`; it also still contains obsolete Compass-profile requirements. Those existing release/configuration issues were not bypassed. No Worker deployment, production source regeneration, corpus registration, upload, activation, or rollback occurred.

## Current truth — 2026-09-05 (dedicated item pages, local)

Saved item titles now lead to their owning pages across Home/resurfacing, Queue, Feeds, Archive, Search, Personal Data Studio, captured Canon books, and lesson material. Sources and books expose Overview, Files, Notes & passages, Recall, Connections, History, and Reflection as bookmarkable sections; books keep Chapters. Source pages show every attached Thread and retained Unit, link notes/cards/files to their own readers, preserve pending recall-draft access, and include the separate personal-ledger details when present. File pages link back to source material or their exact Thread/Level owner. Explicit Original/HTML/PDF/Open controls remain passive content access.

`GET /artifacts/:id/record` loads exact metadata for older, book, Thread, and legacy files without relying on the bounded general Files list or downloading R2 content. Staged, orphaned, and deleted-source files remain hidden. `GET /capture/:id/record` adds the existing `personal_item` projection without changing Queue or learning state. The agent catalog, ownership contract, API/product/design/book documentation, release checklist, project brain, and installed site-operator reference are synchronized; installed guidance conditions the new endpoint on live capability availability.

Item sections retain unsaved mounted forms across navigation and browser Back; reload retains the section rather than an unsaved draft. Saving feedback refreshes the owning record. Generic book-source URLs resolve to the book page. Normal section/annotation query state no longer triggers a false legacy-recovery banner. Library item headers show the item type without unrelated Library mode controls. The redundant inspector-selection effect that could repeatedly rerender detail pages is removed. The local PWA shell advances to v53.

Verification observed: production build and TypeScript pass, with 140.22 KB base JavaScript gzip; targeted API/navigation/ownership checks pass; the complete Worker-backed E2E suite passes across five roots and 16 modes, including exact scoped-file access, source-note-file navigation, unsaved reflection preservation, passive navigation, responsive section controls, and Android/offline behavior. Desktop and 390px mobile renders were inspected after the final routing correction. The full unit run is 498/502 passing: four failures concern the separately installed Lite Visual validator/skill contracts, whose files and tests were not changed by this task. The broader Hermes contract checker is blocked by the installed operating-system router skill exceeding its prompt budget at 8,395 bytes. No deployment, migration, production D1/R2 write, or corpus operation occurred. Implementation is isolated on `codex/item-pages-20260905`; the primary checkout's existing work is preserved.

## Current truth — 2026-09-05 (Lite Visual teaching and batch quality, installed locally)

Native Hermes Lite Visual now teaches in natural written Egyptian Arabic with precise technical terms and quotations preserved in their original register. Its linked teaching guide gives concrete before/after writing examples, source-specific explanation methods, and complete fidelity, teaching, language, and continuity revision passes. The batch guide requires the same full reading and editorial effort for every source, bounded context with exact resumable evidence, and truthful authored/validated/staged/activated counts. The active router, companion resolver, Riyadh workflow, project contract, design/API/operations documentation, and native USER preference are synchronized; the retired Compass profile has not been recreated.

Each claim now includes anchored meaning units, and each newly validated ledger includes `lite-visual-editorial-review/v1` evidence tied to the final parsed authored-text digest. The standard publication runner checks that evidence before claiming a job, rendering, or uploading, including resumed unpublished workspaces. The browser gate checks authored explanation as well as the source edition, catches hidden/low-contrast/clipped/print-dropped content, and actually doubles each computed text size. Source embedding preserves punctuation, notation, diacritics, and paragraphs instead of reconstructing text from normalized tokens; it no longer inserts a visible heading for every 120-word scope. A shared Playwright renderer waits for fonts, blocks external resources, and produces tagged A4 PDFs with heading bookmarks from the same HTML. These are authoring and evidence improvements, not a machine claim of semantic or literary perfection. Published pairs and historical signed receipts remain unchanged.

Verification observed: all 493 unit tests and TypeScript passed; production build passed with 138.72 KB base JavaScript gzip; the Lite Visual skill validator, updated Hermes YAML, Python compilation, and diff checks passed. Focused regressions cover authored visibility, real text enlargement, stale editorial evidence, meaning-unit anchors, pre-publication review enforcement, punctuation preservation, byte-identical repeated embedding, and tagged/bookmarked PDF generation with safe failure. The full local embedding/rendering/signed-validation path passes. The global Hermes contract checker still fails at its obsolete requirement for the intentionally retired Compass profile; router and ordinary loaded-skill byte limits pass, and USER remains within its existing 1,375-character limit. No Worker deployment, source regeneration, production write, or corpus operation occurred.

## Current truth — 2026-09-05 (Threads learning workspace, local)

Threads now opens as a study desk with direct next-lesson actions, counted Planning/active/paused/completed views, priority and pause controls, remembered search/sort, and separate current/future material gaps. New Threads remain drafts and open first-Level authoring; the selected Level reaches the add-lesson form, which accepts optional study text and duration. Thread Now includes an editable outcome, scope-owned learning notebook, and optional saved synthesis; completed Threads also expose sources to revisit. Lesson routes have a searchable curriculum navigator, persistent focus mode, readable authored paragraphs with independent Arabic direction, and a Library material link that preselects the exact lesson. Existing passive source access, direct completion, and automatic advancement remain intact.

The hub adds next-lesson/readiness and duration-coverage fields, excludes completed lessons from gaps, and derives nullable study dates from explicit lesson-status events committed with the lesson update. Browser hub reads revalidate so status and priority changes are visible immediately. No migration is required. The implementation is isolated on `codex/threads-learning-workspace-20260905` from integration base `4234f7a`; the primary checkout is unchanged. The installed authoring and site-operator references describe optional live support for the new fields.

Verification passes quality (ESLint, Knip, Prettier), TypeScript, all 36 focused Thread checks, and the full Worker-backed browser suite across five roots and 16 modes. The added end-to-end flow covers planning, Level/lesson authoring, priority persistence, direct lesson entry and completion, exact-owner Library navigation, focus persistence, goal/reflection edits, restored search, desktop and 390px layouts, dark theme, and 200% text. The base build remains below the 150 KB gzip budget, and the local PWA shell is v54. Screenshots are retained under `test-results/thread-desk/`.

The complete unit suite reports 486/493 passing; seven unmodified Lite Visual tests fail against the newer installed authoring scripts and retired Compass-profile expectations. The separate Hermes contract checker stops at the pre-existing enabled `impeccable` and `linux-computer-use` ownership mismatch. These unrelated release gates remain unresolved; no deployment or production data write has occurred. Dependency audit reports zero vulnerabilities and the existing lockfile is unchanged.

## Current truth — 2026-09-05 Cairo (unrebuilt Riyadh slots cleared)

Mahmood explicitly requested removal of the remaining 210 old companions for a fresh rebuild. Migration `0076_retire_unrebuilt_riyadh_companions.sql` retired the exact 420 live HTML/PDF artifacts and their non-corpus pair states. All 294 canonical source records were read back: the first 84 retain their exact redesigned artifact IDs, and each of the remaining 210 has no HTML or PDF companion. Original sources, all 294 lessons, and progress remain intact: 14 completed lessons, three completed Levels, current `باب 3 — الصبر`. Production readiness is healthy and the migration ledger has no pending entry. No Worker deployment was needed.

The cleanup is recoverable: artifact rows and R2 bytes remain, marked `retirement_operation=riyadh-unrebuilt-210-20260904`. All 420 targeted objects were checksum-verified in restore-tested full snapshot `backup_20260904T190454Z`; the pre-cleanup Time Travel bookmark is `0000098c-00000000-000050dc-5a41507554d6ee8a4100b1a840ccb67d`. Verification passed a focused retirement regression, full live-inventory SQLite replay (420 retired; all 168 rebuilt artifacts byte-identical), repeat-run idempotency, clean/idempotent rehearsal of all 78 migrations, formatting, and diff checks. The application gate and the separate ongoing Hermes profile transition remain as recorded below; this data-only cleanup does not overwrite that session's files. Future work must rebuild the 210 empty companion slots and must not restore their retired low-quality pairs as current.

## Current truth — 2026-09-04 (84 Riyadh replacements live)

The explicitly requested early release is active: recordings 1–84 now expose 84 redesigned HTML/PDF pairs. Corpus `lvc-6b0a55ca8ce85ff6c94264cf` activated at `2026-09-04 20:53:24` UTC. Every included source was read back with its exact new artifact IDs, and all 84 bound jobs are completed. Activation superseded 168 former artifacts; migration `0075` separately retired 12 duplicate legacy artifacts across six sources. All old files remain recoverable in R2/history. The remaining 210 sources retain their prior ready pairs; the full 294-recording redesign is not complete. Lesson progress is unchanged: 14 completed lessons, three completed Levels, and current Level `باب 3 — الصبر` in progress.

Worker `658f9cbd-e68c-4b40-a4e1-c7075f61a25b` is live; application rollback is `4f641e6d-ad43-4240-8942-74dd42f4faef`. Migrations through `0075_riyadh_legacy_pair_visibility.sql` are applied with no pending migration. Production readiness is healthy with 29 required schema objects, all bindings, signing, and zero blockers. The complete application release gate passed 492 unit tests, type/style checks, all integrations, build, responsive/PWA E2E, migration rehearsal, Hermes contracts and installed client checks before deployment. The subsequent exact legacy-data repair passed a focused behavioral regression and clean/idempotent rehearsal of 77 migrations. The publisher's missing-pair 404 handling and immutable validated-HTML reuse were repaired; all 24 local orchestration tests pass. A concurrent, independently authorized Hermes maintenance session removed the Compass profile afterward, so this branch's old profile-dependent aggregate gate cannot be rerun until that maintenance change is integrated; its files were not restored or overwritten to satisfy this branch.

The final signed audit passes 84/84, zero failures, with corpus SHA-256 `e1d445c22b5223aa64bf2b1e52b19e39c468c05e61d522ad15c0ba0ac8cd2315`. Publication evidence is `/home/mahmud/lite-visual-work-v2/prefix-084-publication-summary.json`; the final audit is `prefix-084-final-audit.json` in the same directory. Two earlier zero-upload corpora remain aborted as recovery history. Recovery snapshot `backup_20260904T190454Z` is checksummed and restore-verified; pre-0074 Time Travel bookmark is `00000987-00000000-000050dc-8631ee540add1793cd5c3f08347871d8`, and pre-0075 bookmark is `00000988-00000000-000050dc-c45a3d63626cc8d2c9e979f702f49bbd`.

## Current truth — 2026-09-04 (Riyadh 84-recording early-release candidate)

Mahmood explicitly requested immediate publication of the 84 rebuilt Riyadh companions instead of waiting for all 294. The candidate is an exact ordered prefix of the authoritative 294-recording manifest (`1de52aac5305c071c027650b8494971d7ccbd83a1bfe497b8fbcb067eeae6be4`) and remains bound to the full manifest hash. All 84 included HTML/PDF pairs pass signed `lite-visual-validation/v6`; the aggregate audit passes 84/84 with zero failures and zero anti-template similarity flags after replacing the shared visual shell with source-signature- and semantic-group-scoped layouts. This prefix is an early patch only: the other 210 lessons retain their current companions and the full goal remains 294/294.

A fresh full production recovery snapshot `backup_20260904T190454Z` restored 109,752,120 D1 bytes and checksum-verified all 1,740 R2 objects totaling 278,644,623 bytes. SQLite integrity is `ok` with zero foreign-key violations. At this checkpoint no replacement job, corpus registration, staging upload, activation, or lesson-progression mutation has occurred.

## Current truth — 2026-09-04 (native Hermes profile consolidation, local)

At Mahmood's request, the stopped `compass` profile and `/home/mahmud/.local/bin/compass` alias were removed through the supported Hermes profile commands. Native Hermes's `default` profile remains selected in both the CLI and desktop settings, with all 27 owned Learning Compass skills enabled alongside the existing native capability. The gateway, mobile dashboard, and web UI continue running against `/home/mahmud/.hermes`; no service restart, model change, or toolset change was needed.

A full pre-removal copy, including profile configuration and session data, is retained in the mode-0700 directory `/home/mahmud/.hermes/profile-archive/compass-removed-20260904.ndAn3Z`. A checksum-based comparison found no differences before deletion. Default Hermes's existing memories and configuration were preserved. Its configuration skill now directs Learning Compass work to native Hermes and permits natural result summaries.

The task worktree removes the retired prompt budget and mirror-only test, keeps native skill, security, memory, and workflow checks, and rejects instructions that select or recreate the retired profile. Verification passes 50 focused instruction/agent/Lite Visual tests, 75 migration checks, 30 synchronized checks, 145 owned routes, the native prompt budget (39,796 fixed bytes, 13 tools), the configuration-skill validator, Hermes configuration checks, repository quality, TypeScript, and whitespace checks. No dependencies were installed or Worker deployment performed. Repository changes remain in the dedicated worktree; the primary checkout is unchanged.

## Current truth — 2026-09-04 (Hermes skill loading and instruction checks, local)

The installed default and Compass skills now share shortened operations, recommendation, and Lite Visual entry points with conditional reference routing. Their combined entry text fell from 87,269 to 14,683 bytes (83.2%); matched Telegram fixed payloads stayed at 39,796/39,946 bytes. Detailed source coverage, validation, publication, lease, and verification requirements remain in the owning references. Lite Visual documentation now agrees with the installed validator's 120-word scope limit and complete-source edition; validator and publication scripts were not changed.

The dedicated Learning Compass worktree adds a contract-integrated instruction audit with regression tests for missing npm commands, concrete local links/scripts, conflicting frozen migration claims, and forced reply templates. Stale release commands and broken job/NotebookLM references were repaired in the installed skills. Replies may explain verified outcomes naturally; exact identifiers, state, and verification remain in the existing operation receipts without new read-only audit writes. The separate Hermes evaluator worktree accepts bounded, value-bound paraphrases and evidence-only IDs without relaxing API, mutation, readback, or invented-identifier checks.

Verification: repository quality (lint, dead-code, formatting), TypeScript, 50 focused instruction/agent/Lite Visual tests, the contract verifier, both prompt budgets, three skill-entry validators, and the 43-test deterministic manager harness pass. Hermes's hub audit reports no hub-installed skills for these local skills, so it is not counted as skill validation. Backups are retained at `/home/mahmud/.hermes/skill-archive/skills-maintenance-20260904.3huT4z`. Repository changes live in dedicated worktrees; both primary checkouts remain unchanged. No new dependencies, real-model sampling, production data mutation, or deployment was performed. Byte savings do not establish runtime latency or quality improvements.

## Current truth — 2026-09-02 (automatic Level and lesson continuation, deployed)

Direct lesson completion now owns the complete continuation transition. Completing an available lesson activates its Level without a separate Start Level action; every completion starts the next unfinished lesson, and a Level's final completion activates the next Level plus its first lesson. Home and typed Lesson views expose the same Complete action for ready lessons and reload from canonical state, so the dashboard advances automatically. The compatibility Level-start route remains available for old clients, but the current interface no longer requires or presents it. The PWA shell cache advances to `learning-compass-shell-v53`.

PR #6 merged the change into `main` at `2acbea0`; both GitHub Verify runs and GitGuardian passed. Worker version `fd9edfee-dca7-4fea-bd29-a6d629942286` is deployed at 100%, with `7d6401be-004d-4eb5-8b04-089ba6a4c720` retained as the immediate rollback version. Live shell v53 and fingerprinted client assets return HTTP 200 and contain the new Level/Lesson actions; `/dashboard/briefing` and the Abrahamic Thread path return HTTP 200. Because Level 0 had already completed under the prior Worker, one canonical lesson mutation activated Level 1 and its first lesson after deployment, and readback confirmed both are `in_progress`. Overall `/health` remains `needs_attention` for four pre-existing overdue retries and a recovery snapshot just beyond its freshness window; D1, R2, assets, schema, bindings, signing, and integrity are healthy.

## Current truth — 2026-09-01 (read-only Hardcover history in Books, deployed)

Library → Books now composes the existing D1 Hardcover mirror into My Books without calling the import route or creating personal-ledger records. An explicit `recommendation_id` or one unambiguous normalized title/primary-author match displays once with Hardcover's reading status; unmatched mirror titles remain visibly sourced from Hardcover, open their external book page, and expose no internal branch, dossier, chapter, pin, or reading-state action. The canonical editable `books[]` contract, verified-branch intake, Queue exclusion, and explicit branch-gated `/hardcover/import` path remain unchanged. The installed PWA shell advances to `learning-compass-shell-v54` so cached clients cannot retain the previous Books projection.

The guarded application release passed quality, 491/491 unit tests, TypeScript, all six isolated Worker/D1 integration programs, the production build at 133.26 KB gzip for base JavaScript and 102.34 KB gzip for CSS, full Worker/browser E2E, clean and idempotent rehearsal across 75 migrations, both Hermes prompt contracts with 30 synchronized checks and 145 owned routes, 32/32 agent-control tests, 37/37 deterministic manager-routing tests, both 46/46 installed site-client suites, and the final tracked-diff check.

Fresh recovery snapshot `backup_20260901T054625Z` contains a 107,258,536-byte D1 export with SHA-256 `805074d0d959926123497366f1c125484b7801bd6cadd853ce21fd5226d90734` and 1,448 checksum-verified R2 objects totaling 155,208,175 bytes. Its rehearsal restored 677 recommendations, 80 notes, 1,448 artifacts, four Threads, and 34 recall cards with SQLite integrity `ok` and zero foreign-key violations.

Worker version `0fec3f5b-9d48-4c90-a6c2-0b6a663cadad` is deployed at 100%; application rollback is `767bc906-32df-4450-b6d3-9980e71e4e84`. Pre- and post-deploy readiness passed with zero blockers, and the canonical live smoke suite passed. Live `GET /recommendations/books` returns 36 canonical records plus the separate 45-book Hardcover mirror; client composition reconciles seven identities into 74 visible books with 32 saved, six reading, and 36 finished. The deployed service worker exposes shell v54. No migration, Hardcover import, corpus operation, product-data write, or R2 content write occurred; only the required verified recovery receipt was recorded in D1 before deployment.

## Current truth — 2026-09-01 (resurfacing source dossier link, deployed)

The Daily resurfacing card on Home opens the unified source dossier for its exact recommendation id from the title or card surface. Branch, Original/HTML/PDF, Star, Reviewed, Snooze, and Dismiss remain independent controls with their existing behavior. The destination retains source access, branch context, recall, feedback and outcome, files, and notes. The installed PWA shell advances to `learning-compass-shell-v53` so an installed client cannot retain the superseded Home interaction.

The guarded application release passed quality, 487/487 unit tests, TypeScript, all six standalone Worker/D1 integration programs, and the production build at 132.27 KB gzip for base JavaScript and 102.24 KB gzip for CSS. Worker-backed browser E2E passed five root destinations, 16 internal modes, typed-object navigation, Android/PWA offline behavior, and the mobile shell, including a full-card click that preserved the exact resurfacing recommendation id and opened the source access, Branch, recall cards, feedback and outcome, and files sections. Clean and idempotent migration rehearsal passed across 75 migrations. Hermes passed 30 synchronized checks and 145 owned routes, both Telegram prompt contracts, 32/32 agent-control tests, 37/37 deterministic manager-routing tests, both 46/46 installed site-client suites, installed Python/Node syntax checks, and the final tracked-diff check.

Fresh recovery snapshot `backup_20260901T050418Z` contains a 107,258,082-byte D1 export with SHA-256 `280a0443585608860b6d5e4ae9d96a0caad952ea50d358779ca0fda60c0728e8` and 1,448 checksum-verified R2 objects totaling 155,208,175 bytes. Its rehearsal restored 677 recommendations, 80 notes, 1,448 artifacts, four Threads, and 34 recall cards with SQLite integrity `ok` and zero foreign-key violations.

Worker version `767bc906-32df-4450-b6d3-9980e71e4e84` is deployed at 100%; application rollback is `5e68a5e4-35ff-48db-9d08-a1ac1d595a02`. Pre- and post-deploy readiness passed. Final live readback returns HTTP 200 for liveness, readiness, and free-tier budget; D1, R2, and assets are healthy, integrity reports zero active orphans and zero unresolved quarantines, release bindings and signing are present, maintenance and jobs are healthy, recovery points to the fresh verified snapshot, and readiness has no blockers. The canonical live smoke suite passed every route and confirmed root `no-store` plus no retired authentication surface. Live assets expose shell v53, `.folio-resurfacing-record-link`, and the source-record navigation label. A read-only production browser check clicked the actual Home card surface, preserved its exact source id in the route, and rendered Source access, Branch, Recall cards, Feedback & outcome, and Files. No migration, product-data write, R2 content write, corpus lifecycle action, or target-set mutation occurred; only the required operational recovery-backup receipt was recorded in D1 before deployment.

## Current truth — 2026-09-02 (focused Recall review stage, deployed)

Learn → Practice → Recall now presents the active due card as one intentional retrieval stage instead of a large generic panel. A compact due-position band, quieter source/branch and schedule ledger, centered Arabic reading treatment, explicit question/answer hierarchy, retrieval pause cue, and balanced grading controls keep the learner focused without changing FSRS behavior or review mutations. The phone layout uses deliberate two-by-two view and grading grids and retains the five-root dock without horizontal overflow.

The guarded release gate passed quality, all 488 unit tests plus TypeScript, all six isolated Worker/D1 integration programs, and the production build at 132.40 KB gzip for base JavaScript and 102.62 KB gzip for CSS. Worker-backed E2E passed five roots, 16 internal modes, typed objects, Android/PWA offline behavior, and the mobile shell. Migration rehearsal passed clean apply plus idempotent reapply across 75 migrations; Hermes passed 30 synchronized checks and 145 owned routes, both Telegram prompt contracts, 32/32 agent-control tests, 37/37 deterministic manager-routing tests, both 46/46 installed site-client suites, installed Python/Node syntax checks, and the final tracked-diff check.

Worker version `7d6401be-004d-4eb5-8b04-089ba6a4c720` is deployed at 100%; application rollback is `0fec3f5b-9d48-4c90-a6c2-0b6a663cadad`. Pre- and post-deploy readiness passed, and canonical live smoke returned the expected success and boundary responses across health, briefing, Capture, Notes, Recall, agent jobs, retired auth, and fake-route checks. Final readiness reports D1, R2, and assets healthy, zero active integrity orphans, zero unresolved quarantines, healthy maintenance and jobs, a verified and restore-rehearsed recovery snapshot inside its freshness window, and no blockers. Live browser inspection passed the question and revealed-answer states at desktop and phone widths, including the two-by-two mobile view and grading grids, with zero document overflow. This visual-only application release applied no production migration, review grade, production data write, or corpus operation.

## Current truth — 2026-09-01 (integrated learning workflows and production hardening, deployed)

The deployed release consolidates the complete 2026-08-31 learning-workflow work into the repository and production branch: durable share-intake review, revisioned source anchors, non-destructive recall repair, verified offline packs, advisory source health and guarded replacement, Library-first Thread material organization, exact-Lesson material requests, and continuous next-Lesson navigation. The existing migrations through `0073_source_annotation_revisions.sql` remain the production schema truth; this application release did not reapply migrations or mutate the corpus.

The client architecture is now maintainable without changing those contracts. The Learning Thread route is split into route, Level, project, Lesson, material, source-organizer, offline-pack, and view-model modules. The former global stylesheet is an ordered 24-module product-area cascade whose concatenated source was verified byte-for-byte before the import entry replaced the monolith. ESLint, Prettier, EditorConfig, and Knip now form one `npm run quality` gate; standalone integration scenarios are part of `npm run verify:release`. The browser extension has a least-privilege options flow and production-safe default origin. Dependencies are current through Vite 8.2.2 and Wrangler 4.127.1, obsolete client/config entries and shipped prototypes are removed, and repository, dependency, CSS-placement, and release documentation describe the same boundaries. [GitHub PR #2](https://github.com/mhmudxnasr/learning-compass/pull/2) merged the release into `main` at `b39644a` after both Verify jobs and GitGuardian passed. [PR #3](https://github.com/mhmudxnasr/learning-compass/pull/3) recorded the release and hardened Preferences against narrow-screen enlarged-text overflow; both replacement Verify jobs and GitGuardian passed before it merged at `13181d2`.

The guarded deployment gate passed quality, 487/487 unit tests, TypeScript, all six isolated Worker/D1 integration programs (Branch Deck, Compass feedback, Discovery, feedback proposals, Hermes upgrade, and Taste Rec), and the production build at 132.25 KB gzip for base JavaScript and 102.14 KB gzip for CSS. Responsive E2E passed five roots, 16 internal modes, typed objects, Android/PWA offline behavior, the mobile shell, and the corrected 390×844 Preferences layout at doubled text scale. Migration rehearsal passed clean apply plus idempotent reapply across 75 migrations. Hermes passed 30 synchronized checks and 145 owned routes, both Telegram prompt contracts, 32/32 agent-control tests, 37/37 deterministic manager-routing tests, both 46/46 installed site-client suites, installed Python/Node syntax checks, and the final tracked-diff check.

Fresh full-recovery snapshot `backup_20260901T025854Z` contains a 107,256,401-byte D1 export with SHA-256 `654db319cd671850f3ece777468e8a437fb9990ff70ef03a7f047e924d34bf62` and 1,448 checksum-verified R2 objects totaling 155,208,175 bytes. Its rehearsal restored 677 recommendations, 80 notes, 1,448 artifacts, four Threads, and 34 recall cards with SQLite integrity `ok` and zero foreign-key violations. D1 Time Travel bookmark `00000900-00000022-000050d9-0f0b4d7d87d7bd651e511091bfb976db` is the pre-release database recovery point.

Worker version `5e68a5e4-35ff-48db-9d08-a1ac1d595a02` is deployed at 100%; application rollback is `6986f1f0-46d4-4622-97a1-1c66fbb7beee`. Pre- and post-deploy readiness passed. Final live readback returns HTTP 200 for liveness, readiness, and free-tier budget; D1, R2, and assets are healthy, integrity reports zero active orphans and zero unresolved quarantines, release bindings and signing are present, maintenance and jobs are healthy, and readiness has no blockers. The canonical live smoke suite passed every route and confirmed shell v52, root `no-store`, and no retired authentication surface. The live fingerprinted CSS asset contains the responsive Preferences correction. No corpus registration, staging, upload, activation, rollback, or target-set mutation occurred.

## Current truth — 2026-08-31 (evidence, repair, offline, source health, and Thread materials, deployed)

Five linked learning-workflow upgrades are deployed. Source anchors now resolve current and replacement-lineage URLs to existing branch-owned sources, preserve quote/context/typed locator/language/checksum and optional Thread ownership, support immutable revision/archive/search/evidence retrieval, and expose separate explicit actions for a note, anchored Learning Unit, or learner-authored Arabic recall card; saving an anchor creates none of those automatically. A checksum-changing edit creates a new active revision and archives the previous evidence row, while stale concurrent edits fail without changing Search. Recall → Needs repair now surfaces cards after three unacknowledged lapses plus learner-paused cards with source provenance, review/repair history, and nearby comparisons. Wording revisions preserve FSRS state, semantic revisions reset it, and pause/retire/restore/confirmed schedule reset preserve prior review events and never change lesson progression. Manual split creates one new learner-authored card at a time while leaving the original unchanged until a separate pause or retire action.

The production service worker is shell v52 with explicit versioned offline packs for Queue/source, book chapter, whole book, current Thread, and current Level. Packs admit only same-origin metadata and complete `ready`/validation-passed HTML+PDF artifacts sharing one pair ID; Original and NotebookLM remain online-only. New versions download and validate completely before the manifest switches, and the interface reports measured bytes, partial/evicted, superseded, quota/error, refresh, and remove states. Source health is now a persisted advisory ledger with `verified|restricted|unavailable|unknown|invalid`, false-positive-aware copy, bounded history, and a maintenance scope limited to stale Queue, active-lesson, and Current-Book URLs. Current Book exposes the complete guarded replacement/history control, and Queue has an in-place health-status filter. Replacing Original is verify-first and then explicit, with a stale-current-URL precondition, dedup guards, and old/new URL lineage preserved; personal metadata, ISBN book upsert, recommendation push, and delta sync cannot bypass that path. A failed or ambiguous check never rewrites the source or appears as the verdict for a newer URL.

Thread Resources now searches the existing verified branch/domain Library before research and exposes exact Level/Lesson attachment, role, expected contribution, position, edit/reorder, and removal. **Find material** is an explicit action only for an incomplete current-Level lesson without authored content or attached material. Its idempotent `compass_lesson_material` job is bound to that exact Thread/Level/Lesson and may complete only with one matching ready Compass pick or a reasoned abstention; completion rejects attach, Queue, start, progress, and unrelated output. Material attachment resolves current canonical ownership at write time and never rewrites branch ownership from a stale pre-read. Agent control is `2026-08-31`, and migrations `0069_recall_repair.sql`, `0070_source_health.sql`, `0071_thread_material_organizer.sql`, `0072_share_intakes.sql`, and `0073_source_annotation_revisions.sql` are applied in production with exact repository parity.

The guarded release passes all 486 unit tests plus TypeScript, the 138.72 KB base JavaScript gzip budget, the complete Worker-backed E2E suite across five roots and 16 internal modes including Android/offline behavior, clean and idempotent migration rehearsal, 32/32 agent-control tests, 37/37 deterministic manager-routing tests, both 46/46 installed site-client suites, Hermes contracts and both Telegram prompt budgets, secret scanning, and final diff checks. Pre-migration recovery snapshot `backup_20260831T062702Z` restored 107,168,526 D1 bytes and 1,448 checksummed R2 objects totaling 155,208,175 bytes with SQLite integrity `ok` and zero foreign-key violations; D1 Time Travel bookmark `000008f4-00000053-000050d8-73a1e9e3e09a2dccab4d5d6c9b05771b` is the recovery point. Worker version `c7ff444f-fdfa-464c-ad05-a3c9be966575` is deployed at 100%; rollback is `24ecdbe8-c519-46d0-8489-b937c834149a`. Post-deploy readiness reports every required table, index, column, binding, and signing boundary healthy; canonical and extended live smoke pass across 25 feature/system endpoints with service-worker shell v52 and no retired auth surface. No corpus registration, staging, upload, activation, rollback, or target-set mutation occurred.

Android/web shares now enter migration `0072_share_intakes.sql`: the Worker durably stores the form body before redirecting with only an opaque receipt ID. A URL plus distinct prose is an unresolved `review` receipt, never an inferred anchor; the learner explicitly chooses whole-source Capture or exact selected passage, the same resolution is idempotent, a stale conflicting choice is rejected, and the chosen unfinished flow recovers after restart. Exact passage text over 10,000 characters is rejected instead of truncated. Final consumption atomically validates and closes against the exact non-deleted canonical source, annotation/source branch equality, and current non-pruned branch/domain ownership. Normalized selector identity and canonical/replacement dedup lineage allow harmless tracking/fragment variants while blocking an unrelated same-quote source. Annotation creation/revision revalidates ownership in the write, stores no oversized selector, and Search omits deleted/remapped/pruned active anchors. Closing the app leaves the intake recoverable, while the pending row never masquerades as a branchless captured source. Migrations `0072` and `0073` are deployed and included in live readiness.

## Current truth — 2026-08-29 (premium editorial Continuum and generator brief, local)

Continuum now defaults to a warm editorial light system derived from the supplied reference: `#fcfaf6` shell, `#fefdfb` surface, `#e55a42` coral action, crisp black ink, a dark persistent rail, soft hairline seams, and restrained paper elevation. The direction applies across Home, Library, Learn, Map, Settings, dialogs, inspectors, forms, workspace modes, and the mobile dock. Decisive actions use a solid coral pill treatment; `Surprise me` renders at 145×42px on desktop with bold black type and expands safely on phone layouts. The other seven premium-reference presets remain separate visual systems, while Continuum's reference changes from Linear to Attio.

The AI theme endpoint now chooses among premium contemporary product/editorial directions instead of extreme visual-lottery prompts, while preserving strict paired-palette validation and WCAG AA text checks. The copied full-system prompt now explicitly asks for a 2026 whole-product redesign covering shell, navigation, page horizons, work surfaces, controls, dialogs, inspectors, Map, and mobile; it rejects generic dashboard grids, glass-heavy styling, decorative gradients, low-contrast metadata, and palette-only output. Its importer schema now lists every supported font system.

Verification passes all 387 unit tests, TypeScript checking, the production build at 118.98 KB base JavaScript gzip, the complete responsive/offline E2E suite across five roots and 16 internal modes, and `git diff --check`. In-app browser inspection at 1440×900 and 390×844 confirms the exact coral/paper tokens, a 145×42px desktop Surprise action, 22px Home panels, eight distinct preset brands with separate typography/geometry, zero horizontal overflow, and zero browser errors. The installed-client shell advances locally to `learning-compass-shell-v51`. No deployment, migration, remote setting write, production D1/R2 write, or corpus lifecycle/target-set mutation occurred.

## Current truth — 2026-08-29 (distinct premium-reference presets, local)

Settings → Preferences now ships eight deliberately separate workspace systems: Continuum from Linear, Raycast Command, Superhuman Focus, Reader Study from Readwise Reader, Notion Minimal, Craft Journal, Arc Space, and Are.na Index. Every direction has a unique full semantic palette, font system, base size, reading rhythm, density, text-size preference, and corner geometry. The palette-only gallery uses matching Linear Graphite, Raycast Aubergine, Superhuman Navy, Reader Sepia, Notion Paper, Craft Cream, Arc Lavender, and Are.na Index themes. The default Continuum shell remains the product identity while adopting the revised Linear-inspired graphite values.

All eight presets passed the rendered semantic contrast audit and were applied through the local Worker with distinct root theme/font/display state and zero desktop overflow. Reader Study and Superhuman Focus were inspected across populated Home as the two visual extremes. The preset gallery was inspected at 1440×900 and 390×844; mobile exposes all eight semantic previews in one column with zero horizontal overflow. Verification passes all 385 unit tests plus TypeScript, the production build at 118.43 KB base JavaScript gzip, the complete responsive/offline E2E suite across five roots and 16 internal modes, and `git diff --check`. The installed-client shell advances locally to `learning-compass-shell-v50`. No deployment, migration, remote setting write, D1/R2 production write, or corpus lifecycle/target-set mutation occurred.

## Current truth — 2026-08-29 (Home lesson completion, deployed)

Each directly completable Home Thread turn now includes a compact Finish lesson action. The control preserves passive material access and typed lesson navigation, exposes saving/success/error states, uses transform/opacity completion motion with global reduced-motion compliance, and refreshes the same Thread row to its next ordered lesson without leaving Home. Lessons in an unstarted Level or without study material remain non-completable from Home.

Verification passes 384 of 385 unit tests with the existing Chrome-unavailable test skipped, TypeScript checking, the production build at 118.23 KB base JavaScript gzip, the complete responsive/offline E2E journey including Home completion and next-lesson replacement, Hermes/profile contracts, clean and idempotent migration rehearsal, the 37-test deterministic manager harness, installed-client parity, and final secret/diff checks. The application-only release deployed Worker version `2fff478d-e866-476c-a7bc-ca6142ef1c0f`; rollback is `065dc99c-a54c-43d9-a239-6c08395b78cc`. Pre- and post-deploy readiness and canonical live smoke passed, and the deployed PWA shell is v49. No migration, D1/R2 data write, or corpus lifecycle/target-set mutation occurred.

## Current truth — 2026-08-29 (continuous Thread lessons, deployed)

Completing a lesson from its typed Thread route now opens the next ordered lesson automatically. Reopening a completed lesson remains in place, and completing the final lesson has no synthetic destination. The existing explicit Level-start boundary and direct-completion-only progression rules remain unchanged.

The lesson-to-next-lesson transition is included in the same guarded application-only release at Worker version `2fff478d-e866-476c-a7bc-ca6142ef1c0f`; the complete live release verification is recorded above.

## Current truth — 2026-08-29 (art-directed workspace presets, deployed)

Settings → Preferences now offers eight complete, reference-grounded workspace systems: Continuum, Carbon Precision, Obsidian Study, Midnight Library, Paper Canvas, Art Deco Notes, Mineral Atlas, and Archive Index. Every preset owns its full semantic palette—including due, danger, and map signals—plus real UI/display/reading/mono font roles, reading measure, density, text scale, and corner system. The Google Fonts contract now loads every selectable family used by those systems instead of silently falling back. Preset cards render a miniature semantic workspace with their actual planes, typography, density, and geometry rather than four anonymous color swatches; the underlying theme-only and custom-system controls remain intact.

All eight preset applications were exercised in the local Worker and produced distinct persisted theme/font/density/radius state with zero desktop overflow. Carbon Precision and Art Deco Notes were inspected across Home; the preset gallery was inspected at desktop and phone sizes with zero overflow, no sub-44px main controls, and a clean selected state. Verification passes 380/381 unit tests with the existing Chrome-unavailable skip, TypeScript, production build at 117.70 KB base JavaScript gzip, the complete responsive/offline E2E suite across five roots and 16 internal modes, the full guarded release gate, and `git diff --check`.

The application-only release deployed Worker version `065dc99c-a54c-43d9-a239-6c08395b78cc`; rollback is `cd070789-9032-456a-94e6-1bd7b5aff088`. Pre- and post-deploy readiness passed with zero blockers and verified recovery snapshot `backup_20260828T131356Z`. Canonical production smoke passed, and the live audit confirmed the fingerprinted preset JavaScript and CSS bundles plus `learning-compass-shell-v48`. No migration, D1/R2 data write, or corpus lifecycle/target-set mutation occurred.

## Current truth — 2026-08-29 (Continuum whole-site unification, deployed)

Continuum now governs the entire client rather than only Home and the outer shell. Library, Learn, Map, Settings, typed objects, inspectors, Capture, Search, and supporting dialogs share one page horizon, restrained Iris actions, quiet control surfaces, continuous ledgers, and consistent elevation. Books keeps one focused reading desk while My books and Canon read as lower continuous workspaces. Queue opens as the ranked Ledger by default and preserves Gallery as a saved alternate. Threads, Notes, Recall, Branch Review, Profile, Preferences, Data & recovery, and System retain their existing workflows and data contracts while shedding their older competing card, button, and panel treatments.

Desktop inspection covers all five roots plus Books, Queue, Threads, Notes, Branch Review, Profile, Preferences, Data & recovery, and System in the default sans system with zero horizontal overflow. Phone inspection covers Queue, Notes, and Preferences with the five-root dock, equal-width mode/focus controls, one-column filters, and 44px targets. The complete guarded release passes 379/380 unit tests with the existing Chrome-unavailable skip, TypeScript, the production build at 117.07 KB gzip for base JavaScript, responsive/offline E2E across five roots and 16 internal mode states, clean and idempotent migration rehearsal, Hermes/profile contracts, the 37-test manager harness, installed-client parity, and final diff checks.

The application-only release deployed Worker version `cd070789-9032-456a-94e6-1bd7b5aff088`; rollback is `8cc708d7-36df-436b-b044-1aa5c2337d0c`. Pre- and post-deploy readiness passed with the verified `backup_20260828T131356Z` recovery snapshot inside its freshness window. Canonical production smoke passed, and a broader read-only audit verified the fingerprinted Continuum stylesheet plus HTTP 200 across liveness, readiness, budget, dashboard, capture/feed, Notes, Recall, learning-core, branch intelligence, Compass, search, agent memory/activity/jobs, Hermes analytics, public update, manifest, and service-worker routes. The deployed PWA shell cache is `learning-compass-shell-v47`. No migration, D1/R2 data write, or corpus lifecycle/target-set mutation occurred.

## Current truth — 2026-08-29 (Continuum visual system and Home, deployed)

The active client design is now Continuum. The default system uses a soft graphite night palette, Manrope with Noto Sans Arabic, JetBrains Mono for compact metadata, a 220px labeled desktop rail, an 82px compact laptop rail, and a 58px command bar. Eight curated day/night presets plus Custom replace the old stock theme catalogue. Existing saved `botanical` settings migrate to `continuum`; fonts, typography, density, radius, reduced motion, custom palettes, and paired day/night editing remain user-controlled. The manifest and browser chrome use the night shell color.

Home now leads with the lesson whose turn it is in each active Thread. Each turn can open its Original, newest coherent ready HTML/PDF companion pair, and NotebookLM link from one compact passive material dock. The active Queue source stays visible in the contextual rail with the same material access, while Queue selection, Threads, resurfacing, RSS, and the tracked Start/Resume boundary keep their prior behavior. `/dashboard/briefing` now includes source URLs and the selected visible companion pair for those current lesson turns; staged artifacts remain hidden.

Verification passes 379/380 unit tests with the existing Chrome-unavailable environment skip, TypeScript, the full Worker-backed E2E suite across five roots and 16 internal modes, Android/offline and mobile shells, the production build at 117.08 KB gzip for base JavaScript, theme contrast audits, clean and idempotent migration rehearsal, Hermes contracts and deterministic manager harness, installed-client parity, `git diff --check`, and local desktop/tablet browser inspection with zero horizontal overflow. The E2E pass includes populated Home, theme persistence, typed objects, Queue behavior, and 44px-minimum responsive controls.

The guarded application-only release deployed Worker version `8cc708d7-36df-436b-b044-1aa5c2337d0c`; rollback is `bf9c38f5-6a3a-4995-99be-6dc7e998ec57`. Pre- and post-deploy readiness passed with the verified `backup_20260828T131356Z` recovery snapshot inside its freshness window. The canonical deployment smoke suite passed, and the broader live read-only audit returned HTTP 200 across liveness, readiness, budget, dashboard, capture/feed, Notes, Recall, learning-core, agent, search, Hermes analytics, intelligence, memory, public update, and manifest routes. The fingerprinted production shell serves the Continuum bundle and PWA cache v46. No migration, D1/R2 data write, or corpus registration, staging, upload, activation, rollback, or target-set mutation occurred.

## Native Hermes tools, installed locally

The default-profile `compass-native` plugin is installed and enabled for CLI and Telegram. Seven tools wrap the first-party Compass client, canonical source acquisition, page/annotation/OCR evidence, NotebookLM CLI, and optional Exa discovery. Tavily remains the default. Retired MCP/Thread/companion stubs are archived; focused skill ownership is unchanged. No Worker deployment or migration occurred.

Scoped verification passes native registration, guarded dry-run, live Worker health and NotebookLM reads, canonical article/hash extraction, text and PDF annotation fixtures, bounded subprocess failure, and exact-ID/no-reset NotebookLM checks. The two-query Exa trial succeeded; its limited discovery evidence is recorded in `integrations/hermes-compass/README.md`. The old repository-wide Hermes gate still rejects unrelated useful skills and expects a retired profile; it is not reported as passed. The primary checkout remains untouched, with implementation isolated on `hermes/native-compass-tools`.

## Current truth — 2026-08-30 (Preferences settings studio, local)

Settings → Preferences is rebuilt around decisions instead of repeated component demonstrations. A persistent desktop section index replaces the collapsed jump control, the active visual system reads as one global receipt, and one in-flow studio preview shows the combined theme, type, density, corner, surface, and action result before any controls. Workspace styles, comfort, expert appearance disclosures, learning behavior, and Map tuning now share one hierarchy and visual grammar; compact layouts convert the index to a horizontal strip and keep every control in a single readable column. No preference contract or persistence behavior changed.

## Current truth — 2026-08-28 (structural shell redesign, local)

The five-root client now has three explicit architectural planes: a 108px navigation-only desktop rail, a 64px workspace command bar, and the working field. Search and capture no longer compete with root navigation. Root context and grouped workspace modes live in the command bar, so Books/Triage/Archive/Files and the other mode sets no longer render as detached secondary headers on desktop. Ordinary workspaces retain a centered 1280px field while Atlas remains intentionally edge-to-edge.

Home is rebuilt as a master-detail workbench rather than a dashboard of equal cards. Current source and Queue share the primary decision row; Current rotation and incoming publications share the supporting row; editorial seams replace four floating boxes. Map Review keeps the branch index beside a readable dossier at ordinary laptop widths and moves signals beneath the dossier instead of squeezing a permanent third rail. Phone and tablet layouts keep the branded utility bar, five-item bottom dock, and root-count-aware mode grids. The attached anti-slop guidance informed hierarchy and anti-dashboard checks; marketing-page patterns were intentionally excluded. Browser inspection covers deployed and local Home, Library, Threads, Map Review, Preferences, desktop, and 390px layouts.

Verification passes 379/380 unit tests with one existing skip, TypeScript, the full Worker-backed E2E suite across five roots / 16 mode states / typed objects / Android and mobile shells, `git diff --check`, and the production build at 117.68 KB gzip for the base client bundle. Browser measurements show zero desktop document overflow. This remains local only; no deployment or production mutation has occurred.

## Current truth — 2026-08-28 (Themes preferences redesign, local)

Settings → Preferences now presents built-in themes as separate Day and Night collections with semantic studio miniatures and a visible selected state. The live contextual preview precedes controls in reading order while retaining the sticky desktop rail. Custom themes use one progressive workshop: day/night exploration and history first, declarative semantic color groups with rendered-token contrast feedback second, and AI/JSON/import/export/snapshots behind an advanced disclosure. Narrow layouts use one-column cards, unclamped descriptions, keyboard-accessible transfer controls, and 44px color/mode/import targets. Primary rendered ink is corrected for readable shell/surface use without mutating authored palette values; Gemini pairs fail validation when ink misses 4.5:1 on shell or surface. The active server palette remains authoritative during hydration, while the complete day/night pair and workshop history remain local-only. Client and Worker typography limits are aligned and finite out-of-range values clamp instead of disappearing during the D1 round trip.

Verification passes 372/373 unit tests with one existing skip, TypeScript, the full Worker-backed E2E suite, 19 focused post-fix theme/settings checks, `git diff --check`, and the production build at 117.38 KB gzip for the base client bundle. Bounded desktop/390px browser inspection confirms three theme groups, 21 semantic previews, 10 rendered contrast checks, preview-first source order, zero horizontal overflow or browser errors, and 44×44px mobile mode/color targets. No deployment or production mutation has occurred.

## Current truth — 2026-08-28 (full-tree release preparation, hard stop)

The complete dirty working tree is being aligned as one release candidate. Ordinary Learning Compass reads and writes remain public at the transport layer; the dormant private-auth experiment is removed, `POST /auth/session` remains absent, and first-party clients contain no Learning Compass credential-file, header, or browser-session path. Telegram and external providers retain independent secrets. Durable job ownership uses exact job/workflow identity, worker identity, lease expiry, payload, and supersession rather than an auth-derived lease principal.

At that 2026-08-28 checkpoint, migrations `0066` and `0067` were applied and `0068_lite_visual_corpus_activation.sql` was the sole pending migration. That migration instruction is historical and is superseded by the current truth at the top of this file: production is now applied through `0068`, while local `0069`–`0073` are the exact pending set. The checkpoint’s strict v6 backfill and release-gate evidence remain part of its historical release record.

## Current truth — 2026-08-28 (minimal Thread path, local)

The Learn → Threads flow is now decision-first and visually quiet. The index leads with active work instead of metric cards; typed Thread routes remove the competing Learn switcher; the command center uses a flat compact identity/progress spine, one primary lesson action, understated Now/Lessons/Projects/Resources text tabs, one inline resource count, and the Level journey without duplicate overview or progress panels. Normal Level links, Level indexes, material-owner links, and Lesson breadcrumbs return through the exact `#/learn/thread/:id?tab=curriculum&level=:levelId` state. Typed Level URLs remain recoverable deep links, but no normal path strands the learner on a parallel Level surface. Lesson routes now contain only breadcrumb/position/readiness, valid navigation/progression actions, Study materials, and one Notes/files/recall disclosure. Locked Lessons show one truthful Locked pill and no duplicate warning or disabled action; progression explanations, the Lesson-purpose contract, and authored-guide panel are absent. Each source exposes HTML, PDF, Original, and NotebookLM only as small accessible icon actions with complete purpose, availability, and metadata in their labels. PWA shell cache advances locally to `learning-compass-shell-v46`.

Verification passes six focused Thread contract tests, the complete Worker-backed E2E suite across five roots / 16 mode states / typed objects / mobile and offline behavior, the Impeccable detector with exact result `[]`, and the production build at 115.69 KB gzip for the base client bundle. Real local browser checks pass for normal and locked Lessons at desktop and 390px with zero horizontal overflow, no removed panels, no invalid locked action bar, and compact format icons; the exact Now → Level-in-Lessons → Lesson → Level-in-Lessons route round-trip is verified. Repository-wide TypeScript remains blocked by the pre-existing unrelated `src/index.ts:123` union access (`token` on `{ token?: string } | {}`). No deployment or production mutation occurred.

## Current truth — 2026-08-28 (signed v6 corpus replacement, local release hold)

The Lite Visual replacement trust boundary is implemented locally but not deployed or activated. Validation now HMAC-attests the exact `lite-visual-validation/v6` receipt with a mode-0600 local key; the Worker fails closed without its matching secret, verifies the attestation and five content hashes, derives pair identity server-side, requires R2 and verifies both object heads, enforces one unique HTML/PDF pair record, and binds every staged revision to one active `visualise_source` job plus the exact current-pair supersession. Migration `0068_lite_visual_corpus_activation.sql` adds manifest-, ordered-target-set-, and aggregate-audit-bound corpus state. Replacements stay invisible during resumable staging; one guarded D1 transaction is the only path that supersedes old pairs, exposes every expected replacement, completes the exact jobs, and moves the Thread's active-corpus pointer. The existing 122/294 production coverage remains unchanged: 96 newly published pairs plus 26 earlier pairs.

The authoritative 268-target set remains `18a24b536b45d5c95fd55e638b334f8f933d6707ee024c6672f3fcc45b451f3f`. The full signed validation and semantic-completeness evidence remain incomplete; aggregate audit, independent hardening closure, Worker secret installation, final full gates, migration/deployment, bounded staging, atomic activation, and live `294/294` verification remain blocked behind accepted results. Current local verification passes 342 runnable unit tests with one environment skip, TypeScript, clean/idempotent migration rehearsal, Hermes contract/prompt budgets, the Python orchestration suite, Python compilation, profile-script parity, the 116.94 KB gzip base client build, and `git diff --check`. No Worker deployment or production mutation has occurred.

## Current truth — 2026-08-28 (Riyadh corpus companions, semantic-completeness hold)

The remaining 268 Riyadh workspaces are mechanically complete and pass the current `lite-visual-validation/v5` source-scope, traceability, responsive, A4, and HTML/PDF parity gates, but they are not yet proven complete enough to replace the recordings. Direct review of recording 120 found meaningful omitted interpretation despite a passing receipt: the coarse-garment hadith lost the explicit lessons of Prophetic asceticism, humility, contentment, and avoidance of extravagance and pride, and the closing supplication lost its explicit rejection of worldly adornment and pleasure. The weakness is systemic in the gate: one broad claim can satisfy an approximately 300-word source scope without proving every fact inside it reached the article. Across the batch, 121/268 articles contain less than 40% of their source word count, 195/268 contain less than 50%, and the median ledger has one claim per 280.8 source words. These figures do not prove every article is defective, but they invalidate the corpus-wide completeness claim. Production currently exposes 96 newly published pairs, recordings 019–081 and 088–120, plus 26 earlier pairs; all 96 new exact jobs completed and no pair is partial. The remaining 172 pairs must not resume publication after the free-tier reset until each source is rebuilt or cleared against a finer atomic inventory of every hadith, ruling, example, qualification, attribution, correction, definition, and conclusion. Pair identity and publication hardening remain valid: five-hash identity, workspace/transcript provenance checks at upload, public-address validation for every browser request, atomic publication, canonical readback, and exact job completion.

## Current truth — 2026-08-28 (caption-first media transcription, local)

Hermes now has one active `media-transcription-systems` owner for timed-media transcript acquisition. YouTube extraction uses manual or generated published captions whenever any track exists and may invoke audio transcription only after a complete caption inventory positively confirms zero tracks. Caption lookup failures, IP/library errors, and published tracks that fail download, parsing, or completeness checks block audio instead of silently downloading the video. Accepted audio fallback receipts preserve `audio_fallback_reason=youtube_captions_confirmed_absent` and exact absence evidence. The workflow carries forward the completed Riyadh corpus pattern: measured and hashed audio, bounded 240-second chunks, hash-matched checkpoints, verbatim timestamped prompts, deterministic coverage checks, atomic transcript/receipt writes, resumable retries, and temporary-audio cleanup. The shared skill, Lite Visual extractor, YouTube verification procedure, default Hermes profile, Compass profile, router, project contract, and regression tests are synchronized. The active graph now contains 27 owned skills; measured Telegram fixed payloads are 49,815 bytes for default Hermes and 50,179 bytes for Compass with the same 13-tool surfaces. This configuration/workflow change is local and does not require a Worker deployment.

## Current truth — 2026-08-27 (Riyadh Levels 3–5 companions, published)

Levels 3–5 of `شرح رياض الصالحين — الشيخ عبد الرزاق البدر` now have complete Lite Visual coverage: all 13 lessons expose one ready Arabic HTML/PDF pair. The five Level 3 pairs were already present; eight new source-specific pairs were validated with `lite-visual-validation/v5`, atomically published, verified through each canonical source dossier, and their exact `visualise_source` jobs read back `completed`. Production now has 26 complete companions across the 294-source Thread. The broker transcript for recording 011 was rejected despite `quality.passed=true` because one long passage repeated four times; the published pair instead used a prior passing transcript whose stripped transcript hash and current official MP3 hash both matched their receipt, with no repeated 40-word window. Recordings 012–017 used the same hash-matched bounded fallback after the broker failed to complete within the run; recording 018 used a passing broker receipt. The active Riyadh skill now rejects repeated-chunk false positives rather than trusting the provider flag alone.

## Current truth — 2026-08-27 (Riyadh source-type repair, deployed)

All 294 lesson sources in `شرح رياض الصالحين — الشيخ عبد الرزاق البدر` now retain their existing canonical YouTube URLs and read `content_type=video`. A bounded preconditioned `PATCH /recommendations/content-types` route corrected the 293 stale `article`/`audio lecture` classifications without changing source identity, Thread attachment, branch ownership, or artifacts; it rejects missing/deleted records, changed prior types, duplicates, and non-YouTube URLs before writing. Production readback confirms 294/294 video records, zero mismatches, 18 complete Lite Visual HTML/PDF pairs, 276 sources without a complete pair, 2/183 completed Levels, and 7/294 completed lessons. Verification passes 333 unit tests with two existing skips, TypeScript, the 116.94 KB gzip production build, 30 agent-contract tests across 119 owned routes, focused repair tests, and `git diff --check`. Worker version `45c93b9d-55a7-4d76-8aa0-e30af425e69f` is deployed; rollback version is `9d950650-eccd-405e-bd85-9cd6257c030a`.

## Current truth — 2026-08-27 (Learn opens on Notes, deployed)

The Learn root now opens Practice → Notes by default. Threads remains directly addressable through `#/learn?mode=paths`, and typed Thread, Level, Lesson, Note, Unit, Recall Card, and Canon-domain routes retain their owning Learn mode without adding redundant query state. Thread breadcrumbs return to the explicit Threads index instead of the new Learn default. The installed PWA shell cache advances to `learning-compass-shell-v45`.

Verification passes 331 unit tests with two existing Chrome-unavailable skips, TypeScript, the 116.94 KB gzip production build, full five-root/16-mode desktop/mobile/offline E2E, clean and idempotent rehearsal of all 69 migrations, 30 agent-contract tests, Hermes synchronization and both Telegram prompt budgets, the 37-case deterministic manager harness, and `git diff --check`. The initially observed Data Studio timeout did not reproduce on the complete rerun. Worker version `5ad589b7-d710-447e-b388-e3318b846847` is deployed. Post-deploy `/health/live` and `/health/ready` return 200 with zero blockers; every required application read passes, the ordinary evidence search returns 200, malformed unauthenticated writes reach domain validation, retired `/auth/session` returns 404, and live desktop/390px checks confirm Notes and Threads active states with zero overflow or browser errors.

## Current truth — 2026-08-26 (Hermes manager architecture, local)

Worker version `8c36ae09-7496-4d2d-a555-a8fbcfd25dac` removes the Learning Compass API-token gate, browser unlock prompt, signed-session exchange, and production auth flag. Post-deploy `/health/live` and `/health/ready` return 200; unauthenticated `/agent/capabilities` returns 200; retired `POST /auth/session` returns 404; and an unauthenticated malformed write reaches normal domain validation with 400 instead of an authentication challenge. The release passed 331 unit tests with two environment skips, TypeScript, a 116.82 KB gzip base-client build, Hermes contract verification, both prompt budgets, and `git diff --check`.

Hermes now operates Learning Compass through one first-party path: manager router → focused on-demand site-operator skill → `site_request.py` → allow-listed Worker API → canonical D1/R2 state → verified receipt. Learning Compass and NotebookLM MCP subprocesses remain absent. Open-ended management uses one `/agent/briefing` read; ordinary work uses the narrow target endpoint; `/agent/context` is reserved for decisions that genuinely need broader state. The site client owns the turn-local call ledger across fresh foreground skill handoffs: Hermes exports only a 76-character `turn-sha256-…` identity, while reusable/background PTYs and helpers receive none; owner-only, bounded, atomically locked state caches successful GETs and preserves the two-attempt safe-GET budget across processes. This is coordination state, not authentication or an exactly-once security boundary. The client owns the only bounded timeout/5xx retry for a top-level read-only GET; manager instructions forbid re-invoking the exhausted command. Mutations, 4xx responses, and guarded verification reads are never generically retried.

The mutation boundary now preserves ambiguous outcomes as durable non-expiring tombstones and requires stable idempotency, exact high-risk confirmation/preconditions, and canonical readback. Exact verification is defined for Compass picks, the job lifecycle, and note deletion; deletion succeeds only with the exact target's observed 404/`absent:true` receipt. Briefing is assembled in one D1 batch, distinguishes delayed from overdue retry work, and reports unavailable/degraded sections rather than healthy empty arrays. Agent responses, receipts, activity metadata, and job errors are recursively redacted and bounded.

Loaded memory is verified rather than inferred from file size: `MEMORY.md` is 2,007/2,200 characters with 9 entries and `USER.md` is 1,358/1,375 with 7; both tails load, mirrors match, and normalized duplicates are zero. The default Telegram fixed payload is 49,694 bytes (25,596 system + 24,098 schemas, 13 tools), down 1.1% from the task's 50,244-byte baseline and 10.7% from the broader 55,657-byte measurement. Compass Telegram is 49,940 bytes (25,825 + 24,115, 13 tools). Both checked contracts pass their slightly higher fail-closed ceilings. Ordinary router plus site-operator instructions are 19,642 bytes (8,086 + 11,556), down 57.5% from 46,180 and within the 19,724-byte release ceiling. From this Worker repository, default/Compass CLI totals are 105,012/78,126 bytes; CLI context is project-sensitive, so cross-directory comparisons are invalid.

A short-lived private-auth experiment from this work period was never the accepted product contract and is now retired. Current source removes its token gate, browser-session exchange, client token-file plumbing, and auth-derived lease ownership. Service-worker activation still purges the retired pre-auth namespace; artifact cache v2 and the upload, URL, Web Push, and R2/D1 compensation checks remain verified independently.

Active synthetic R1/R2/R3 knowledge rounds are removed from derivation, API/sync/capture projections, types, search, and rendering; only inert historical columns, migration names, and bracket-label cleanup remain. Zero-TTL reads bypass memory and edge caches. Six end-to-end integration flows pass, including upgrade, Branch Deck, Compass feedback, discovery, feedback proposals, and Taste Rec. Canonical and Compass site-client mirrors each pass 50/50 tests, compile, and are byte-identical. The full client E2E harness now bounds navigation/service-worker waits and cleans up its Wrangler/Workerd/Playwright lifecycle.

Migrations `0066` and `0067` were applied remotely and had no pending successor at that release point; the current release candidate adds local `0068`, which must be the sole pending remote migration before application. The portable ordinary search projection is live, the six dangling Thread pointers are repaired without deleting learning events, and the previous Worker's legacy search command remains rollback-compatible. Production readiness is healthy with zero blockers. Data quality is trusted 5/5: all 487 active sources have valid identity, branch, and uniqueness; all 257 learning events have valid lineage; all enabled feeds have reviewed default branches. Backup `backup_20260826T123709Z` includes a checksummed D1 export, all referenced R2 objects, and a passing disposable restore rehearsal.

The deterministic manager harness validates all 21 definitions and passes 37/37 tests. Foreground turn propagation passes 40/40, and both installed site clients pass 52/52 with byte-identical canonical/profile copies. Focused regressions pass for the single-recommendation routing rule, personal-media branch/domain receipt, job capability reuse, and complete five-item dossier verification. Preserved combined real-model slates remain diagnostic evidence with variable 19/21 and 20/21 outcomes; they are no longer a Worker deployment gate because their sampling is nondeterministic. The deterministic harness plus focused defect regressions is the canonical manager release gate.

The latest local gate passes 344/346 Worker tests with two expected Chrome skips, TypeScript, the 117.14 KB gzip base build, clean/idempotent migration rehearsal, full five-root/15-mode mobile/offline E2E, 69-migration/32-check/120-route Hermes verification, prompt contracts at 49,694 bytes default and 49,940 Compass, and diff checks. The prior detached isolation proves the 146-path release boundary; later changes are limited to synchronized release truth and installed manager skills, so a new final isolation hash is still required before deployment. The exact secret scan checks 13 configured credential values across 161 changed/injected/log files with zero hits. The Hermes API-server key was rotated: the new key returns 200 and the old key returns 401.

The historical private-auth rollout described during this work period has been superseded by the public ordinary API contract and its code/secret dependencies are not part of the current release. The previously exposed Linux/sudo password still requires separate user-present rotation; it is not part of the Worker deployment and no machine shutdown is scheduled.

## Current truth — 2026-08-26 (cross-product UI and workflow hardening, deployed)

The five-root product audit repaired the highest-impact confirmed defects without changing canonical learning behavior. Personal Assistant previews now clear before each interpretation, expose an accessible Arabic/RTL input, allow questions-only taste signals to be saved, and require granular selection for every durable profile signal; a knowledge branch is required only when selected media records are being saved. Global Add anything now moves focus into the newly selected type and cannot be dismissed while a save is in flight. Locked Thread Levels distinguish prerequisite locks from missing material. Library and Settings labels now consistently read Files and Data & recovery, and narrow-screen Home heading actions, Queue controls, and Notes deletion meet the 44px product target. The Personal Assistant surface returns to the flat Botanical Folio token system instead of a gradient and inset accent stripe. PWA shell cache advances to `learning-compass-shell-v44`. Verification passes 298 tests plus two environment-only skips, TypeScript, Hermes synchronization across 67 migrations / 29 checks / 116 owned routes, clean and idempotent migration rehearsal, production build at 116.90 KB gzip base JavaScript, full desktop/mobile/offline E2E, live five-root responsive smoke checks, production PWA readiness, and remote migration parity. Worker version `285f3765-2663-4df9-990f-ce04f6699d53` is deployed; rollback version is `ea3b8152-4060-4686-9d0f-72c5ef11ce4c`. `/health/live` is healthy; aggregate `/health` remains at the pre-existing `needs_attention` state because of one overdue retry and the known recursive FTS maintenance failure.

## Current truth — 2026-08-25 (stopped-feedback Queue exit, local)

Ordinary `POST /feedback/record` and session-return feedback now treat `completion_state=stopped` as a terminal user rejection: the source becomes `rejected`/`excluded`, leaves Queue, resolves any active Compass pick, preserves the exact reason and reflection, and still queues Taste Mapper without starting note extraction. `in_progress` remains the only nonterminal feedback state. The focused regression test, TypeScript, production build, and diff checks pass. Deployment is intentionally pending because the working tree contains a larger in-flight release that must not be partially published.

## Current truth — 2026-08-25 (feedback branch mapping, deployed)

`POST /feedback/record` now accepts an explicit verified `branch_id` and persists it atomically with feedback, including when a source is completed. The direct high-confidence `/capture/:id/branch-map` repair route now also accepts existing completed/excluded sources without changing consumption state. The previously unlinked قيام الليل video was repaired and verified under `qiyam-al-layl` (`status=love`, `priority_rank=1`). Verification passes the full unit suite (294 passed, 2 environment-only Chrome skips), TypeScript, production build, Hermes contract checks, live branch-map/source-dossier readback, and branch ledger readback. The integration feedback-flow test could not start its local Wrangler worker in this environment. Deployed Worker version `ea3b8152-4060-4686-9d0f-72c5ef11ce4c`.

## Current truth — 2026-08-25 (Personal Assistant, deployed)

Personal Data Studio now includes a natural-language Personal Assistant with three modes: log consumption, log-and-ask, and get-to-know-me. `POST /assistant/interpret` uses the server-side AI key to turn Egyptian-Arabic or English notes such as “اتفرجت على الفيلم ده وعجبني” into a reviewable preview of typed books, movies, series, podcasts, courses, games, albums, or other records, plus high-confidence taste signals and at most two follow-up questions. It is deliberately no-write: the user must select a verified branch and explicitly confirm before personal records or profile assertions are saved. The live smoke test correctly extracted Interstellar and Atomic Habits, ratings 8 and 3, and one follow-up question; the personal ledger remained at 36 records. Worker version `d650ce4d-66a0-4a65-aacd-1b013fc916b0` is deployed.

Verification passes 296 unit tests (294 passed, two environment-only Chrome skips), TypeScript, production build (116.65 KB gzip base client JavaScript), full five-root/15-mode mobile/offline E2E, clean/idempotent migration rehearsal, Hermes synchronization across 67 migrations / 29 checks / 116 owned routes, and `git diff --check`.

## Current truth — 2026-08-25 (Hardcover sync/import, deployed)

Settings → Data & recovery now includes an explicit Hardcover panel. The Worker uses the server-only `HARDCOVER_API_TOKEN` to refresh a D1 mirror through `POST /hardcover/sync`; `GET /hardcover` exposes exact mirror counts, statuses, progress, covers, journal counts, and import linkage without exposing the token. `POST /hardcover/import` requires a user-selected verified branch and can import selected or all unlinked books into the typed personal ledger without creating Queue commitments. The live sync completed successfully as `MahmudElmorsy`: 45 books, 116 journal entries, zero imported books, and last sync `2026-08-25 00:58:57`.

Verification passes 292 unit tests (two environment-only Chrome skips), TypeScript, production build (114.78 KB gzip base client JavaScript), full five-root/15-mode mobile/offline E2E, clean/idempotent migration rehearsal, Hermes synchronization across 67 migrations / 29 checks / 115 owned routes, and `git diff --check`. Worker version `8a4965f9-b630-451c-9974-dc797a1119f7` is deployed; previous version `459f6b69-6d6e-447c-aac1-948582487d0e` remains the immediate rollback target.

## Current truth — 2026-08-25 (book-state reconciliation, deployed)

The Personal Data Studio now matches the canonical Books ledger for imported books. Migration `0065_reconcile_personal_book_states.sql` repaired the five consumed books that the first personal-library import had labeled `planned` because their older records stored completion in recommendation status or learning state instead of `book_reading_state`. Production now reads 6 finished books, 1 in-progress book, and 29 saved books in both Books and the personal ledger; no intentional personal edits were overwritten. The remote migration queue is empty, and the repair was verified through both `/recommendations/books` and `/capture/personal`.

## Current truth — 2026-08-25 (typed personal Data Studio, deployed)

Learning Compass now has one branch-verified personal ledger for books, movies, series, podcasts, courses, games, albums, and other media. Global **Add anything** keeps ordinary source/file capture intact, adds a fast type-aware core form, and progressively discloses link, release year, duration, progress, rating, tags, and personal note. Migration `0064_personal_library_studio.sql` adds `personal_library_items` beside canonical recommendation identity, backfills existing books, synchronizes Books reading state in both directions, and records import/create/update lineage in `learning_events`. Canonical URL or normalized type/title/creator deduplication prevents duplicate identity; item type and ID stay fixed while every useful personal field remains editable. Personal state is deliberately separate from recommendation lifecycle and Queue: logging, progressing, or finishing media never creates a Queue commitment, session, lesson completion, or automatic recommendation.

Settings → Data & recovery now opens with the Personal Data Studio. It renders exact tracked/in-progress/finished/rated counts, interactive type and status distributions, six months of edit activity, and branch distribution; its searchable, filterable ledger exposes every mutable field inline and confirms durable lineage on save. JSON and Markdown library exports now preserve type, state, progress, rating, tags, personal note, branch, and canonical link. The agent/OpenAPI surface owns `GET/POST /capture/personal` and `GET/PATCH /capture/personal/:id`, `/agent/system` counts personal records, the active Hermes router/operator/ingestion contracts distinguish personal history from source ingestion, and the PWA shell cache advances to `learning-compass-shell-v43`.

Verification passes 290 unit checks (288 passed, two environment-only Chrome skips), TypeScript, the production build (114.23 KB gzip base client JavaScript), full five-root/15-mode mobile/offline E2E including API create/dedup/edit/clear/export/Queue isolation and the 390px Data Studio, clean and idempotent rehearsal of all 66 migrations, Hermes synchronization across 29 checks and 112 owned routes, exact canonical/profile skill mirrors, and `git diff --check`. Production migration `0064` imported 36 active books into 36 valid personal records with 36 lineage events and zero orphans; the remote migration queue is empty. Worker version `459f6b69-6d6e-447c-aac1-948582487d0e` is deployed with Worker rollback `1043e9c6-9607-4b66-91e3-6b485feae5ac` and pre-migration D1 Time Travel bookmark `000007d5-00000000-000050d2-f26ed89ebd13838b4c2295939ceb8ae7`. Every required functional release read returns HTTP 200, the live API/OpenAPI expose both personal paths, PWA cache v43 and the new asset fingerprints are served, and read-only desktop/390px browser checks render all 36 rows with Add anything visible, zero horizontal overflow, and zero browser errors. Aggregate `/health` remains `needs_attention` only for one pre-existing overdue retry, stale/absent maintenance success, and no recent verified full recovery backup; the attempted pre-release full snapshot made no schema write and remains blocked because Cloudflare D1 export refuses databases containing FTS5 virtual tables.

## Current truth — 2026-08-25 (data trust contracts and branch-safe RSS, deployed)

Learning Compass now applies explicit data contracts instead of treating row volume as data quality. `GET /agent/system.data_quality` and Settings → Data & recovery expose five named completeness, validity, uniqueness, and lineage checks with exact affected counts and coverage over active sources, learning events, and enabled feeds; the existing retained-source inventory remains separately truthful. Manual capture and feed subscription require one verified non-pruned top-level branch in both the UI and OpenAPI contract. Migration `0063_data_trust_and_feed_branches.sql` adds a D1-enforced default branch to every feed, backfills the three current subscriptions, and maps their 33 previously unclassified active RSS captures without overwriting explicit source mappings. New feed source records inherit the reviewed default atomically; a deduplicated source preserves its existing canonical branch and reports `branch_conflicts` rather than being silently remapped. Feed and article branch pills render in Library, and the PWA shell cache advances to `learning-compass-shell-v42`.

The pre-release audit measured 556 retained sources and 470 active sources, with all 33 active branch gaps coming from the three legacy RSS feeds; active source identity and deduplication were complete and no duplicate canonical keys were found. Verification passes 285 unit tests with two environment skips, TypeScript, the production build (108.93 KB gzip base client JavaScript), full five-root/mobile/offline E2E, clean and idempotent rehearsal of all 65 migrations, Hermes synchronization across 27 checks and 108 owned routes, and `git diff --check`. Production migration `0063` is now applied: all three enabled feeds have valid reviewed defaults, all 33 source gaps are repaired, and the live source identity, branch, uniqueness, and feed-default contracts pass. Event lineage separately and truthfully flags six legacy events that reference removed Threads; none comes from the 36 valid `0064` personal-library imports. This contract shipped in Worker version `459f6b69-6d6e-447c-aac1-948582487d0e` with Worker rollback `1043e9c6-9607-4b66-91e3-6b485feae5ac`.

## Current truth — 2026-08-24 (free-tier circuit breaker, deployed)

Every dynamic Worker request now atomically reserves a conservative route cost in a UTC-day D1 ledger before execution. Reads stop at 4,000,000 estimated rows and writes stop at 70,000 estimated rows, leaving 20% and 30% headroom below Cloudflare's free limits; rejected work returns `429 daily_free_tier_budget_exhausted` with the UTC reset time while static assets and health diagnostics remain available. `GET /health/free-tier-budget` exposes the live ledger. Migration `0062_free_tier_budget.sql` is applied, the real list route reserved exactly 5,000 read units, and the account audit confirms zero KV namespaces plus an idle second D1 database with zero operations. Verification passes 279 unit tests with two environment skips, TypeScript, production build, full E2E, clean/idempotent rehearsal of all 64 migrations, live budget/list/health readback, and `git diff --check`. Deployed as Worker version `1043e9c6-9607-4b66-91e3-6b485feae5ac`; rollback is `eab1cc25-c1a0-464f-a8c9-76ef944eb3aa`.

## Current truth — 2026-08-24 (D1 row-read repair, deployed)

The two dominant recommendation reads no longer run note, recall, and artifact scans once per returned source. `GET /recommendations/list` and the branch linked-items dossier now select their recommendation rows first and enrich only those IDs in bounded batches of 75, preserving note, recall, and HTML/PDF companion projections. Migration `0061_recommendation_read_efficiency.sql` adds the missing recommendation/due index for recall cards. Cloudflare's pre-fix 24-hour diagnostics measured 8,117,072 rows read; the list query alone used 7,381,018 rows across 34 calls (about 91%). Verification passes 277 unit tests with two environment skips, TypeScript, production build, full E2E, clean/idempotent rehearsal of all 63 migrations, live list and branch contract readback, migration readback, and `git diff --check`. Deployed as Worker version `eab1cc25-c1a0-464f-a8c9-76ef944eb3aa`; rollback is `864fafe7-f868-4ffc-bbcb-90b20f4192fc`. The rolling Cloudflare 24-hour counter will retain pre-fix reads until they age out.

## Current truth — 2026-08-24 (completed lesson correction)

Learner-confirmed corrections can now reopen a completed lesson. Status synchronization derives the affected Level and Thread from direct lesson status, returns a completed Thread to active progression, clears stale completion timestamps, and locks later Levels until the reopened prerequisite Level is complete. This repairs the Behavioral Psychology migration mismatch exposed by its Obsidian checklist without letting sources, notes, projects, or legacy items control progression. Nine affected lessons are reopened with the remaining path preserved in lesson content, three lessons remain completed, Level 1 is current, and the Thread reads `active`. Verification passes 276 unit checks with two environment skips, TypeScript, production build, migration rehearsal, the agent/Hermes contract, full E2E, guarded mutation receipts, exact Thread/Hub readbacks, and integrity health. Deployed as Worker version `864fafe7-f868-4ffc-bbcb-90b20f4192fc`; rollback is `34ea7c5d-bfc7-46ca-b664-edc4b9bb063a`. Aggregate `/health` retains only the pre-existing maintenance-search and recovery-backup blockers.

## Current truth — 2026-08-24 (Learning Thread Vertical Journey, deployed)

The selected Vertical Journey prototype is now the real Learning Thread command center. Overview, Curriculum, Practice, and Materials share one persistent Thread spine with direct lesson progress, exact Level/lesson position, and one next move. The working surface starts at the canvas's left edge and uses its full available width instead of centering the tab body in a narrow `1120px` column. Overview keeps a compact complete Level map without repeating the spine action; Curriculum and Practice keep exactly one Level open without duplicate Open/Review footer buttons; Materials keeps one Thread/Level/Lesson owner open with type/owner facets and bounded excerpts, with item titles carrying navigation instead of redundant owner-workspace actions. Search, owner, and item results reveal in batches of 24, selected Curriculum/Practice Levels persist in the typed Thread URL, typed Level/Lesson breadcrumbs recover the command center, and typed Level pages no longer auto-open the full Level wall. Projects and synthesis remain optional practice, while only direct lesson completion advances Levels and Threads. Empty Threads now read as setup rather than completed. The layout is container-aware, preserves the Botanical Folio hairline system, exposes all four tabs as a visible 2×2 control on narrow canvases, and keeps coarse-pointer targets at least 44px. The PWA shell cache advances to `learning-compass-shell-v41`.

Verification passes 276 unit checks with two environment skips, TypeScript, the production Vite build, the focused Vertical Journey source contract, the final Impeccable layout detector with exact result `[]`, Hermes synchronization across 62 migrations / 27 checks / 108 owned routes, clean and idempotent migration rehearsal, and `git diff --check`. The full E2E contract was updated for the new journey, exact Level URL persistence, owner disclosure, and collapsed typed-Level spine; it was intentionally not executed because this task explicitly required code-only verification without browser use. Deployed as Worker version `34ea7c5d-bfc7-46ca-b664-edc4b9bb063a`; rollback is `f82e267a-c82d-42c7-ab9b-161f30c266d8`. Production liveness and all functional release reads pass, including the exact `thread_1786576304649_69e7c056` path with 7 Levels / 71 lessons, fingerprinted JavaScript copy, Vertical Journey CSS, PWA cache `v41`, and source-anchored evidence search. Aggregate `/health` remains `503 needs_attention` only for the pre-existing stale/absent maintenance success and missing recent verified full recovery backup.

## Current truth — 2026-08-24 (automatic flash-card generation disabled, deployed)

No automated workflow can create flash cards or recall drafts. `source_note_v2` and legacy extraction reject non-empty `srs_drafts`; extraction no longer contains a draft insert; retain/apply consolidation waives recall preparation and closes from the proportional note, anchored Units, and complete anchors alone. Direct text generation, legacy HTML-to-SRS sync, and the dormant Hermes maintenance backfill cannot create cards. New cards remain available only through an explicit learner-authored Arabic question and answer, and existing drafts/cards remain editable, approvable, reviewable, and deletable. The obsolete Recall-generation preference was removed from Settings, and both active Hermes skill trees, payload examples, repository contracts, and operating guidance now prohibit generated cards. Verification passes 271 unit checks with two environment skips, TypeScript, production build, full E2E including rejected generated drafts and successful note-only consolidation, Hermes synchronization, skill-mirror comparison, focused no-insert regression checks, and `git diff --check`. Deployed as Worker version `98d67de9-a4ae-470b-a01f-abf6d7c6a534`; rollback is `61aa6ddd-fdbe-4bec-8e15-1c87ecf31525`. Production liveness passes, both automated generation routes return HTTP 409, and the existing 49 Arabic drafts remain intact (33 pending, 4 approved, 12 rejected) alongside one active learner-approved card.

## Current truth — 2026-08-24 (Arabic recall contract and legacy backfill, deployed)

Every recall question and answer is now primarily Arabic across source-note extraction, legacy extraction completion, direct card creation, draft editing and approval, and HTML-card sync; exact technical terms may remain in their original language. Recall content renders RTL in the Recall, Thread, Level, and Lesson surfaces. All 49 existing drafts were translated through the live Worker API without changing IDs, statuses, provenance, ownership, anchors, or review schedules; production currently has 36 pending, 3 approved, and 10 rejected drafts, with 0 active cards. Hermes active/default profile skills and completion examples are synchronized, and the stale rating-based extraction instruction is removed. Verification passes 271 unit checks with two environment skips, TypeScript, production build, full E2E, Hermes contract synchronization, skill-mirror comparison, and `git diff --check`. Deployed as Worker version `3d175f37-234d-4e62-974b-26c27150cb0a`; rollback is `74585e73-1f0c-49d0-93b4-3205895a089b`. Live contract `2026-08-24`, liveness, Arabic idempotent write/readback, complete legacy readback, and HTTP 400 English rejection are verified.

## Current truth — 2026-08-24 (rating-only note extraction removed, deployed)

A feedback score never infers `retain` and never queues Notes Extractor, including scores of 7 or higher. Structured source-note extraction and recall-draft preparation now require an explicit `retain` or `apply` disposition; the legacy `auto_enqueue` bypass and the extraction-completion rating fallback are removed. Personal reflections remain preserved as reflection notes, and feedback analysis remains unchanged. Verification passes 267 unit checks with two environment skips, TypeScript, production build, full E2E, clean/idempotent migration rehearsal, Hermes contract synchronization, and `git diff --check`. Deployed as Worker version `74585e73-1f0c-49d0-93b4-3205895a089b`; rollback is `86d1cd3e-7c5a-4b66-93f5-fc45d7c0d90a`. Live liveness and all six functional release reads return HTTP 200; aggregate `/health` remains blocked by the pre-existing failed FTS maintenance and missing verified recovery backup.

## Current truth — 2026-08-23 (Compass CPU repair, deployed)

`GET /compass/pick` no longer reloads the complete Thread coverage corpus for every stale pick it repairs; one bounded request now reuses one canonical coverage snapshot across its existing 25-pick repair limit. `POST /compass/picks` persists its already-capped 3–24 scored candidates in one D1 batch instead of serial post-commit writes. Scoring, selection, response fields, source content, and Queue state are unchanged.

Verification passes 269 unit checks with two environment skips, TypeScript, production build, full E2E, clean/idempotent rehearsal of all 62 migrations, Hermes contract verification across 27 synchronized checks and 106 owned routes, and `git diff --check`. Live `GET /compass/pick` returns HTTP 200 with ready pick `pick_21389aa0-f1b4-425a-8db9-337a44644395`; canonical dossiers for `cap_1787483371109_ca6cc8` and `cap_1787483373993_0d6387` return HTTP 200 with their original Compass pick IDs and verified active `pkm` and `neg` branches. Deployed as Worker version `86d1cd3e-7c5a-4b66-93f5-fc45d7c0d90a`; rollback is `a7854657-56ff-4948-9a53-602add047fc3`. `/health/live` and every required functional read pass; aggregate `/health` remains `needs_attention` only for the pre-existing stale maintenance success and missing recent verified full recovery backup.

## Current truth — 2026-08-23 (Atlas reliability and focused navigation, deployed)

Map → Atlas now fills all workspace height below the mode switcher instead of stopping at its 480px minimum inside the keyed workspace wrapper on tall displays. It opens from the canonical knowledge graph without waiting for the unrelated learning-balance projection, and later frontier enrichment no longer resets an active view. Typed node routes reveal and focus their exact node without remounting the workspace. A selected node exposes one-click neighborhood focus across its ancestry, subtree, and immediate semantic links; the canvas is keyboard-focusable with arrow-key traversal and Enter expansion. Preference saves expose saving/saved/failure state, Reset cancels stale debounced writes before restoring the canonical PC preset, and the preset now dims unrelated nodes by default while retaining arrows off, 0.85× nodes, 1.4× evidence links, 1.5× hierarchy links, and 14 / 0.65 / 1.25 forces. Browser zoom shortcuts are no longer hijacked, reduced motion disables hover pulsing, minimap framing refreshes on resize, meaningful bracketed titles remain intact, edge IDs cannot collide with node IDs, mobile controls no longer cover breadcrumbs, short desktop viewports no longer clip at a forced 480px minimum, and force interaction changes from all-pairs to spatially bounded neighborhoods above 320 visible nodes. The PWA shell cache advances to `learning-compass-shell-v40`.

Verification passes 268 unit checks with two environment skips, TypeScript, production build, full five-root/mobile/offline E2E, clean and idempotent migration rehearsal, Hermes synchronization across 27 checks, the focused Impeccable detector, `git diff --check`, and exact live desktop/mobile browser replay. At a 1260×1300 viewport, the live workspace, Map surface, and Atlas now measure 1300 / 1300 / 1224px with zero unused lower space and zero overflow. The live 205-node/204-edge graph focuses `Applied Creativity` to four nodes, preserves neighborhood state during keyboard traversal, changes the typed route to the traversed node, leaves browser Ctrl/Cmd zoom shortcuts untouched, renders mobile controls without breadcrumb collision or overflow, and reports no console or page errors. The saved PC preset preserves Mahmood's 0.1 center / 17 repel tuning and now enables focus dimming. Deployed as Worker version `a7854657-56ff-4948-9a53-602add047fc3`; rollback is `078f19ba-3f87-4440-a641-d4d743b0f291`.

## Current truth — 2026-08-23 (Atlas Obsidian-style force motion restored, deployed)

Map → Atlas restores lively force motion while closing the production force-control regression and reducing full-graph clutter. Initial load and depth changes preserve the hierarchy-aware constellation without automatically applying center attraction; physics wakes only for direct manipulation or force-control input and always cools to rest. Hover pulse is visual-only and cannot move nodes. Single tap selects without moving the camera, allowing two quick taps on the same node to reliably toggle its complete subtree across mouse and touch without changing zoom; empty-canvas double-tap retains fit behavior. Canonical defaults remain arrows off, 0.15 text fade, 0.85× nodes, 1.4× evidence links, 1.5× hierarchy links, and balanced 14 / 0.65 / 1.25 charge-center-spring forces. The installed PWA shell cache advances to `learning-compass-shell-v38`. The release passed Atlas tests, TypeScript, production build, full E2E, and a real live browser double-click check that changed 27→22→47 nodes while zoom remained exactly `0.3938898361`. Production liveness and required functional reads return HTTP 200; aggregate readiness remains `needs_attention` from pre-existing operational debt. Deployed as Worker version `62291387-f267-4f43-b6bf-c1f3a4926640`; rollback is `77a17b59-49a9-4353-91bf-ab66161c1709`.

## Current truth — 2026-08-23 (durable bilingual note-authoring contract, local)

Hermes Notes Extractor now optimizes for useful four-year retrieval rather than chapter replay: precise English terms, claims, and evidence are paired with concise natural Egyptian-Arabic explanation in separate directional blocks. Supported sources include an explicit Misconception vs. Truth outline and complete Case Studies & Experiments coverage with researchers, year, methodology, findings, and stated limitations. Handwriting remains verbatim in native note blocks without Obsidian callouts, frontmatter, wiki links, embeds, or highlight syntax. Book and book-chapter notes remain in Learning Compass and never create Obsidian archive copies. This is a local Hermes/documentation contract change; no Worker deployment is required.

## Current truth — 2026-08-23 (Obsidian concept integration, deployed)

Eleven recurring Obsidian product concepts are now integrated without restoring Obsidian as canonical storage: daily branch-connected resurfacing with explicit weighting and seven-day suppression; anchored meaningful backlinks; derived cross-branch bridges; reversible contradiction review; advisory adaptive depth; knowledge-frontier states; evidence-indexed perspective diversity; additive progressive note distillation; explicit context-aware delivery; and explainable semantic relationships. The requested daily review mode was intentionally excluded. Learning progression remains direct lesson completion only, Queue ordering remains canonical, unknown delivery/perspective data stays neutral, and canonical note text is never rewritten by distillation. Migrations `0058`–`0060` are applied in production and Worker version `34b908c5-dff0-4a47-b942-588fb59bc54f` is deployed. All 264 runnable unit tests pass with two environment skips; TypeScript, production build, clean/idempotent migration rehearsal, Hermes verification across 62 migrations and 106 owned routes, full five-root/15-mode E2E including Android offline behavior, and `git diff --check` pass. Live API smoke checks pass for resurfacing, contradictions, frontier balance, Compass delivery context, Queue delivery projection, Notes, distillation, shell assets, and migration state. Production readiness remains `needs_attention` because the pre-existing FTS5 search-maintenance step fails with `recursively defined fts5 content table`, and Cloudflare D1 SQL export cannot back up databases containing FTS5 virtual tables; D1 Time Travel rollback bookmark `00000769-00000096-000050d0-bc6c7e810f027f992f8a3bb00800d342` was captured before migration.

## Current truth — 2026-08-23 (Map Review filter persistence, deployed)

Selecting a branch in Map → Review now preserves the active search and Area filters while updating the typed branch URL and dossier. The Review workspace stays mounted across branch-only route changes, while other route changes retain their existing remount behavior. The installed PWA shell cache advances to `learning-compass-shell-v26`. TypeScript, production build, 25 focused route/PWA tests, the Impeccable detector, diff checks, and the exact local and production browser regression pass. The full E2E run reached and passed this regression before an unrelated Book note-reader timeout; the full unit run has one unrelated semantic-relations assertion failure. Deployed as Worker version `af815765-50e4-497a-9db0-8ff58e6cb5af`; all seven functional release reads return HTTP 200. Aggregate `/health` remains blocked by the pre-existing stale maintenance receipt and missing recent verified full recovery backup.

## Current truth — 2026-08-23 (Scholar Workspace note reader, deployed)

The Scholar Workspace direction is implemented on the canonical note route. It combines section navigation, bilingual English/Arabic reading columns, source and knowledge-branch context, related-note and retention tools, and responsive single-column mobile reading while preserving editing and canonical API data. The reader starts at the workspace's left edge, uses its full available width, follows active system surface/action colors, uses standard secondary/primary controls, and omits the redundant back bar. Its contextual action now opens the owning lesson overview, Thread overview, Book dossier, or Library source record/inspector; only unowned notes fall back to the external source. The superseded prototype and review captures are removed. TypeScript, production build, note-reader/router tests, a three-case route matrix, authenticated browser navigation, and live API readback pass. Bounded note-read fallbacks handle live migration-history mismatches without mutating canonical data. Deployed as Worker version `786fd4c0-9ff9-4ad0-bf90-7bdb7e759386`.

## Current truth — 2026-08-23 (preferred Original source URL, deployed)

One source can now replace the URL opened by **Original source** through `PATCH /recommendations/:id/source-url` without recreating the source or disturbing its Thread, branch, companions, or NotebookLM ownership. The route normalizes the replacement, rejects a dedup collision, updates source identity, and preserves the first former URL as `source_metadata_json.archive_source_url`. The first ten Riyadh al-Salihin lessons now point to their duration-verified exact old YouTube uploads while retaining the official `al-badr.net` archive locators. Verified with focused route and capability tests, all 240 unit tests with two environment skips, TypeScript, production build, full E2E, guarded mutation receipts, exact source-record and Thread readbacks, and a production browser traversal confirming all ten rendered **Original source** hrefs. Deployed as Worker version `7d127924-a8d7-46e8-a00b-6f5e72c8bc55`.

## Current truth — 2026-08-23 (consumed companion revisions, deployed)

Atomic Lite Visual publication now accepts non-deleted active or consumed source targets. A completed lesson can therefore receive a hash-bound HTML/PDF revision without reopening its source state, while rejected or deleted targets remain blocked. This repairs the production `recommendation_not_found` failure observed when revising the consumed first lesson of the Riyadh al-Salihin Thread. The Lite Visual runner also reuses an unchanged passing local pair instead of reprinting nondeterministic PDF bytes under the same pair ID; exact live replay now returns `reused:true`. Verified with focused regression tests, all 238 unit tests with two environment skips, TypeScript, production build, full E2E, targeted diff checks, live health, and exact source-record/Thread readback. Deployed as Worker version `c7ed2e9b-201d-40e4-a9dd-7aaa95dc99d1`.

## Current truth — 2026-08-23 (direct Thread progression, deployed)

Thread progression is now only direct lesson completion. Completing the final lesson automatically completes its Level and unlocks the next; completing every Level automatically completes the Thread. Optional projects, sources, notes, recall, and legacy path items cannot gate or advance progression. The old Level and Thread completion actions, waiting states, progress counters, UI copy, mutation routes, agent context fields, and dormant styles are removed. Migrations `0056` and `0057` recompute legacy Levels and Threads from lesson status and clear legacy path-item gating fields; Behavioral Psychology now reads `completed` with all 4 Levels and 12 lessons complete. The installed PWA shell cache is `learning-compass-shell-v25`. Verified with 237 unit tests (two environment skips), TypeScript, production build, clean/idempotent migration rehearsal, the 27-skill Hermes contract, full E2E, `git diff --check`, no pending remote migrations, retired-route `404` checks, six required live API reads, and exact live Thread readback. Deployed as Worker version `ec2b2ceb-0827-4b17-9f20-d52a19de016a`; rollback baseline is `aac63da4-aab3-4940-93c9-680ebc74abf9`. Aggregate `/health` remains degraded by pre-existing operational-readiness debt while `/health/live` and functional checks pass.

## Current truth — 2026-08-23 (Home rotation grid restored, deployed)

Current Rotation is again a normal Home grid card beside Queue and RSS Feeds instead of spanning the full row. Desktop renders three equal-width 338px panels in one row; mobile stacks the same panels in reading order. The one-current-turn-per-Thread behavior is unchanged. Verified with 237 unit tests with two environment skips, TypeScript, production build, full E2E, migration rehearsal, the 27-skill Hermes contract, a clean Impeccable layout scan, `git diff --check`, and live desktop/mobile browser measurements with zero overflow. Deployed as Worker version `aac63da4-aab3-4940-93c9-680ebc74abf9`; rollback is `2d6071f9-4f80-4526-8478-f79e236a8d8e`.

## Current truth — 2026-08-23 (Archive navigation label, deployed)

Library now names the completed-and-excluded mode `Archive` instead of `Catalog` in both the canonical route registry and workspace switcher. The compatibility URL remains `mode=catalog`; visible navigation, active mode, and page heading all read `Archive`. The PWA shell cache advances to `learning-compass-shell-v24`. Verified with 237 unit tests (two environment skips), TypeScript, production build, the 27-skill Hermes contract, full E2E, `git diff --check`, and a live browser readback showing `Books / Triage / Archive / Files`, active `Archive`, and 85 records. The label is live in current Worker version `aac63da4-aab3-4940-93c9-680ebc74abf9`.

## Current truth — 2026-08-23 (Archive pagination repair, deployed)

Library → Archive now requests completed and excluded records before the 200-row pagination limit. Large active source imports can no longer crowd archived history out of the response and produce a false empty state. Production currently renders all 85 archived manual records: 61 completed and 24 excluded. The installed PWA shell cache advances to `learning-compass-shell-v23`. Verified with 237 unit tests (two environment skips), TypeScript, production build, clean/idempotent migration rehearsal, no pending remote migrations, the 27-skill Hermes contract, full E2E, `git diff --check`, required live endpoint reads, and a production browser readback of 85 rows with no empty state. Deployed as Worker version `2d6071f9-4f80-4526-8478-f79e236a8d8e`; rollback is `6d944162-7207-4b7c-8062-5e6553a737ef`. Aggregate `/health` remains degraded by the pre-existing operational-readiness debt while `/health/live` and functional checks pass.

## Current truth — 2026-08-23 (one current turn per Thread on Home, deployed)

Home now shows one rotation list with exactly one current turn from every active, paused, or planned Thread. Each row independently resolves that Thread's in-progress lesson or first unfinished lesson; a Level with no unfinished lesson is complete. Production currently shows Riyadh al-Salihin's current lesson and Systems Thinking's `What systems thinking is`; consecutive Systems lessons such as `Bottlenecks` and `Feedback loops` no longer occupy the other Thread slots. The compact payload avoids loading complete curricula. Verified with 236 unit tests with two environment skips, TypeScript, production build, full local E2E, and `git diff --check`. The production browser renders three direct rows with zero overflow. Deployed as Worker version `6d944162-7207-4b7c-8062-5e6553a737ef`; rollback is `ddd2709f-8bb0-4ba5-a02c-dce33a4acd77`.

## Current truth — 2026-08-23 (books excluded from Queue, deployed)

Books and book chapters never appear in Queue or the Home active-Queue shelf. Personal `saved|reading|finished` state, the explicitly pinned reading desk, progress, chapters, and companions remain unchanged in Library → Books. The release passed 233 unit tests with two environment skips, TypeScript, production build, full E2E, migration rehearsal, Hermes contract verification, and `git diff --check`. Live Queue and Home readbacks contain only the Holmström video while `Predictably Irrational` remains `reading`, 62% complete, and Queue-excluded. Deployed as Worker version `ddd2709f-8bb0-4ba5-a02c-dce33a4acd77`; rollback is `29b763e4-3c57-41d7-bbd5-9c1796ef7304`.

## Current truth — 2026-08-23 (Home Active Thread lesson turns, deployed)

Home now reads the active Thread path and shows exactly three current lesson turns: the in-progress or first unfinished lesson followed by the next two lessons in its active Level. Each row preserves its Level/Lesson position, source cue, status, and direct lesson route. The Thread-path hook now runs consistently across loading and loaded renders. The release passed 238 unit tests with two environment skips, TypeScript, production build, full E2E, migration rehearsal, remote migration readback, Hermes contract verification across 27 synchronized skills, the Impeccable detector, and `git diff --check`. Live browser verification rendered `What systems thinking is`, `Bottlenecks`, and `Feedback loops` with correct lesson links and zero overflow. Deployed as Worker version `29b763e4-3c57-41d7-bbd5-9c1796ef7304`; rollback is `e956ad76-7ce3-420f-9062-5f4265a70966`. Production liveness and functional smokes pass; canonical readiness remains blocked by the pre-existing recovery-backup and maintenance debt.

## Current truth — 2026-08-23 (Book Files responsive repair, local)

Focused Book Files rows now stack filenames above chapter and size metadata instead of forcing both fields into one horizontal row. Long names and enlarged text wrap within the Files panel without clipping or horizontal overflow. Source anchors and Feedback & outcome now remain in the same Book overview but start collapsed and reveal their existing controls on demand. These repairs are local and not deployed.

## Current truth — 2026-08-23 (archived Atlas filtering, deployed)

Atlas now excludes every canonical `pruned` node, all descendants of a pruned parent, and every edge touching those hidden nodes. Paused (`held`) branches remain visible. The fix was verified against the live 205-node graph: `linux-automation` is canonical `pruned`, is absent from the 193-node Atlas model and production branch selector, and no archived node leaks remain. Unit tests, TypeScript, production build, full E2E, liveness, and `git diff --check` pass. Deployed at `https://recommendations-worker.mhmudnasr30.workers.dev` as Worker version `e956ad76-7ce3-420f-9062-5f4265a70966`.

## Current truth — 2026-08-23 (unified Map Review Branch Dossier, deployed)

Map → Review now implements the selected Branch Dossier as one production surface instead of separate Branches and Balance tabs. A compact branch index, inline scope editor, branch ledger, and persistent live signals-and-decision rail combine branch status, priority, attention, coverage, retention, and filed records. The branch index was distilled after visual feedback: horizontal area chips became one labeled dropdown, default Active badges and list dates were removed, metadata uses plain interface type, names can wrap, selected rows use a quiet tonal fill, and the scrollbar follows the theme. Keep active, Make first priority, Pause branch, and Archive branch remain visible together; legacy Branches and Balance URLs recover to `#/map?mode=review`, and typed branch routes preserve the selected dossier. Unit tests, TypeScript, production build, focused desktop/mobile E2E, live API reads, enlarged-text checks, target sizing, and overflow checks pass. Deployed at `https://recommendations-worker.mhmudnasr30.workers.dev` as Worker version `f79acb7d-e9be-4376-9519-a6416811b0a8`.

## Current truth — 2026-08-23 (Books moved to Library, deployed)

Books is now the first Library mode and the default `#/library` surface, ahead of Triage/Queue. Learn contains Threads and Practice only. Existing Learn Books URLs and typed Book links recover to Library without changing the reading desk, My Books, Canon fields, personal reading state, or Queue boundaries. The responsive My Books toolbar keeps All, Reading, Saved, and Finished visible without horizontal scrolling while search absorbs available width and the regions stack before clipping. Books no longer overrides the shared local-navigation rail, so Library uses the same centered width, typography, active fill, underline, and truncation behavior across Books, Triage, Catalog, and Files and matches the other roots. The desktop root rail now scales from 152px to 180px with text preferences; it renders at 162px under the production preference set, with every Compass, utility, and root label visible and no page overflow. The release passed 232 unit tests with two environment skips, TypeScript, production build, full E2E, Hermes contract verification, desktop/mobile live checks, and `git diff --check`. Deployed as Worker version `b06bb251-82ab-4d28-ad5c-e3a4c580ec9f` with rollback version `055ec5c5-1283-4676-a585-2edefe08e660`. Production liveness is healthy; canonical readiness remains blocked by the pre-existing missing recovery backup and failed maintenance receipt while storage, integrity, assets, and jobs remain healthy.

## Current truth — 2026-08-23 (Atlas motion and fluidity, deployed)

Atlas now reveals a newly opened or expanded constellation in one hierarchy-aware sequence: domain anchors, branches, topics, then links. Pointer hover and selection transition through the graph renderer, selection glides the camera toward the chosen node, zoom/fit motion uses longer natural deceleration, and the force simulation settles with softer damping instead of snapping. A slow GPU-only canvas light drift keeps the map from feeling inert without running continuous graph physics. The complete spatial layer, camera movement, entrance sequence, and ambient drift are removed when reduced motion is active while interaction feedback remains visible.

Focused Atlas tests, TypeScript, production build, full E2E, detector checks, Hermes contract verification, migration rehearsal, and browser passes at desktop, 390px phone, and reduced-motion modes pass. The live browser pass verifies completed entrances, hover state, camera movement, zero runtime errors, and no spatial motion under reduced motion. The full suite passes 232 tests with two environment skips. Worker version `e8320fb4-345b-4388-b2a8-fb298554b795` is deployed; `/health/live` is healthy and the stored, resolved, and default Atlas presets match exactly.

## Current truth — 2026-08-23 (branch-first My Books, deployed)

My Books combines the selected Branch-first Library and Reading Bands directions. Verified branches are the outer groups, the current book's branch leads, and each branch separates visible titles into Reading now, Saved for later, and Finished bands. The initial bounded reveal keeps up to four titles from the leading branch before sampling later branches, so branch structure is visible without rendering the entire collection. Search, state filters, incremental loading, branch pills, current-book controls, the separate Canon surface, and 44px mobile targets are preserved. The capped search toolbar and disclosed Branch/Canon filters are deployed with the Library move in Worker version `c777f37b-e32b-49a4-b06f-105f31bab15a`.

## Current truth — 2026-08-23 (cross-workspace frontend polish, deployed)

The Botanical Folio client now restores cypress-filled primary actions, uses one workspace masthead rhythm, labels the compact desktop rail, wraps Learn filters without clipping, enforces 44px mobile controls, progressively discloses secondary Map and Preferences controls, presents a direct first-book empty state, uses a restrained Atlas palette, and dismisses legacy-route recovery notices after their announcement. Broad `transition: all` declarations were replaced with explicit paint/transform properties. The focused Book dossier aligns its domain/context rail, Reading status label, and select to one full-width desktop edge while preserving the mobile stack. Its linked notes now stay in one closed ledger instead of rendering raw bodies; each title opens the canonical formatted Notes reader while source anchors remain in place. The release passed 233 unit tests with two environment skips, TypeScript, production build, migration rehearsal, Hermes contract verification, full E2E, and `git diff --check`. Deployed as Worker version `e331dc00-87d1-4031-aef7-fff7879dae40` with rollback version `cba019a3-c3f8-444d-b381-d7b95404d540`. Live browser verification on `Predictably Irrational` confirmed equal domain/select alignment, a closed eight-note ledger with no raw front matter, Chapter 8 routed to its canonical reader with six rendered headings and six blockquotes, and zero overflow. `/health/live` and all required application reads are healthy; aggregate readiness remains blocked by stale/absent maintenance success and no recent verified full recovery backup.

## Current truth — 2026-08-23 (Atlas interaction and spatial-engine repair, deployed)

Map → Atlas now keeps one incremental Cytoscape canvas with deterministic domain placement, label-aware collision separation, live force preferences, stable debounced persistence, accurate camera minimap framing and recentering, responsive resize handling, and reduced-motion-safe simulation. Direct node taps again select and expand nodes after repairing an event-unbinding regression; selection ancestry, accessible list navigation, keyboard tabs, depth controls, fullscreen, export, and viewport controls remain available without restoring the intrusive inspector or cluster hulls. Domain lookup is now constant-time, category anchors share their canonical domain cluster, synthetic round depth aliases are removed from the Atlas model, and duplicate endpoint pairs retain relation-safe edge identities. The verified production composition is now also the canonical Atlas preset and reset state: `0.05` text fade, `0.58` node scale, `4` relationship-link weight, `2.75` branch-link weight, `0.83` center force, `11` repel force, and `2.75` link force.

Desktop and 390px real-browser checks against the live 204-node/190-link production projection pass with direct canvas selection, visible mobile viewport controls, zero horizontal overflow, and zero console or page errors. The release passed 230 unit tests with two environment skips, TypeScript, production build, migration rehearsal, full E2E, Hermes contract verification, focused live endpoint checks, and final diff checks. Remote migration `0054_production_operations.sql` is applied and Worker version `5b5d1c2b-e047-4bfb-bd7b-392aa6353d36` is deployed. Aggregate health remains intentionally degraded by four pre-existing overdue retries, missing maintenance success, and no recent verified recovery backup; storage and integrity checks are healthy.

## Current truth — 2026-08-23 (large Learning Thread path scaling)

The Thread path reader now batches artifact and NotebookLM receipt lookups below the D1 bind-variable limit. The real 294-recording `شرح رياض الصالحين — الشيخ عبد الرزاق البدر` draft therefore opens as one complete path with 183 Levels, 294 lessons, and 294 exact primary source attachments instead of returning HTTP 500. Regression coverage fixes the batch size at 75 and proves all 294 identifiers are preserved without duplication or loss. The release passed 225 unit tests with two environment skips, TypeScript, production build, migration rehearsal, full E2E, Hermes contract verification, remote-D1 replay, and exact production readback. Deployed as Worker version `926fca19-a3d2-4770-bc8b-a6968aa2b422` with rollback version `c5ccf235-6448-43f5-a49c-bb59148627fa`. The Thread path is live and complete; global readiness remains degraded by pre-existing integrity, retry, maintenance, and recovery-backup blockers.

## Current truth — 2026-08-23 (Clean knowledge branch taxonomy and round retirement)

Restructured knowledge branches across the entire site, database, API, client views, test suites, and Hermes skills/rules:

- Retired synthetic `R1`, `R2`, `R3` round numbering and progression guesswork in favor of a clean, production-grade 3-tier domain taxonomy: Super Category (Domain) → Knowledge Branch (`tree_nodes`) → Topics/Leaves (`meta_json.leaves`), with explicit priority (`priority_rank` 1–5) and status (`love` / `active` / `held` / `pruned`).
- Added migration `0053_retire_synthetic_rounds.sql` clearing synthetic `round_label` from `tree_nodes` and `round` from `recommendations`.
- Updated client UI (BranchDeckPage, BooksView, LibraryViews, LearnThreadView, Inspector, studio.css) to eliminate `[R1]`/`[R2]` round tags and render clean Domain / Branch / Priority badges everywhere.
- Updated Atlas Cytoscape graph model to cluster directly by domain and branch hierarchy rather than round rings.
- Synchronized Hermes skill files (`~/.hermes/skills/`), `AGENTS.md` rules, and worker documentation (`docs/API.md`, `docs/architecture.md`, `docs/books-api.md`, `docs/hermes-contract.json`).
- Verified with 219 passing unit tests, TypeScript typecheck, production build, full E2E across five roots and 16 internal mode states, and Hermes contract verification.

## Current truth — 2026-08-23 (Books room visual refinement, deployed)

Learn → Books now carries one cohesive reading-room treatment across its mode control, Books introduction, pinned reading desk, chapter disclosure, searchable My Books ledger, add form, empty states, integrated Canon field surface, and focused Book hub. The focused route is a composed folio with reading typography, status and knowledge-context chips, a clear reading-state row, next-chapter panel, collapsed chapter ledger, overview metadata, notes and source anchors, recall, connections, reading history, files, and feedback. Every section reads the canonical book record and renders a truthful empty state when no related data exists. The continuous Books-room structure, routes, pinned-book behavior, and closed chapter milestone disclosure are unchanged. The integrated Canon header and filter toolbar remain visible, and the mobile composition retains 44px controls with no meaningful horizontal overflow. The grouped My Books experiment is reverted locally; ten isolated HTML directions are available from the local dev server at `/prototypes/my-books-variations.html` for selection before another production redesign.

The current production release remains Worker version `926fca19-a3d2-4770-bc8b-a6968aa2b422`, which preserves the verified 294-lesson Thread fix and still contains the grouped My Books experiment inherited from `c5ccf235-6448-43f5-a49c-bb59148627fa`. The restored old list and ten-option prototype pass TypeScript, build, E2E, focused Books tests, desktop/mobile browser checks, and diff checks locally. Deployment is intentionally blocked until the concurrent production-operations release resolves its stale Android PWA assertion, Hermes migration inventory for `0054`, and pending remote migration.

## Current truth — 2026-08-22 (Books room interaction redesign, deployed)

Learn → Books remains one room with one explicitly pinned current book and no Shelf/Canon tabs. The dominant reading desk now leads into a visible, searchable, state-filterable My Books ledger with bounded incremental expansion, followed by a visible Canon discovery surface with search, family filtering, and explicit Foundation/Representative/Boundary title mappings. Finished books report completion truthfully, chapter disclosure motion communicates state, decorative page-load motion is absent, custom themes preserve text/action contrast and visible seams, and mobile Books controls use 44×44px minimum targets at 200% text.

Verified with 215 passing unit tests and two environment-skipped checks, TypeScript, a 102.18 KB gzip base-client build, full E2E across five roots and 16 internal mode states, clean/idempotent rehearsal of all 54 migrations, Hermes contract verification across 27 synchronized checks and 95 owned routes, Compass bridge verification, and `git diff --check`. Deployed as Worker version `18e6e512-f9a6-4334-b7b0-fead6dc0e084` with rollback version `d51825f7-9bb1-4a7d-b227-0d0feeda510a`. Required production reads returned HTTP 200 with valid inputs; the root retained no-store; live Canon returned explicit role mappings; and live Books passed at 1440px and 390px with zero horizontal overflow, one active mode treatment, resolved theme seams, a 46px search control, and 21:1 primary-action contrast under the saved custom dark theme.

## Current truth — 2026-08-22 (Reading Fold and focused Book overview, deployed)

Learn → Books uses one Reading Fold: one explicitly pinned current book owns the dominant reading desk, progress, next chapter, and available passive HTML/PDF/NotebookLM formats. My Books is the same room’s visible searchable, state-filterable incremental ledger, while Canon is a visible first-class discovery section with search, family filtering, and Foundation/Representative/Boundary field roles; neither becomes a Shelf/Canon tab or second collection. Pinning another title replaces the desk without changing other reading states or Queue commitment. The focused Book overview keeps status, author, verified branch/round, Canon and Thread context, progress, next chapter, and an accessible chapter disclosure while leaving deeper records in canonical storage. The Thread command center presents direct lesson progress and Practice without a separate progression gate.

Verified with 209 passing unit tests and two environment-skipped checks, TypeScript, a 99.25 KB gzip base-client build, full E2E across desktop/mobile routes, clean idempotent rehearsal of all 54 migrations, Hermes contract verification across 27 synchronized checks and 95 owned routes, and `git diff --check`. Deployed as Worker version `269cb51d-26e1-48b2-8b6a-dd80775395da` with rollback version `8778477c-29d5-4c0e-9386-787cda9c54c9`. Twenty-three production API/document reads returned HTTP 200; the root retained no-store; live Books passed at 1440px and 390px with five mobile roots, zero browser errors, and zero horizontal overflow.

## Current truth — 2026-08-22 (Archive-only Catalog, deployed)

Library → Catalog now resolves directly to Archive. All sources, Reading journal, and Collections are retired from the route registry, client workspace, typed object model, Books links, Android Capture return path, Worker route inventory, and agent capabilities. Legacy section URLs recover to Archive. Captured-source storage remains intact so removing the surface does not delete or orphan user data. Verified with 208 unit tests, TypeScript, production build, full E2E across 16 mode states, Hermes synchronization, idempotent migration rehearsal, and `git diff --check`. Deployed as Worker version `17ad8dd4-b651-41c8-8cd8-cc64567f85f9` with rollback version `81af49ed-4194-477c-8d13-4ab3a172db19`; live smoke checks confirm Archive copy in the shipped bundle, no retired section labels, HTTP 404 for `/hardcover` and `/collections`, and HTTP 200 across the required operational endpoints.

## Current truth — 2026-08-22 (compact Books and Canon surface, deployed)

The Books → Canon surface no longer renders the three-role explainer strip, duplicate page-level descriptions, the empty Coming next coverage block, or the Selection Rubric / Curatorial Standard footer. Ready field coverage remains collapsed by default, role labels remain on actual Canon book slots, and Books uses the full canvas width with desktop edge alignment while preserving mobile gutters. Book rationale is clamped to two lines and the reading-desk/ledger spacing is compact. TypeScript and production build passed, and live health returned 200. Deployed as Worker version `81af49ed-4194-477c-8d13-4ab3a172db19`.

## Current truth — 2026-08-22 (Books and Threads connection, local)

Learn → Books now projects every active book-to-Thread source membership into the Reading desk and My books ledger. Each connected Thread is a direct typed link, and book search matches Thread title or source role; the authoritative book dossier continues to show the fuller role/status context. The API returns `threads[]` alongside Canon memberships without per-book reads. Verified with 213 unit tests, TypeScript, production build, full E2E, Hermes contract synchronization, clean/idempotent migration rehearsal, the real browser book/Thread fixture and link assertion, and `git diff --check`. Deployed as Worker version `557e1477-82b1-4a08-98d2-30a38972655e`; all 22 live release reads returned HTTP 200, all 37 production books expose the `threads[]` contract, and the deployed JS/CSS contain the Thread navigation and styling.

## Current truth — 2026-08-22 (Books section navigation repair, local)

Learn → Books section controls now scroll to Reading desk, My books, and Canon fields without replacing the hash-routed Books URL. The E2E contract waits for the API-backed Canon section, verifies every section control preserves `#/learn?mode=canon`, and confirms its target is revealed. Verified with 212 unit tests, TypeScript, production build, full E2E, Hermes contract and migration rehearsal, live health/Books/Canon/System reads, the deployed asset contents, and `git diff --check`; deployed as Worker version `dc09eaa6-170c-4ae6-aeaf-c882de0d6101`.

## Current truth — 2026-08-22 (Books projection and reading-room repair, local)

Learn → Books is again one continuous workspace: a next-chapter Reading desk, an incremental 12-row My books ledger, then integrated Ready to explore / Coming next Canon fields. Book and dossier access remains passive; personal Saved / Reading now / Finished controls do not change Queue, and only Queue owns tracked Start/Resume sessions. Add Book now asks for title, author, and branch first, with ISBN, original URL, and reason under progressive disclosure.

The list and typed book record now use one canonical companion projection for personal/Queue state, ordered chapters, newest deterministic companion links, normalized QA, progress, and next chapter. Books GET performs no writes, excludes deleted/non-book rows, fans out independent reads, and omits the legacy synthetic whole-book Chapter 0. Explicit completion can safely materialize an artifact-only chapter; registration cannot recreate the placeholder. Verified branches stay linkable while unresolved legacy labels are visibly unverified. Duplicate Add Book preserves explicit reading state, whose metadata value is authoritative over compatibility fallbacks. Neutral dequeue is limited to active Queue commitments. Idempotent migration `0051_remove_synthetic_book_chapters.sql` is applied remotely and removes only the known synthetic rows. This work shipped in Worker version `dc09eaa6-170c-4ae6-aeaf-c882de0d6101`.

## Current truth — 2026-08-22 (Queue-independent book reading state)

Books now persist a personal `saved|reading|finished` state independently from Queue commitment. The Reading desk and My books filters use that state, while neutral `dequeue` removes an item from Queue without recording the negative outcome produced by `exclude`. Canon membership, branch/round, chapter companions, and reading completion remain unchanged. Verified with 200 unit tests plus TypeScript, production build, Hermes and agent contracts, full E2E, live capability/readback checks, and `git diff --check`; deployed as Worker version `52684b94-df32-46b3-b2ac-e50dc719c34e`.

## Superseding operational contract — 2026-08-22

Hermes and the canonical documentation are synchronized to the shipped Library model: captures are `captured` records in Library → All sources, Queue is a separate five-item commitment shelf, and every captured/recommended/queued item requires a verified non-pruned branch plus round with a visible pill. Threads and Levels advance only through learner-confirmed direct lesson completion; projects, sources, notes, ratings, dispositions, recall, and provider receipts never gate or advance progression. Recommendation targets come from `/compass/context`, while `/agent/context.learning_gaps` remains a placeholder. Local agent contract `2026-08-22` adds atomic branch-aware ordinary capture and removes retired progression-gate mutations from Hermes discovery; the bridge retains a one-read compatibility repair until that Worker change is deployed. The default Hermes installation and the real `compass` profile are maintained as byte-identical active skill trees. Historical release entries below describe earlier snapshots and are not operating instructions.

## Current truth — 2026-08-22 (one Books room and complete book dossier)

Books is now one continuous Learn workspace with no Shelf/Canon subordinate switch. A single Books header leads into the reading desk, shared search and reading-state controls, integrated Canon field guides, and My books. Canon remains a distinct evergreen data model, but field cards provide path context instead of duplicating titles as a second book collection. Explicit capture projects `canon_memberships[]` onto the same personal book identity, so its Canon domain/role and personal reading state render together. Old Shelf, Atlas, Library Books, and typed book URLs recover to `#/learn?mode=canon` or the corresponding typed dossier without becoming not-found routes. Incomplete Coming next fields remain non-actionable, surprise discovery remains limited to ready trios, and Thread creation still requires a captured selection.

The typed book route is now an authoritative dossier rather than a generic object stack. Its editorial identity header combines lifecycle, full branch/round, Canon placement, rationale, chapter progress, next companion, original access, Queue handoff, and chapter editing. Anchored sections keep overview, chapters, notes and evidence, structured feedback, recall, branch/Thread/Unit connections, session history, and files visible on one responsive page. The client now preserves the complete `/capture/:id/record` response instead of discarding notes, recall, Threads, sessions, and annotations before render. Queue remains the only owner of tracked Start/Resume; all original and companion links are explicitly passive.

Manual `POST /recommendations/books` intake still requires an existing non-pruned branch, derives its round, and atomically persists the captured recommendation plus canonical `recommendation_meta.branch_id`. Missing, unknown, and pruned branches are rejected, and activity-backed rounds remain available for valid legacy branches without an explicit `round_label`. The add form preserves entries after failure and exposes branch loading/error state. Verification passes all 200 unit tests plus TypeScript, the production build at 106.71 KB gzip for the base client bundle, the complete five-root/19-mode E2E suite including desktop/mobile dossier coverage and legacy URL recovery, clean/idempotent rehearsal of all 52 migrations, Hermes contract verification across 98 owned routes, and `git diff --check`. Released as Worker version `a3bec3a9-1a51-4fe1-af80-cf8d4e653199`; rollback is `1ee941cd-fb90-426a-a9c9-7fbad9371285`. The required live API set returned its expected responses, Books projected 30 Canon-linked identities, a real Canon book record preserved its membership, the public learning-materials folio retained one unique Learn target and its restrictive content policy, and real-browser Books/dossier checks at desktop and 360px found zero overflow or error states with a ready service worker.

## Repository hygiene — 2026-08-22

The repository no longer carries the retired UI mockups, dated implementation plans and audits, one-off curriculum and Visual Lite batch scripts, old rendered PDF inspection pages, or the dead Discovery client and Gemini recall generator. The canonical product/design/architecture documents, active analytics and recovery tooling, migrations, browser extension, Worker APIs, and local D1 state remain intact. A runtime dependency audit reports 103/103 source files reachable, and all remaining local Markdown links resolve. Verification passes with 198 unit tests plus TypeScript, production build, all 52 migrations applied twice cleanly, Hermes contract checks, the full five-root/20-mode E2E suite, and `git diff --check`.

## Current truth — 2026-08-22 (structured feedback ledger)

Feedback now uses one structured contract across ordinary source returns and Compass outcomes: exact reflection, `completed|in_progress|stopped`, canonical reason tags, expectation versus result, effort, minutes, score, and separate `retain|apply|reference|drop|undecided` disposition. Stopped feedback requires a reason; continue-later with `not_now` remains neutral. Every ordinary revision is retained in the append-only learning-event ledger, while every Compass event retains its pick, branch, round, lane, Thread, target lesson, engine, candidate count, and observational exposure. `/feedback/context` returns the complete revision history, the source dossier exposes parsed Compass events, and `/analytics/hermes` aggregates Compass outcomes by reason and lane. Feedback continues to create analysis proposals or no-change decisions only; it never creates or starts another recommendation.

Library source dossiers now include an accessible three-state feedback panel with state-specific reason chips, separate score/disposition controls, exact reflection, optional expectation/result/effort fields, and a durable visible receipt. The responsive layout follows the existing Botanical Folio system and keeps the expectation-to-result seam readable on phone widths. Migration `0050_structured_compass_feedback.sql` adds the structured Compass payload and disposition columns without replacing historical rows. Verified locally with 199 unit tests, TypeScript, clean/idempotent rehearsal of all 52 migrations, Hermes contract verification across 92 owned routes, the expanded ordinary/Compass feedback integration flow, production build (98.84 KB gzip base client bundle), the full five-root/20-mode E2E suite including a real neutral receipt submission and canonical readback, a 390px Playwright visual/overflow check, and `git diff --check`. Released as Worker version `b30b7576-ef57-4b6a-90d8-bab1101531af`; rollback version is `55aa7c6e-5263-4824-80ea-b20c978432f4`. Remote migrations `0048`–`0050` are applied with no pending migrations; `0048` converted the one remaining obsolete capture state to canonical `captured`. Live health, capability, feedback-context, Hermes analytics, and OpenAPI checks passed. A production source dossier rendered the feedback panel, all three outcomes, and branch round `R1` at 390px with no horizontal overflow or browser errors.

## Current truth — 2026-08-21 (recommendation target and source verification repair)

Compass now derives recommendation targets from actionable missing-material lessons in the active Thread instead of the retired Thread-wide evidence model. `GET /compass/context` exposes those lessons as `recommendation_target_gaps`; candidates may name an exact `target_lesson_id`, and starting the winner atomically attaches it as the lesson's replacement-safe primary source without changing lesson progress. Exact targets bypass only their own Thread's coverage gate while all other Thread coverage remains excluded.

Candidate and evidence URLs now share one public-only verifier: credentials, local/private/reserved addresses, unsafe redirects, and redirect loops are rejected; every redirect destination is revalidated; unknown reachability is ineligible; access-restricted sources remain explicitly classified. Candidate evidence is bounded to four claims and each request to 40 unique URLs, with per-claim verification receipts persisted beside the candidate. Decision confidence is labeled `heuristic_uncalibrated`, and feedback exposure is canonical observational metadata for the single displayed winner rather than a claimed position-bias correction. Rollout and adaptation gates are unchanged; multilingual semantic retrieval remains advisory rather than controlling hard exclusions.

Verified with all 197 unit tests plus TypeScript, production build at 96.62 KB gzip for the base client bundle, Hermes contract verification (51 migrations, 25 synchronized checks, 92 owned routes), synchronized canonical/Compass Taste Rec skills, clean/idempotent migration rehearsal, the full route/responsive E2E suite, the taste-rec/branch-deck/compass-feedback integration flows, and `git diff --check`. The stale compass-feedback integration expectation was corrected to the canonical `captured` state. Deployed as Worker version `55aa7c6e-5263-4824-80ea-b20c978432f4`; rollback version is `ddc8712e-528a-4941-99fd-1e35169d85cc`. Twenty release endpoints returned HTTP 200 live, `/analytics/hermes/repair` dry-run and the public folio headers passed, and a live active Thread resolved `/compass/context` with the new `recommendation_target_gaps` contract and empty gap set.

## Current truth — 2026-08-21 (additive optional lesson sources)

Lesson source attachment keeps `primary`, `case`, `challenge`, and `reference` as replacement-safe single slots, while `optional` is additive so a lesson can retain several distinct non-required extensions. Every optional source still requires a verified branch and remains visible with its role badge.

## Current truth — 2026-08-21 (learner-first Canon)

Canon is now organized around the learner’s decision rather than the internal curation pipeline. The atlas leads with family browsing and field/book search, separates complete **Ready to explore** trios from non-actionable **Coming next** coverage, hides detailed counts and selection rules behind disclosure, and offers surprise discovery only when a complete trio exists. Ready field pages present Foundation, Representative, and Boundary as one vertical reading path with clear book actions, progressively disclosed selection notes, a compact comparison table, and one finite Thread action. Unfinished fields render a single honest preparation state instead of three empty dossiers.

Thread creation is guarded at the API boundary: the field must retain a verified non-pruned branch, contain exactly three distinct approved roles, be marked complete, and have at least one selected book already captured. Empty and unfinished domains return HTTP 409. The client mirrors that rule and keeps Thread creation disabled until a captured book exists. The PWA shell cache advances to `learning-compass-shell-v17` so an installed Android client cannot retain the superseded Canon surface. Released with Worker version `29f733b3-7b2e-41bb-922c-e7c7f0da72ef`; rollback is `799d2624-90ab-43ac-b372-02eeab8c2903`. The release passed 189 unit tests plus TypeScript, production build at 92.80 KB gzip for the base client bundle, clean/idempotent migration rehearsal with no remote migrations pending, full E2E across the five roots/21 internal modes/typed Canon routes/mobile shell, Compass bridge, Hermes contract verification, `git diff --check`, and all required live smokes. Production Canon returns ten branch-connected pilot domains; its atlas, a real Psychology domain, the v17 service worker, and the live bundle's learner-facing ready/coming states all passed direct readback.

## Current truth — 2026-08-21 (AI-ready source acquisition)

Visual Lite now has one canonical acquisition command for every supported source: `extract_source.py`. It routes public articles through Mozilla Readability with a bounded script-rendered fallback, YouTube through published captions then `yt-dlp` subtitles then local Faster-Whisper, PDFs through PyMuPDF with Arabic/English Tesseract OCR only for near-empty pages, EPUBs through exact OPF spine order, office documents through Pandoc, and text/uploads through direct UTF-8 extraction. The output is always immutable `source.txt` plus a `lite-visual-source-extraction/v1` manifest containing source identity, method, cache key, content hash, counts, timings, warnings, and a truthful `complete|blocked` state. Paywalls, shallow bodies, truncated articles, encrypted PDFs, and incomplete books block authoring instead of being reconstructed or padded.

The cache is keyed by source identity, extractor version, language/timestamp options, and local file state; it verifies the stored content hash before reuse. Only this cache/local fast path is described as millisecond retrieval. First network reads, browser rendering, subtitle discovery, and speech transcription report their actual elapsed time. Visual Lite jobs now advertise the source command and exact checkpoint requirements, and the Worker rejects stage advancement unless the previous stage supplies schema-valid evidence: extraction receipt/hash/cache fields, gapless source-scope hashes, HTML/PDF hashes, validation receipt, atomic pair ID, and two distinct artifact IDs.

Verified with `npm test` (187 unit tests plus TypeScript), `npm run verify:hermes` (49 migrations, 25 synchronized checks, 92 owned routes), clean/idempotent migration rehearsal, the guarded Compass bridge handshake, Node/Python syntax checks, a production build at 91.59 KB gzip for the base client bundle, the complete route/responsive E2E suite, byte-identical canonical and Compass-profile source tools, and `git diff --check`. The extraction regression suite observes a cache hit below 100 ms and covers complete article parsing, paywall blocking, every-page PDF extraction, transcript cleanup, and invalid checkpoint rejection. A live 3,322-word YouTube manual transcript completed through the canonical router in 1.00 seconds; its verified second read completed from cache in 0.45 ms. Production deployment completed as Worker version `9e58ae7a-0ec5-40b5-82c0-c910f58fe329`; rollback version is `e7e7be4a-11fe-4d0b-9a5f-ac226c499962`. Twenty-two required live endpoints returned HTTP 200, the public update folio preserved one unique Learn target and zero phone overflow, desktop/phone app checks found all five roots with a ready controlling service worker and zero browser errors, and an existing 14,802-character HTML companion reloaded offline with an exact SHA-256 match before the offline Home shell recovered without overflow.

## Current truth — 2026-08-21 (Visual Lite linear code-only workflow)

Visual Lite v4 is one resumable source-to-companion line: resolve source, extract and checksum `source.txt`, partition every normalized source word in `source-scope.json`, author one fresh Arabic semantic HTML article after Intent and Frontend Design reasoning, print that exact HTML to A4 PDF, validate, atomically publish, and verify the canonical source record. The HTML is now the only canonical reading body; the duplicate canonical JSON/prose layer is removed.

At that release point, new publications used `lite-visual-validation/v5`; the active replacement contract is now signed `lite-visual-validation/v6`. A `lite-visual-coverage-ledger/v1` binds exact source anchors to exact visible HTML anchors, covers every scope, and preserves every declared numbered source item. `build_source_scope.py` creates the gapless authoring skeleton; `run_workflow.py` automates extraction, rendering, validation, atomic publication, record verification, and optional leased-job claim, heartbeat, checkpoints, and completion.

This contract is live in Worker version `799d2624-90ab-43ac-b372-02eeab8c2903`. The first v5 production pair, `lv-cap_1787259550326_5e11f8-dbf06a86-6f2e1e12`, passed 25 exact claim mappings across all 15 numbered source notices, six source scopes, 360/768/1280px geometry, 200% text, and six A4 pages; the source record exposes its HTML and PDF as one ready revision-2 companion.

The workflow no longer delegates to Visual Mind or requests AGY/Antigravity/ImageGen. Templates, style locks, stored palettes, preset themes/layouts, mind maps, raster/generated images, external assets, scripts, widgets, quizzes, and mandatory pauses are forbidden. Prose, native semantic structures, equations, and rare justified accessible inline SVG are the complete rendering vocabulary. The deterministic validator checks gapless source coverage, Arabic RTL semantics, HTML accessibility, responsive geometry at 360/768/1280px, 200% text resizing, A4 size and readable text on every PDF page, and HTML/PDF text parity; it emits one hash-bound receipt without screenshots, vision review, or a subjective score.

`POST /artifacts/pairs` is the only Lite Visual publication boundary. It verifies the receipt and file hashes, stores both objects, commits both artifact rows in one D1 batch, cleans staged objects on failure, and makes only complete ready pairs selectable. Generic single-file Lite Visual uploads are rejected. Repeating the same pair and hashes is idempotent. Job checkpoints advance only through declared adjacent stages, completion is allowed only after `verify_record`, and a stale completed current-v4 job without a ready pair is requeued from its valid persisted `workflow_step`. A production replay exposed that a legacy v2 job could otherwise retain obsolete stages; the pending local repair upgrades legacy or malformed payloads to the full v4 contract and restarts them at `resolve_source`.

Verification observed for the v4 workflow: 181 unit tests plus TypeScript passed, both canonical and Compass-profile Hermes skill copies passed syntax and byte-identity checks, the Hermes contract passed with 49 migrations and 92 owned routes, the production build passed at 91.59 KB gzip for the base client bundle, the complete route/responsive E2E suite passed, and `git diff --check` passed. The legacy-retry repair additionally passes its regression test, the 35 focused Lite Visual/artifact/job/route tests, TypeScript, and `git diff --check`. This change was not deployed.

## Current truth — 2026-08-20 (Hermes reliability, calibration, and loop repair)

The live Hermes backlog was fully resolved: 11 feedback jobs completed with evidence-backed no-change or one applied behavioral-economics depth rule, 12 orphaned/rejected visual jobs were cancelled after exact target verification, and the Contract Theory extraction completed with one source note, five anchored Learning Units, and five editable recall drafts. Job health now reports zero pending, retry, running, failed, stale, delayed, or dead-lettered work. Compass serving was rolled back to shadow because the challenge lane has only 5/8 required outcomes; generic settings can no longer bypass rollout gates. A bounded 28-outcome recalibration updated all six recommendation weights with a conversation-bound receipt.

The pending release fixes the degraded neglected-branch query, adds comparative Compass selection explanations plus recall/consolidation load, adds a Settings → System operational-health ledger, and adds deterministic consolidation reconciliation. A stranded run closes only when canonical note, Unit, anchor, and required draft outputs exist; otherwise one idempotent linked extraction job is restored. Extraction completion also recovers an open run by recommendation ID when legacy job linkage is missing.

Hermes config is migrated to v38. A cloned `compass` profile provides a lean Learning Compass control plane with tool-schema load reduced from 54.1 KB to 33.0 KB. A pre-maintenance snapshot was created, 32 ended sessions older than 30 days were pruned, and compact FTS migration plus VACUUM reduced `state.db` from 803.1 MB to 326.4 MB. The disabled audit-blocked Antigravity Hub skill was removed from both profiles; both audits now report no Hub-installed skills.

The final repair is deployed as Worker version `e7e7be4a-11fe-4d0b-9a5f-ac226c499962` with rollback version `4b798325-6ab2-41d5-89a4-89faf03e9111`. All eight stranded consolidation runs are closed: one reconciled from existing canonical outputs and seven completed through newly linked source-grounded extraction jobs. The stale-Thread foreign-key failure discovered during replay is fixed by validating legacy `thread_id` before note, Unit, draft, or Thread-link writes. Contract Theory and the repaired historical sources now have anchored Units and editable drafts; the Harvard negotiation record was explicitly reprocessed to restore its three missing drafts. Local verification passes 174 unit tests plus TypeScript, production build, clean/idempotent migration rehearsal, Hermes contract, full E2E, and `git diff --check`.

## Current truth — 2026-08-20 (personal knowledge-branch rebuild)

Branches have been rebuilt locally as Mahmood's personal knowledge registry rather than a test-driven review deck. The manager shows only 22 durable top-level branches across six live categories; topics and all linked sources, notes, recall, units, and files stay inside each branch dossier. No branch starts paused. Practical AI material is filed under Linux & Automation, Systems Thinking, or Power & Social Dynamics according to its actual subject. The manager supports search, dynamic category filters, Active/Priorities/Paused/Archived views, create, identity-preserving edit, rank-1 promotion, pause/resume, reversible archive/restore, and undo. Metadata edits do not alter taste/profile state, and every priority mutation keeps one contiguous order that undo restores exactly.

Migration `0047_personal_branch_map.sql` consolidates temporary/test expansions into topic leaves, maps legacy branch IDs, assigns the 43 previously unmapped production records to verified personal branches, resets stale branch-review signals, and installs five explicit priorities while keeping ordinary branches taste-neutral. The API now returns only top-level branches plus live categories, accepts category-validated `add` and taste-neutral `update`, and preserves branch/round provenance. Hermes' installed operating skills and repository contract describe the same personal registry.

Released as Worker version `96e47652-59cc-4a56-998a-28217ba5cfdb` with rollback version `f657c0e8-d6c6-4201-a3a1-b18fd34b6f8d`. Migration `0047` is applied remotely with no pending migrations. Production reports 22 top-level branches, six categories, five contiguous priorities, zero paused branches, zero unmapped live/learning/consumed sources, and no Practical AI top-level node; 28 deleted historical tombstones remain intentionally outside the active map. The release passed 171 unit tests plus TypeScript, production build, clean/idempotent migration rehearsal, the expanded branch integration flow, Hermes/Compass contracts, full E2E across five roots and 21 mode states, `git diff --check`, all required live API smokes, activated service-worker readback, and real-browser Atlas/Branches/public-folio checks at desktop and 390px with no overflow or browser errors.

## Current truth — 2026-08-20 (Three-Book Canon Learn mode)

Canon is now implemented as a dedicated Learn mode instead of a finite Thread or a Books-shelf variant. Migration `0046` adds the atlas charter, branch-verified domain families/domains, three unique dossier roles, replacement history, database completion gates, and lossless supersession metadata. It copies the existing Three-Book Canon Thread brief into the atlas, preserves that Thread read-only, hides it from the normal Thread ledger, and redirects its client route to Canon. Ten varied pilot domains are seeded only when their real non-pruned production branches exist.

The Canon atlas exposes search, curation/field-test filters, counts rather than a false global completion percentage, verified branch badges, and “Take me somewhere unfamiliar.” Each domain renders permanent Foundation, Representative, and Boundary slots with beginner/expert rationale, contribution, limitations/difficulty, rejected alternative, evidence, consumed/blacklist state, explicit branch-preserving Library capture, and explicit Thread creation. Canon mutations are available through the same agent capability registry; completion requires three approved dossiers and selection replacement keeps its reason/history.

Released as Worker version `f657c0e8-d6c6-4201-a3a1-b18fd34b6f8d` with rollback version `0895cea9-3fa1-40db-a86f-e866cf2a774b`. Migration `0046` is applied remotely; the production atlas contains four families and ten pilot domains, and the original Three-Book Canon Thread remains readable with its Canon supersession link. The 170-test suite, TypeScript, production build, clean/idempotent migration rehearsal, Hermes contract, full Worker/browser E2E, and `git diff --check` pass. Live smoke checks passed for Canon, the superseded Thread, the release API set, the public learning-materials folio, manifest, service worker, and application shell. Its earlier command-center and capture copy was superseded by the current direct-lesson and captured-Library contract.

## Current truth — 2026-08-20 (Thread command center and lifecycle repair)

Threads now open to a command center with searchable Curriculum and a scope-preserving Materials index. Levels require explicit Start, then advance strictly through direct lesson completion; projects, notes, sources, ratings, and recall never gate progression. Lesson content renders structurally, lesson status has explicit Start/in-progress/error states, and source cards show their verified branch and round. Migration `0045` adds exact Lesson ownership for notes, files, cards, and drafts; migrations `0043` and `0044` removed the superseded alternate progression schema.

Released as Worker version `cfc8d381-e1bc-46f1-a4e8-aa056198a657` with rollback version `a63e7e48-ac57-458c-a3fb-7104fbf1c949`. Migration `0045` applied remotely with no pending migrations. The 170-test suite, typecheck, production build, clean/idempotent migration rehearsal, Hermes contract, full E2E, and diff check passed. Live smoke returned the expected responses across the release API set; the Thread command center rendered four tabs at 1440px and 390px with zero overflow or browser errors, and the public learning-materials folio passed its phone check.

## Current truth — 2026-08-20 (public learning-materials update folio)

The standalone public page at `/updates/learning-materials` explains the lesson-material workflow without exposing private Thread data or adding a sixth application root. Its source-spine folio shows one clear starting material plus the Original, HTML, PDF, and NotebookLM roles when available; the active v4 contract uses canonical HTML, a matching tablet-ready PDF, focused on-request NotebookLM, truthful provider states, paired revision integrity, and the unchanged no-score/no-XP mastery boundary. Every `Open Learn` action returns to the canonical `/#/learn` workspace.

The page is self-contained semantic HTML with one heading, a keyboard skip link, system fonts, light/dark palettes, reduced-motion handling, visible focus, and a restrictive no-script content policy. The Worker serves it publicly even when private API authentication is enabled, sets no-store and anti-framing headers, and permanently redirects the `.html` spelling to the canonical extensionless URL. It remains network-served and is deliberately not added to the offline application-shell cache.

Verified with `npm test` (169 unit tests plus TypeScript), production build, full E2E, clean/idempotent migration rehearsal, Hermes and Compass bridge checks, and `git diff --check`. Real-browser inspection passed at 1440px light/dark, 390px, 320px, reduced motion, keyboard-only entry, and 200% text size with zero horizontal overflow. Production deployment completed as Worker version `0dfd17ec-93e1-4439-a7ae-5237f1c8fa28`; rollback version is `43915344-2e62-4a75-b409-f6e296cdabe3`. The live page returns HTTP 200 with the expected security headers and passed desktop, dark-mode, and phone browser checks with four material roles and zero browser errors.

## Current truth — 2026-08-20 (Lesson learning-material enhancement)

The Learn structure and typed Thread/Level/Lesson routes are unchanged. Each Lesson source now has one clear material launcher: an explicit usable `recommended_start` when available, otherwise HTML then Original, followed by purpose-labelled alternatives. An unindexed or failed NotebookLM link stays an alternative instead of becoming the main start. The launcher shows real size, page, language, revision, active-check count, and plain provider state; it never calls a NotebookLM link ready without a matching receipt. The Thread path returns the newest complete HTML/PDF pair instead of mixing revisions, preserves the legacy metadata JSON while also exposing parsed metadata, batches NotebookLM state reads, and the source dossier uses the same coherent pair selection.

Visual Lite v4 supersedes the earlier contract-v3 pause/widget layer. New companions are quiet reading documents with no mandatory interaction; HTML is the canonical body, PDF is printed from it, and one atomic pair plus its validation receipt carries revision and source identity.

NotebookLM source learning now requires the saved notebook URL plus an observed indexed-source receipt. Its focused router defaults to one hard 5–8 question source-grounded quiz with hints before explanations and at least one transfer question; Arabic audio is reserved for orientation/review, rich formats require a matching concept, and every plan is capped at three outputs. Canonical `pending|indexed|ready|failed` receipts live in the existing ledger, accept only known fields, invalidate outputs from an older index or notebook URL, and keep each displayed format tied to its own real status. They never change lesson, Level, or Thread completion or recall scheduling. The Lesson launcher renders those states as Linked, Indexing, Ready to ask, Building, Ready, or Needs attention.

Verified with `npm test` (167 unit tests plus TypeScript), `npm run build` (84.02 KB gzip base client), `npm run test:e2e` including the desktop/390px material launcher and NotebookLM ready-state flow, clean/idempotent migration rehearsal, `npm run verify:hermes`, `npm run test:compass-bridge`, Python compilation for the pair uploader, and `git diff --check`. Production deployment completed as Worker version `43915344-2e62-4a75-b409-f6e296cdabe3`; rollback version is `91fe5365-2e72-48c9-9ead-8e18eb0b8ace`. Twenty-three release and learning endpoints returned their expected live responses. Real-browser checks at 1440px and 390px in light/dark media modes found the five roots, the live material launcher with one start and three alternatives, zero overflow, and zero browser errors. A fresh live service worker reached ready; an 8,930-character HTML companion reloaded offline with an exact text match, and the offline mobile shell retained all five roots with zero overflow.

## Current truth — 2026-08-19 (transparent offline HTML companions)

Cache v11 is deployed with no UI changes. After an HTML reading companion opens successfully online, the service worker refreshes a dedicated exact-URL artifact cache; the same URL then reloads offline. Application-shell navigation is isolated, so an artifact can no longer replace the cached PWA shell. Unopened HTML companions and PDFs remain network-dependent. The full E2E suite verifies online open → network blocked → offline companion reload → offline Home recovery. Production version `91fe5365-2e72-48c9-9ead-8e18eb0b8ace` passed all 19 live endpoint smokes and a fresh Android-profile test against an existing Arabic companion: the network probe failed as expected, the 8,901-character reading body matched online exactly, Home recovered offline with five dock items and zero overflow, and the dedicated artifact cache remained separate from shell v11. Rollback version: `6e2e4ad4-00cf-4fd6-9be8-0ba83b431fbb`.

## Current truth — 2026-08-19 (native Android PWA foundation)

Learning Compass now has one canonical Android experience built from the existing client rather than a second mobile fork. The application shell links a single Worker-served manifest with standalone launch, exact 192px/512px PNG launcher assets, a dedicated maskable icon, Capture/Queue/Recall launcher shortcuts, `navigate-existing` relaunch behavior, and the Android share target into Library → All sources. The active entry registers the service worker; cache v10 precaches the fingerprinted shell, launcher assets, and bounded Home briefing required for a first-install cold offline reload. Installed mode respects cutout/system safe areas, uses a sticky mobile utility bar, and suppresses browser overscroll. Android receives a dismissible install card only when the browser emits `beforeinstallprompt`; Not now suppresses it for 30 days. The Capture launcher shortcut opens the existing global capture dialog and returns to All sources.

Verified with 136 unit tests plus TypeScript, production build, clean/idempotent migration rehearsal, Compass bridge, Hermes contract verification (44 migrations, 25 synchronized checks, 87 owned routes), `git diff --check`, exact PNG dimensions/hashes, and the full E2E suite across five roots and 20 mode states. E2E verifies service-worker readiness, Android launcher capture, a cold offline mobile reload, the Android install/dismiss flow, five-root navigation, and no horizontal overflow. The initial live release exposed that a first-install offline reload had the shell but not Home's read model; cache v10 closes that gap by precaching `/dashboard/briefing`. Replacement deployment `6e2e4ad4-00cf-4fd6-9be8-0ba83b431fbb` passed a fresh-profile live Android test with the network blocked: Home rendered from cache, all five dock items remained available, horizontal overflow was zero, and browser errors were zero. The earlier release passed 19 live JSON smokes, matched all launcher-icon hashes, exposed 190 capabilities and one schedule, and completed a live Hardcover sync/readback with 45 books, 129 entries, zero duplicate groups, and zero stored line breaks. Rollback version: `551d81cd-cd29-4a12-82b8-4669106b372a`.

## Current truth — 2026-08-19 (KOReader/Hardcover reading journal)

Library → Catalog now includes Your reading journal, which securely syncs Mahmood-authored KOReader highlights and notes through Hardcover. The compact catalogue card combines identity, sync state, and ledger totals; search and both filter groups share one command bar, so books begin immediately below it. The default scope shows only books with at least one highlight or note; All books restores the complete synced library. Each sync collapses imported line wrapping and removes only resend duplicates with identical book, type, normalized text, timestamp, and page provenance while retaining a stable source ID. The journal UI is read-only apart from explicit synchronization and no longer exposes branch assignment, Add to Compass, or In Compass controls. `HARDCOVER_API_TOKEN` is a Worker-only secret and explicit sync writes the D1 mirror. The production mirror currently contains 45 books and 129 clean entries: 110 highlights and 19 notes, with zero duplicate groups or stored line breaks.

Migration `0042_hardcover_reading_journal.sql` is applied and production version `1e45acd5-2f90-4a6f-b709-46d8f84e1594` is live; rollback version is `0cee35f4-fd59-49f5-b765-86f16a7a4faa`. Verified with 133 unit tests plus typecheck, production build, Hermes and Compass bridge contracts, full route/responsive E2E, `git diff --check`, a successful live sync, and the production duplicate/line-break detector returning zero for both defects.

## Current truth — 2026-08-19 (bilingual note reader redesign)

The typed note route is now a dedicated long-form reading surface instead of a plain-text dump. It removes Obsidian front matter and duplicate embedded source lines from reading mode, promotes an embedded source URL when the note record lacks one, preserves Markdown headings, ordered/unordered lists, quotes, emphasis, links, and inline code, and computes word count plus reading time. Direction is resolved per content block, so Arabic prose reads RTL while English titles and passages remain LTR; the responsive Evidence Ledger margin index follows the reading direction. Editing remains lossless and continues to expose the original section content.

The visual surface uses the active semantic theme and typography tokens, a 45–75ch manuscript measure, quiet source/status metadata, one top-level edit action, and a completion-oriented recall action. Verified with 131 unit tests plus typecheck, production build, full E2E, Hermes contract verification, `git diff --check`, and real-browser inspection at 1280px and 390px with zero horizontal overflow. Deployed as Worker version `0d0116c8-0ecc-4c5b-9f83-86bde987d328`.

## Current truth — 2026-08-19 (authored lesson creation and Obsidian path migration)

Learning Threads can now create complete Level-owned lessons through `POST /learning/core/threads/:id/stages/:stageId/lessons`. The guarded capability accepts full pedagogy/content and can link an existing work item through `legacy_item_id`, avoiding duplicate progression records while switching the Level to course progression. The Behavioral Psychology Thread was migrated from Obsidian into 4 Levels and 12 fully authored lessons, with 26 preserved files, 22 readable notes, 12 Level-owned recall cards, and five canonical consumed book/video sources attached to exact lessons with verified branches. Duplicate publisher-URL book records created during migration were reversibly archived in favor of the existing canonical consumed records. Production version: `8427edef-4a7e-44af-81fd-426e76adb417`; rollback version: `ffafa36f-7235-4b0e-9073-9d21770eb314`. Verified with 126 unit tests plus typecheck, production build, full E2E, agent/Hermes contract, clean/idempotent migration rehearsal, deploy smoke, and canonical Thread/source readbacks.

## Superseded history — 2026-08-18 (reading-companion workflow repair)

The former v2 workflow used per-section visual decisions and an external image path. Visual Lite v4, documented at the top of this file, retires that architecture completely; this entry remains only as release history for artifacts produced under the old contract.

Lesson 01's four Hermes-owned companions were rebuilt from scratch with complete Arabic explanation, source-specific colored editorial systems, varied reading rhythm, and 12 grounded AGY SVG figures. All four passed the strict validator and responsive/PDF checks, were uploaded and verified on their source records, and the eight superseded image-atlas artifacts were removed. Repository verification passes with 125 unit tests plus typecheck, `verify:hermes`, and `git diff --check`.

## Current truth — 2026-08-18 (branch-safe lesson source portfolios)

Exact lesson source attachments now require a visible `primary|case|challenge|reference|optional` role and a verified non-pruned Level branch. The atomic attachment stores the lesson role, persists `recommendation_meta.branch_id`, and moves newly pushed study material from `captured` to `learning_state=attached`, so curriculum filling no longer creates unmapped recommendations or pollutes the All sources catalog. `global_fetch_strictly_public` is enabled because guarded `/agent/request` uses same-zone Worker subrequests; without it Cloudflare returned error 1042 and blocked every guarded mutation. URL deduplication now runs YouTube canonicalization only for actual `youtube.com`/`youtu.be` hosts, preventing ordinary paths such as `nist.gov/publication/...` from being corrupted into fake YouTube URLs.

The Level/path source-selection procedure now compares exact objective coverage, prerequisite fit, depth, source authority, accessibility, sequence placement, and semantic novelty; additional sources are accepted only for distinct roles. Level 0 of `Systems Thinking — From Orientation to Advanced Practice` is now complete: 7 lessons × 5 exact visible roles = 35 unique, live-verified sources; all 35 persist `systems-thinking-level-0`, 34 are `attached`, the previously completed source remains `completed`, none entered Queue, and the historical administratively excluded Meadows record is not attached. Lesson role attachment is now replacement-safe, so one role cannot accumulate duplicates. Verified with 124 unit tests plus typecheck, production build, full E2E, Hermes contract, clean/idempotent migration rehearsal, Compass bridge handshake, deployment smoke, canonical Thread/source readbacks, and `git diff --check`. Production version: `ffafa36f-7235-4b0e-9073-9d21770eb314`; rollback baseline: `89ac4dd0-26c5-46ce-b243-ed0c4e3436ca`.

## Current truth — 2026-08-18 (Thread study-to-proof enhancement)

The Learning Thread workbench now represents the actual learning state instead of treating every unfinished lesson as active. Lessons resolve to Ready, In progress, Completed, or Needs material from their status plus authored content/verified sources. Missing-material lessons explain the block and keep completion disabled; the next action skips blocked lessons and becomes a preparation state when nothing is ready.

Level progress is one direct lesson-completion track, presented as a single numbered curriculum spine rather than a two-column card wall. Typed Level and Lesson pages have real `h1` headings and a Threads breadcrumb that returns to `#/learn`. Lesson pages render `why_learn`, `why_now`, and `takeaway` as a learning contract, with progressively disclosed Level-owned note, file, and recall tools that do not affect progression. Level and Thread material ledgers are collapsed by default; primary back/source targets meet mobile touch sizing.

Verified with `npm test` (120 unit tests plus TypeScript), `npm run verify:hermes`, clean/idempotent migration rehearsal, Compass bridge verification, `npm run build`, `npm run test:e2e`, `git diff --check`, and real-browser renders at 1440px and 390px. Production deployment completed as Worker version `89ac4dd0-26c5-46ce-b243-ed0c4e3436ca`, serving `/assets/index-Di0OyU1O.js` and `/assets/index-DXa9WIU1.css`. Live smoke checks confirmed all required release endpoints, zero horizontal overflow, active work before Levels, one ready and six blocked lessons, collapsed Level/Thread materials, a 44px mobile back target, visible missing-material recovery before the lesson contract, disabled completion for the blocked lesson, and no browser errors.

## Current truth — 2026-08-18 (Thread workbench audit and responsive repair)

The live Learning Thread level surface was audited at desktop and phone widths using the Anthropic `frontend-design` guidance plus the local usability, journey, accessibility, and edge-state standards. The primary defect was responsive ordering: at 390px the Levels roadmap rendered before the active level, pushing the lesson work below the fold and behind the mobile dock. The level workbench now keeps the active level first at every width, then the roadmap, then the material ledger.

The active level now exposes an explicit `Next up` lesson action, a `0 of N lessons complete` progress bar, a `Threads / Learning path` wayfinding cue, a sequence completion count, and stronger visual treatment for the next lesson. Level cards now show lesson progress rather than only total lesson count. Existing typed Thread/Level/Lesson routes, source/material ownership, completion mutations, and the five-root shell remain unchanged.

Verified with `npm test`, `npm run build`, `npm run test:e2e`, `git diff --check`, and Playwright renders at 1440px and 390px. The local render confirmed the active level begins at y=151px on the phone and the Levels roadmap begins after the active work; production was deployed as Worker version `6b09bc69-292f-4154-a372-e89d1005a489` and the live 390px smoke check confirmed the same ordering, the new progress/Continue panel, and zero horizontal overflow.

## Current truth — 2026-08-18 (thread surface simplification)

Learning Thread tabs no longer show the top header or right-side command card: back navigation, status, title, guiding question, current-level, focus, progress, Continue, Edit Thread, and finish-line elements are removed. Stage-level learning content remains available.

The level map is now dismissed from the default Thread view, and the active level content expands across the page as the primary surface.

The active level view now omits the Next action card and Evidence gate entirely; the lesson sequence is the remaining primary content surface.

The remaining action-count labels were removed, and the active level canvas is constrained to a readable 980px desktop width instead of expanding edge to edge.

The excessive blank gap above the lesson sequence was removed by collapsing the stage-header and lesson-section spacing.

The decorative divider beneath “Learn in sequence” was removed so the lesson grid starts without an extra line.

The Level project card was replaced with a selectable Levels section below the lessons. Selecting a level updates the active content above.

The Levels section is collapsed by default and expands as a native accessible disclosure when needed.

Lessons now have canonical nested routes under their parent Thread: `#/learn/thread/:threadId/lesson/:lessonId`. Lesson cards, previous/next controls, and the lesson back link preserve that route identity.

Lesson routes now use the shorter canonical form `#/learn/t/:threadId/l/:lessonId`; the verbose `/learn/thread/.../lesson/...` form remains readable and canonicalizes to the compact route.

Lesson UX now hides the global Learn switcher, uses “Back to level,” removes the obsolete Edit instruction, removes duplicate empty-state separators, and gives the reading surface a more useful width.

Nested lesson pages no longer render the parent Levels disclosure; lesson navigation stays focused on the lesson itself, while Levels remains on the parent Thread view.

The parent Thread now opens Levels by default, remains collapsible, and shows each level’s lesson count so the existing curriculum is immediately discoverable.

Lesson footer controls now use consistent inline-flex button geometry, no inherited link underlines, stable icon alignment, and responsive full-width behavior on mobile.

## Current truth — 2026-08-18 (Preferences workspace redesign)

Settings → Preferences is now a deliberate workspace instead of one continuous same-priority control dump. It leads with the active visual system and four complete workspace styles, keeps native density/corner/text-size choices and learning behavior visible, progressively discloses theme, font, detailed typography, custom-system, and Map tuning, and pairs the desktop controls with a sticky non-interactive studio preview. Mobile uses readable choice targets, a compact two-column theme gallery where space allows, and corrected sticky section jumps that do not hide destination headings. The active-workspace summary reports a named preset only while its typography and display values still match; fine-tuning correctly changes it to Custom tuning.

The behavior contract remains real and global: E2E changes and reloads density, corner geometry, text size, reduced motion, and a dark theme through the Worker settings API, then verifies the corresponding computed application behavior. Advanced groups use native disclosures, primary comfort choices use native radios, the preview exposes no fake actions, and every existing theme/font/custom/typography/learning/Atlas capability remains available.

Verified with `npm test` (114 unit tests plus TypeScript), `npm run build`, `npm run test:e2e`, `npm run verify:hermes`, `npm run verify:migrations`, `npm run test:compass-bridge`, and `git diff --check`, plus real-browser inspection at 1440px, 900px, 390px, and narrow mobile behavior. Production deployment completed as Worker version `d1c97c60-0b9c-4826-a1d3-e3c87a0fb466`; the live route serves `/assets/index-GVxlMmkV.js` and `/assets/index-uhD0e6uM.css`, has no browser warnings/errors or horizontal overflow, and exposes the expected five roots, four workspace styles, three comfort groups, four closed advanced disclosures, 188 System operations, and one schedule. All required release endpoints and the Hermes repair dry-run returned HTTP 200; service-worker cache v8 is live.

## Current truth — 2026-08-18 (frontend systems and settings remediation)

The frontend settings contract now changes the complete product rather than only its preview: theme/custom palette, typography, density, corner geometry, and reduced motion flow through shared semantic tokens, persist across reloads, and preserve contrasting action ink. Server-saved custom palettes take precedence over stale local custom-theme state. The global desktop rail now exposes Search, Capture to Library, and sync state; tablet/mobile use separate Search and Capture utilities above an exact five-root dock. The default capture service and every visible capture entry point create ordinary `captured` records, while Queue remains an explicit triage commitment.

Learning Threads expose the authored curriculum, direct lesson progress, optional projects, and final synthesis. Settings controls use native pressed/toggle semantics, full-row toggle targets, unique IDs, labeled color inputs, visible focus, and global motion behavior. Empty-state sizing and redundant mobile capture actions were tightened after desktop, phone, and tablet inspection.

Verified locally with `npm test` (112 unit tests plus TypeScript), `npm run build`, `npm run test:e2e`, `npm run verify:hermes`, clean/idempotent migration rehearsal, Compass bridge verification, `git diff --check`, and real-browser desktop/phone/tablet inspection. The base application bundle is 76.70 KB gzip; Atlas remains a lazy graph chunk. Production deployment completed on 2026-08-18 as Worker version `7305cead-f45d-4017-942b-29e311a33814`. All release-checklist read endpoints returned HTTP 200, the live frontend served the new fingerprinted assets and service-worker cache v8, and live desktop/mobile/Settings System checks confirmed five-root navigation, 44px touch actions, zero overflow or browser errors, all 188 allow-listed operations, and the configured six-hour schedule.

## Current truth — 2026-08-17 (Compass bridge mutation repair)

The Learning Compass MCP bridge now sends guarded mutations directly to `/agent/request` instead of routing them through the same-Worker `/agent/tool-call` proxy, which returned a generic HTTP 500 during feedback recording. Canonical reads remain direct GETs; mutation idempotency, dry-run, risk gates, and verification are unchanged. A regression test now asserts that `compass_feedback` targets `/agent/request`, and a live production feedback dry-run returned `ok:true` with no blocker.

## Current truth — 2026-08-17 (branch-verified Hermes recommendations)

Hermes Compass recommendations now enforce the branch contract at candidate submission. Every candidate must name an existing, supported, non-pruned `tree_nodes` branch; missing, unknown, invalid-type, or pruned branches are rejected before scoring. A confident winner stores that exact verified `branch_id` on `recommendation_meta` and `recommendation_outcomes`, and the Queue reads the same branch identity. This closes the gap where the API documentation required a branch but the route could previously accept an unlinked candidate. The fix is deployed at `https://recommendations-worker.mhmudnasr30.workers.dev` as Worker version `51d16175-0f2a-4e10-b5ba-81833063db83`.

Verified with 106 unit tests plus typecheck, production build, Compass feedback integration, full E2E, `npm run verify:hermes`, `git diff --check`, and live `/health` HTTP 200.

## Current truth — 2026-08-17 (profile taste affinity labels)

The Settings profile no longer exposes internal branch IDs in **Taste affinities**. `GET /brain/profile` keeps each vector's internal `topic` key for scoring but resolves a readable `label` from `tree_nodes` (or the priority label as a fallback); the profile list uses that label and shows an affinity score instead of repeating the storage key. The fix is deployed at `https://recommendations-worker.mhmudnasr30.workers.dev` as Worker version `32139a90-d5db-4070-b827-0b8d88a93718`.

Verified with 106 unit tests plus typecheck, production build, full E2E, `git diff --check`, live `/health` HTTP 200, and a live profile read confirming the four previously leaked IDs resolve to their Arabic branch labels.

## Current truth — 2026-08-17 (Settings control-center redesign)

The Settings workspace now leads with a readable learning-profile summary instead of the internal profile model: an explicit Edit profile action, health/status language oriented around user decisions, summary cards for context/priorities/boundaries/quality/workflow, and searchable learned signals behind progressive disclosure. Technical model identifiers remain available under Model details, malformed legacy JSON stays out of the normal view, and Data/System copy now distinguishes backup/recovery and advanced operations from everyday controls. Existing routes, API contracts, and operational inventory behavior remain compatible. This redesign is deployed at `https://recommendations-worker.mhmudnasr30.workers.dev` as Worker version `51469e14-fdd8-4ea8-896e-b98bac23d1c1`.

Verified locally with `npm test` (105 unit tests plus typecheck), `npm run build`, `npm run test:e2e`, `npm run verify:hermes`, and `git diff --check`; live `/health` returned HTTP 200 after deployment.

## Current truth — 2026-08-17 (Hermes cockpit, evidence ledger, recovery, and capture interoperability)

The Hermes enhancement wave is implemented locally. Home and `/agent/briefing` share a deterministic next-action projection across due recall, direct lesson progress, consolidation repair, Queue, drafts, and captured Library sources. `/agent/activity` and the System surface show persisted agent receipts, audit events, jobs, proposals, and runtime health. Migration `0038` adds source-native annotations with web/PDF/video/EPUB/artifact locators, language, context, checksums, and links from Learning Units, notes, and recall drafts; `/search/evidence` returns those locators and downstream derivations for grounded retrieval. Semantic indexing now carries language metadata and includes active annotations.

That historical release briefly documented an optional private mode; the mode is now retired and ordinary Learning Compass API access is public. Telegram webhooks still require their independent secret header, can restrict an allowed chat, and deduplicate `update_id` through migration `0039`. A least-privilege Manifest V3 extension opens the existing global capture dialog for page URLs and selected passages, preserving captured-Library semantics. `npm run export:recovery` creates a checksummed D1 export and manifest; `npm run verify:recovery` checks the payload before storage or restore, while R2 binary backup remains a separate verified operator step.

Verified locally with 104 unit tests plus typecheck, production build, E2E, migration rehearsal, Hermes contract verification, MCP handshake, recovery export/checksum verification, and `git diff --check`. Remote migrations `0038` and `0039` applied successfully. The Worker is deployed at `https://recommendations-worker.mhmudnasr30.workers.dev` as version `f7a10602-1a62-48f0-8727-716cb171fc84`; live smoke checks returned HTTP 200 for `/health`, `/agent/briefing`, `/agent/activity`, `/agent/capabilities?q=briefing`, and `/search/evidence?q=learning`.

## Current truth — 2026-08-17 (agent control protocol v2)

The Learning Compass operator uses contract `2026-08-20` / `learning-compass-agent-http/2`. `/agent/context` reuses the complete canonical Queue projection, distinguishes completed Threads from compatibility-only legacy mastered exclusions, reports per-section freshness/errors, and returns HTTP 503 when required Queue/branch components are unavailable; its `learning_gaps` array is currently a placeholder, so `/compass/context` owns recommendation targets and the Thread path owns direct lesson progress. `/agent/capabilities` is structured and filterable; `/agent/openapi.json` is generated from the same registry and includes the guarded `/agent/request` control route. Function calling exposes only `list_capabilities` and `site_request`. Agent mutations provide dry-run impact, exact-target asserted preconditions for high-risk actions, atomic method/path/body-fingerprinted idempotency reservations (migration `0037`), batch-aware canonical readbacks, and explicit committed-but-unverified receipts when post-write verification fails. Contract drift is enforced by `npm run verify:agent-contract` across source, routes, tools, schemas, migrations, documentation, and both installed Hermes profiles.

Verified in an isolated release worktree: agent tests 8/8, complete unit suite 103/103, typecheck, E2E, clean and idempotent migration rehearsal, production build, contract verification, and `git diff --check` pass. Remote migrations `0036` and `0037` applied successfully. Production version `63769064-0bec-45e1-9c7b-3e5d95167a6e` passed live health, context, capability, OpenAPI, tool inventory, dry-run, dashboard, Queue, Notes, Recall, and job smoke checks; rollback version is `fad550a7-ba31-4bb1-867a-cc6ab08891f0`.

## Current truth — 2026-08-15 (book chapter metadata)

Books can now register chapter metadata without uploading files. `POST /recommendations/books/:id/chapters` creates or updates book-scoped chapter rows only; it reports `artifacts_created: 0`. When a chapter PDF is explicitly uploaded with `scope=book`, it is shown only in the owning Books chapter row and excluded from the general Files library.

## Current truth — 2026-08-15 (permanent source deletion)

Archive, All sources, and Books now expose a guarded **Delete permanently** action for non-active records. It requires confirmation, removes the recommendation and linked feedback, sessions, notes, recall, learning history, branch evidence, jobs, and generated R2 artifacts, and cannot be undone. The API contract is `DELETE /recommendations/:id/permanent`; active records remain protected until archived, and the existing archive/delete flow remains reversible/soft-delete behavior.

## Current truth — 2026-08-15

The visual-system JSON contract is now applied globally and documented in the UI: imported/saved partial custom font objects are normalized, server settings hydrate on every route at startup, the active day/night palette pair rehydrates after reload, headings use the display stack, Arabic-capable fallbacks are explicit, and local-only Berkeley Mono is never sent to Google Fonts without a web-safe fallback. The visible palette textarea now accepts either the original 11 color codes or a complete visual-system JSON, so pasted `appearance.customFont` and `appearance.typography` values are applied instead of being silently ignored. The Preferences UI explicitly explains the full scope; import/export actions and the AI prompt use the same contract. Preference jump links now scroll the nested workspace canvas without replacing the application route. Surprise me now selects a randomized extreme art direction per request, including monochrome, high-chroma, brutalist, Swiss, editorial, astronomical, and other palette families, while using famous web/design traditions only as inspiration rather than copying exact branding. Verified with 93 unit tests, typecheck, production build, browser JSON-paste test, cross-route Home font test, and live health check; deployed as Worker version `955f008a-fa29-4c2a-9932-aed4543a349b`.

Queue now defaults to its ranked Ledger view. The compact Gallery remains a selectable alternative, and the choice persists locally per browser. Both views share the existing branch, note, recall, companion, Record, and tracked Start/Resume behavior.

The product is now **branch-centric**: branches (with evidence-based R1/R2/R3 progression rounds) are the organizing axis across the backend, the UI, and the Hermes layer.

- **Systems-thinking alignment (live, 2026-08-15)**: the `systems-thinking` branch now contains seven visible active child nodes matching its Learning Thread levels, while branch metadata mirrors the senior review: one recurring anchor problem, compact Level 0 packet, evidence-gated unlocking, and transfer into a real decision/project. The live branch remains `love`, priority 3, R2 after its first consumed/mapped source, with all seven curriculum children still active/uncompleted; verified through `/knowledge/graph` hierarchy edges and `/brain/node/systems-thinking`.

- **Round model** (`src/services/branch-rounds.ts`): explicit `round_label` wins; otherwise R1 until a source is consumed, R2 at first consumed source (or two notes / three approved recall cards), R3 at three consumed sources plus consolidation and recall strength. `r2-`/`r3-` id prefixes are fallbacks. `recommendations.round` (0035) remains a display column; `recommendation_meta.branch_id` is the canonical linkage.
- **New route** `GET /brain/branches/:id/items` — the authoritative linked-items ledger for one branch: header (label, status, derived round, priority, balance node), ancestor path, recommendations (rating, consumed date, recall, companions, note), notes, recall cards, pending SRS drafts, and R2 artifacts.
- **Enriched contracts**: `GET /capture/queue` items now carry `branch` (`id/label/round/status`), `note`, `recall` (`count/due`), and `companions` (`html/pdf` artifact ids); `GET /capture/:id/record` adds `branch`, derived `round`, `companions`, and `srs.recall_summary`; `GET /recommendations/list` rows add `branch_label`, `round_label`, `branch_status`.
- **UI**: Queue and All-sources views render branch/round badges, note links, recall status, and companion buttons; the Branch Deck gained a Linked Items Ledger panel (sources, notes, recall cards, drafts, artifacts with dossier jumps); the unified source dossier gained Branch, Reading Companions, Active Recall, and Feedback & Evidence sections; the global inspector shows Branch and Round facts.
- **Settings consistency fix**: migration 0023 seeded `profile_automation` as `automatic`, silently overriding the new `manual` default on fresh databases; the seed now stores `manual` and new migration `0036` aligns existing databases. E2E updated for the manual-review contract (proposals hold for approval, `decision_source: 'user'` on approve).
- **Hermes contract v5**: adopted `learning-compass-source-ingestion` and `learning-hub-companion-authoring` as active skills, retired the renamed `learning-thread-curation` (disabled in `~/.hermes/config.yaml`), fixed the `progressive-learning-curriculum` frontmatter, added route ownership for the new ledger/queue/record reads, and completed the migration inventory through `0037`. `npm run verify:hermes` passes (previously blocked on 0031–0033 inventory drift).

Verified: `npm test` 93/93, typecheck, production build, and live health smoke check pass. Full E2E remains blocked by the typed-source inspector assertion noted in the task result. Gallery Queue deployed to Worker version `e6c2e1f2-5221-4417-950d-5c71a3c8433a`.

## Previous truth — 2026-08-14

The frontend replacement is implemented, wired as the active entry, verified, and **deployed to production** at `https://recommendations-worker.mhmudnasr30.workers.dev` (Worker version `e9542138-53d2-4aa1-91e3-b1d75ed4af3f`).

- `client/src/main.tsx` imports `client/src/app/entry.tsx`; the old monolithic frontend is no longer the runtime entry.
- The shipped visual contract is **Botanical Folio / Evidence Ledger**: green and cream planes, a persistent desktop root rail with command bar (⌘K search, capture), grouped workspace modes, a working canvas, dynamic theme engine (20 presets + custom palettes and typography), and an optional inspector.
- Navigation has five root destinations and 12 grouped modes with subordinate focus filters: Home/Today; Library/Triage (Queue, RSS Feeds), Catalog (All sources, Reading journal, Collections, Archive), Assets (Files); Learn/Threads, Books (Shelf, Canon atlas), Practice; Map/Atlas, Review; Settings/Personal, Data & recovery, System.
- Desktop uses rail + canvas + optional inspector; there is no permanent context pane. Mobile and tablet use a five-item bottom dock with primary modes in an equal-width visible grid and subordinate filters as compact wrapping controls; object inspection becomes a sheet or pushed detail view.
- Legacy hashes, typed object identity, global Capture/Search, route recovery, offline mutation flushing, D1/R2 ownership, and the learning behavior contract remain preserved.
- The retired `app.tsx`, `destinations.ts`, `styles.css`, and `experience-polish.css` are removed. Old `mockups/` and `output/` materials were moved outside the repository; the recoverable pre-reset snapshot remains available through the rollback window.

## Behavior invariants

Every ordinary capture becomes a `captured` Library record. RSS/Atom refreshes remain in the feed history and All sources; they never create a Queue commitment automatically. Queue normally caps queued/in-progress sources at five and requires an explicit override to exceed it. Every captured/recommended/queued item has a verified non-pruned branch plus round. Consumption happens at the real source or a verified canonical reading companion through an explicit learning session. Notes remain structured, editable, searchable, and bilingual by block. Retain/apply—not rating—may create editable SRS drafts, and approval is required before Review. Feedback processing never requests another recommendation automatically. Threads and Levels advance only through direct lesson completion. Lite Visual mines the complete source, writes one canonical Arabic semantic HTML body, and prints its linked A4 PDF from that exact file. It uses prose, native structures, equations, and only rare justified accessible inline SVG; templates, mind maps, raster/generated images, image agents, interaction widgets, hidden transcript padding, and repeated generic UI are forbidden. Gapless source coverage plus deterministic responsive/PDF validation gates one atomic pair, and Notes Extractor never starts automatically.

## Verification observed

Observed release-gate results: `npm test` 83/83 plus typecheck; Hermes contract and migration rehearsal clean; production build clean; and live endpoint smoke tests returned HTTP 200 across `/health`, `/dashboard/briefing`, `/capture/queue`, `/capture/feeds`, `/notes`, `/learning/srs/due`, and `/agent/jobs?status=pending`. Production deployment succeeded on 2026-08-14 (Worker version `e9542138-53d2-4aa1-91e3-b1d75ed4af3f`).

The 2026-08-14 Lite Visual repair was replayed against the complete 82.5-minute Paths to Power source. The replacement contains 5,070 canonical Arabic words across 12 evidence spans with 100% timed coverage, zero generated visual assets, responsive checks at 320/390/768/1024/1440px plus 200% text resize, and all 23 A4 PDF pages rendered and visually inspected. Validator result: pass. The live replacement pair is `paths-power-r5-6c04afa46bd0b0fc` (`artifact_1786694644282_299a2f` HTML and `artifact_1786694654369_e67101` PDF); both live-download hashes match the validated local files. Current repository verification observed `npm test` 81/81 plus typecheck, production build pass, and `git diff --check` pass. `npm run verify:hermes` was blocked by unrelated pre-existing migration inventory drift (0031–0033 missing from the expected contract); that drift is resolved in the 2026-08-15 update, which also completed the inventory through `0036`.

## Post-deploy follow-up

Keep the recoverable pre-reset snapshot through the rollback window. VAPID secrets are configured; a real subscribed-device notification delivery test remains a separate explicit operational check because it sends an external notification.

## Learning workflow improvement — 2026-08-17

The Learning workspace connects the authored curriculum to the primary path screen. A learner can start an available Level, understand why a locked Level cannot open, and advance through direct lesson completion. Optional projects and study materials remain visible without affecting progression.

The selected level is now presented as a learn-first workbench: one dominant next action, compact stage navigation, a prioritized first missing proof, and collapsible proof/lesson inventories reduce the flat course-detail page without changing the API or mastery semantics. Responsive styles preserve the same priority order on mobile.

Verified: `npm test` (103/103 unit tests plus typecheck), `npm run build`, `npm run test:e2e`, and `git diff --check` pass.

## Learning Threads remediation — 2026-08-17

Learning Thread proof submission now carries its exact work-item target in one atomic evidence request; direct stage status PATCH is lifecycle-protected; typed Learn Unit/Card routes resolve exact objects instead of falling back to the Threads list; and the additive 0040 migration establishes explicit progression/evidence target columns and requirement associations. The visible Learn surface now consistently presents Threads, with an explicit Edit Thread entry point for existing authoring controls.

Verification now passes as part of the 2026-08-18 frontend gate: all 112 unit tests, including the transcript adapter, pass with TypeScript, production build, full E2E, and `git diff --check`.

Hermes Thread control is now expanded through the capability registry: project updates and exact SRS card/review operations are discoverable, Thread mutations advertise typed schemas and canonical Thread-path readbacks, and evidence/verification/recall actions are high-risk guarded operations. Permanent Thread deletion is available only as an explicit irreversible exact-target operation with confirmation, precondition, idempotency, and absence verification. Active Hermes procedures use Thread terminology and require user-supplied evidence. The guarded-delete repair was deployed 2026-08-20 as Worker version `90b390e7-4b3c-4ded-93ea-3ac24527a97f` with rollback `0dfd17ec-93e1-4439-a7ae-5237f1c8fa28`; 170 unit tests, typecheck, build, E2E, deploy smoke, live capability discovery, and exact deletion readback passed.

## Hermes Thread authoring skill - 2026-08-18

`learning-thread-authoring` is now canonical at `~/.hermes/skills/learning/learning-thread-authoring/SKILL.md`. It owns Thread, level/stage, lesson, project, source, progression, and evidence authoring, delegates curriculum design to `progressive-learning-curriculum`, and delegates guarded mutations to `learning-compass-site-operator`. The project OpenCode skill is a thin discovery adapter to this Hermes source.

## Hermes skill graph repair - 2026-08-18

The active Learning Compass skills have synchronized ownership. Visual Lite is code-only and has no AGY/image-agent permission or Visual Mind delegation; it never auto-chains extraction. Source notes default to English with source-original Arabic quotations; recommendation serving mode and exploration are Worker-owned live values; source Queue placement requires canonical branch mapping; and JSON mutations use guarded `/agent/request`.

# Current contract update — captured Library sources

New and migrated captures use `learning_state='captured'` and remain durable source records. The retired All sources surface no longer appears in Library; legacy Catalog URLs recover to Archive.

# Current truth — 2026-08-28 (premium shell and guarded releases)

The Learning Compass shell is now a deliberate desktop workbench: a narrow navigation-only root rail, a separate workspace command bar, integrated grouped modes, and a master-detail Home canvas. Mobile keeps the five-root dock and compact wrapping mode controls. Application-only releases now have an explicit guarded lane; corpus publication remains separately blocked until its signed v6 aggregate audit and review exist.

Production recovery is healthy. Snapshot `backup_20260828T131356Z` contains the verified D1 export and all 1,448 R2 objects, passed SQLite integrity and foreign-key checks, and is recorded in production. The automatic backup timer is installed and active. The stale overdue `visualise_source` retry was cancelled through the supported job API; overdue retry count is zero. `npm run deploy` now runs the complete local release gate, requires healthy readiness before deployment, deploys with Wrangler, and requires readiness plus canonical smoke checks afterward.

Release-candidate verification passes: 379/380 unit tests passed with one intentional Chrome-unavailable skip, TypeScript passed, production build passed at 117.68 KB base JavaScript gzip, responsive E2E passed, all Hermes/profile tests passed, migrations applied cleanly and idempotently, and `git diff --check` passed. The guarded release deployed Worker version `bf9c38f5-6a3a-4995-99be-6dc7e998ec57`; pre/post readiness and canonical live smoke checks passed. Browser inspection at 1440×900 and 390×844 confirmed the intended rail/command-bar and bottom-dock layouts with no horizontal overflow or console errors.
