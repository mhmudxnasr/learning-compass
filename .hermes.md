# Learning Compass — Hermes Context

This repository is Mahmood's private Learning Compass learning operating system.

Read `AGENTS.md`, `PROJECT_CONTEXT.md`, and `CURRENT_STATE.md` before changing the project. They are the authoritative behavior, architecture, and handoff sources.

The machine-readable contract at `docs/hermes-contract.json` is canonical for endpoint ownership, permissions, routing policy, side-effect tiers, and the standard specialist receipt. `npm run verify:hermes` rejects drift.

## Product

- Visual system: Scholar's Instrument.
- Loop: capture → curate → consume externally → reflect → notes → review → map update → resurface.
- D1 is canonical. R2 stores large artifacts. Obsidian is archive/export only for extracted notes.
- Eighteen real destinations are defined in `client/src/destinations.ts`; no fallback screens. Curate includes the canonical Books shelf with book visual-companion controls. Files, source-centric Notes, Recall, and Activity live together in Learn; Hermes operations and Memory Review live together in Insights; legacy routes redirect to merged destinations.

## Architecture

- AI routing matrix: basic intro → Inbox; corporate PR → reject or Inbox; practical applied tool → recommendation allowed; major release or hardware analysis → recommendation allowed; low-level training-internals theory → reject.

- Hono Cloudflare Worker in `src/`.
- Vite/Preact/TypeScript client in `client/`.
- Base schema: `schema.sql`.
- Migration order: `migrations/0000_brain.sql`, `migrations/0001_production_rebuild.sql`, `migrations/0002_rss_feeds.sql`, `migrations/0003_feedback_review.sql`, `migrations/0004_discovery_engine.sql`, `migrations/0005_recommendation_notebook_url.sql`, `migrations/0006_hermes_upgrade.sql`, `migrations/0007_sync_notifications.sql`, `migrations/0008_compass_cascade.sql`, `migrations/0009_proposal_dedup.sql`, `migrations/0010_compass_queue_fill.sql`, `migrations/0011_compass_adaptive_learning.sql`, `migrations/0012_context_brief.sql`, then `migrations/0013_book_visual_chapters.sql`.
- API contract: `docs/API.md`.

## Invariants

- All captures, including share target, Telegram, RSS, and Atom, land in unlimited Inbox. Enabled feeds refresh every six hours.
- Feed orchestration uses `~/.hermes/skills/productivity/rss-feed/SKILL.md`; user-requested refreshes send `{ "limit": 5 }`, paginate imported history, and never bypass Inbox triage.
- Archive keeps feed captures in a pinned RSS/Atom shelf; use `GET /recommendations/list?source=manual` when reporting the clean manual archive.
- Queue contains only queued/in-progress items and normally caps at five.
- Queue owns start, resume, and completion. Hidden sessions preserve linkage; `POST /sessions/:id/return` with `reflection`, `rating`, and `complete:true` closes the handoff and auto-creates the linked reflection note.
- Structured bilingual notes live on the site.
- Lite Visual resolves the exact source/pair, mines the complete source, builds a checksum-backed evidence packet and coverage matrix, and separates coverage from design passes.
- Lite Visual validates measured metadata, source anchors, responsive HTML, and the six-page PDF inspection set; HTML must score at least 8/10 with no factual/rendering defect.
- Lite Visual uploads a versioned linked HTML+PDF R2 pair with revision/supersession metadata and queues Notes Extractor from its HTML artifact only, exactly once; the extraction job and canonical source record must reach verified terminal state.
- `visualise_source` is enqueue-only from the Worker: its payload declares `custom_prompt_required`, `qa_required`, `quality_threshold: 8`, and expected `html`/`pdf` roles. Book payloads additionally require chapterized output with matching `chapter_key`, `chapter_title`, `chapter_number`, and `pair_id` metadata on each HTML/PDF pair. Hermes owns source-specific prompting, mining, rendering, validation, upload, and completion. Artifact publication must satisfy the machine-readable HTML/PDF QA contract before release.
- Book visualisation requests add `book_mode=true`, `visual_mode=book_annotation_companion`, and `chapter_outputs=true`; Hermes creates one linked HTML/PDF pair per chapter with stable chapter metadata, mines complete chapters, and queues extraction once per HTML chapter. The Books shelf exposes both formats and stores finished state per chapter. Repeated requests for one source reuse the idempotent job.
- **Books chapter UI**: The Books page expands/collapses chapters on book title click. Each chapter row shows number badge, title, status, file links (HTML/PDF/NBLM), upload buttons for missing files, and Finish/Undo toggle. Chapter file uploads use existing `POST /artifacts` endpoint with chapter metadata (`chapter_key`, `chapter_title`, `chapter_number`, `role`, `pair_id`).
- Lite Visual PDFs use portrait-tablet typography: 15pt body text, readable supporting text, portrait-safe margins, and single-column print layouts even if that increases page count.
- Visual Mind assets use contract v2 (`~/.hermes/skills/visual-mind/references/visual-contract.schema.json`): SHA-256 identity, source anchors, typed placements, learning roles, style locks, and critic receipts. Gemini API is the sole OCR/review/image-generation provider; deterministic Mermaid/SVG/chart rendering and PIL QA remain allowed, while local OCR, alternate providers, and local-generation fallbacks are forbidden. Lite Visual embeds manifest assets as Base64 Data URIs and validates each visual exactly once at its placement.
- A reflection supplied to Hermes is explicit feedback: `learning-compass-operating-system` preserves it verbatim in the canonical source record, queues exactly one Taste Mapper analysis, applies only evidence-qualified profile/map/scoring proposals during that conversation, and leaves system-layer or uncertain proposals reviewable in Learn → Activity. If the source is untracked, capture it and create the completed source record first. It never follows feedback with a recommendation.
- Ratings 7–10 automatically queue Notes Extractor, which writes a complete bilingual English/Egyptian-Arabic source note covering the full source plus editable SRS drafts. User approval activates cards; drafts and approved cards remain deletable.
- Source notes expose a full bilingual re-run action when an older extraction is incomplete.
- Hermes model order is explicit and verified: OpenCode Go `deepseek-v4-flash`, OpenCode Zen `deepseek-v4-flash-free`, OpenAI Codex `gpt-5.6-luna`, then Nous `tencent/hy3:free`.
- Handwritten PDF annotations are personal reflection data: preserve them with page anchors, flag uncertain OCR, and never merge printed source claims into the user's voice.
- Feedback never triggers a recommendation automatically.
- Personal Bayesian Cascade is the canonical recommendation workflow: submit 3 candidates first and expand only after a justified abstention up to 8, send structured metadata/evidence without scores, let the Worker own reachability checks, semantic deduplication, exclusions, D1-context ranking and calibrated confidence, and require explicit Start plus explicit feedback. Compass completion, decline, and abandonment atomically place the source in completed or excluded history; written feedback/rating is retained and creates one review-gated Taste Mapper job. `GET /compass/pick` returns the newest active ready/started pick for compatibility. For an explicit Queue-fill request, continue creating picks while the active queued/in-progress count is below five and explicitly start each ready pick; `POST /compass/picks` never auto-starts. `/discovery` and `/ai/suggest` remain compatibility/archive paths and feedback never auto-chains a new recommendation.
- NotebookLM is the grounded knowledge corpus for Hermes. Use notebook `https://notebook.google.com/notebook/2c8a58a9-32b8-45db-804f-b48bf756e82c`.
- NotebookLM sync is feedback-driven and Hermes-controlled: when `taste-rec` recommends an item and Mahmood later gives explicit feedback, `taste-mapper` sends the complete relevant corpus update and feedback context to NotebookLM during that workflow. Do not create a background D1-to-NotebookLM sync job for every mutation.
- Send clean original source material and Mahmood's own reflections, notes, ratings, and feedback. Exclude generated Egyptian-Arabic translations, generated summaries, Lite Visual HTML/PDF companions, and any AI-written text unless Mahmood explicitly confirms it is his own thought. Use the original source URL/transcript/PDF/web text instead of Lite Visual output.
- NotebookLM queries must identify the role as Mahmood's grounded Taste Strategist and Chief Knowledge Officer, include the complete available relevant corpus, enforce all taste exclusions, cite exact source evidence, and flag contradictions. Studio generation is never automatic; explicit requests execute immediately. For a Learning Compass source-specific request, create a fresh per-source notebook, add the link as a Website source, start the requested artifact without downloading/uploading or waiting, and immediately save that notebook URL on the matching site item. Direct artifact requests may use the normal deduped wait/download flow.
- NotebookLM interactions use pinned `PleasePrompto/notebooklm-mcp@2.0.0` (`notebooklm_*` MCP tools) as the primary engine and `teng-lin/notebooklm-py` as the direct Python API & CLI backup engine. Playwright DOM automation is completely removed.
- Hermes quality evidence is recorded in `recommendation_outcomes`; guarded memory is stored in `hermes_memory`; failed jobs use `agent_job_retries` with backoff, dead-letter alerts, and replay.
- Insights → Hermes is the operational control surface for queue health, prediction error, engine evidence, alerts, failures, and memory state.
- Insights → Hermes includes memory review: it searches, approves, rejects, expires, and inspects evidence for Hermes memories; source records expose `memory_influences`. `/analytics/hermes/weekly` reports evaluator signals and `/analytics/hermes/evaluate` creates only canonically supported proposals; only evidence-qualified profile/map/scoring proposals may be applied during the active conversation. `/analytics/hermes/backfill` is dry-run by default and recalibration remains gated by five real rated outcomes. NotebookLM broker health is explicit through `/notebooklm/health` and `/notebooklm/recover`; stale or missing heartbeats are never reported as grounded.
- Hermes claims only the durable work created by an explicit Learning Compass request; there is no scheduled host poller and job operations are not shown in the site.
- Every Hermes conversation ends with one evidence-based self-improvement pass. Automatic changes include evidence-qualified profile/map/scoring learning and narrowly scoped, reversible skill-procedure memory when a skill failure or better path is validated by replay/test evidence. Actual skill-source edits, prompts, code, schema, runtime, and workflow changes remain proposal-only; deployment, destructive deletion, and external publication are explicit-only. Persist the improvement to D1 `hermes_memory` with evidence, validation, scope, and supersession; avoid mutation without evidence and never create cron/background self-improvement.
- Use one stable worker name for claim, heartbeat, complete, and fail calls. Keep `Mozilla/5.0 (compatible; HermesCron/1.0)` in the retained manual runner.
- E2E tests own and terminate Wrangler, Workerd, and Playwright processes.

## Commands

```bash
npm test
npm run build
npm run test:e2e
npx wrangler deploy --config wrangler.toml
```

Deploy only source/schema/client changes after an explicit deployment instruction. D1/R2 data writes do not require deployment.

## Hermes synchronization

### Procedural operating system

- `~/.hermes/skills/workflow/learning-compass-operating-system/SKILL.md` is the only entry point for Learning Compass chat. It classifies requests into reflection/feedback, session, capture/triage, recommendation, notes/artifact, proposal review, feed, job recovery, or product-operation procedures.
- The router calls one focused specialist after classification. `learning-compass-site-operator` executes the verified Worker API steps; specialist skills never infer a competing workflow.
- Reflection text is preserved verbatim through one `POST /feedback/record`. Numeric ratings mean completion unless explicitly provisional. The response is the canonical receipt for tracked and untracked sources.

### Agent control protocol

- Read `GET /agent/capabilities` before acting; it is the source of truth for methods and paths.
- Use `POST /agent/request` with `{method,path,body}`, or `/agent/tools` and `/agent/tool-call` for function-calling clients.
- Use `GET /learning/balance?window=30|90|365` for attention/retention balance across map depth; it is the same read model shown in Coverage and summarized in `/agent/context` for Compass decisions.
- To close the mapping gap, Hermes reads balance plus consumed history, matches completed sources to existing `/brain/tree` nodes, calls `POST /recommendations/map`, and verifies each source record plus the reduced unmapped count. Ambiguous matches remain unresolved; mapping never changes completion, queue state, or reflections.
- Send `x-agent-name` for audit attribution and `x-api-token` for authenticated writes.
- Never guess routes, use arbitrary SQL, or use an external proxy. Preserve queue, SRS, D1/R2, feedback, and Lite Visual invariants.

When product API, schema, algorithm, or workflow contracts change, update:

- `~/.hermes/skills/workflow/recommendations-worker-ops/SKILL.md`
- `~/.hermes/skills/workflow/learning-compass-operating-system/SKILL.md`
- `~/.hermes/skills/workflow/learning-compass-site-operator/SKILL.md`
- `~/.hermes/skills/taste-mapper/SKILL.md`
- `~/.hermes/skills/personal/taste-rec/SKILL.md`
- `~/.hermes/skills/taste-enhancer/SKILL.md`
- any other active skill that references the changed contract
- durable rules in `~/.hermes/memories/MEMORY.md` or `USER.md` only when the change is a durable user/workflow rule

Do not restore the deleted template-string frontend, `schema_v2.sql`, or obsolete artifact scripts.
After a material change, update `CURRENT_STATE.md`; update `PROJECT_CONTEXT.md` only when a stable contract changes.
